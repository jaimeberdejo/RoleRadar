---
phase: 02-normalizacion-y-deduplicacion
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app/dedup/embedder.py
  - app/dedup/exact.py
  - app/dedup/semantic.py
  - app/dedup/merger.py
  - tests/dedup/test_dedup_units.py
autonomous: true
requirements: [DEDUP-01, DEDUP-02, DEDUP-03]
must_haves:
  truths:
    - "Importar app.dedup.embedder NO importa torch ni sentence_transformers (lazy import)"
    - "Existe el protocolo Embedder y dos implementaciones: BgeM3Embedder (lazy) y FakeEmbedder (test)"
    - "El clustering exacto agrupa ofertas con misma empresa+título normalizados"
    - "El clustering semántico agrupa por coseno ≥ umbral usando vectores L2-normalizados (E@E.T)"
    - "merge_group conserva la oferta con descripción más completa y acumula urls_alternativas"
  artifacts:
    - path: "app/dedup/embedder.py"
      provides: "Embedder Protocol + BgeM3Embedder (lazy) + FakeEmbedder"
      contains: "class Embedder"
    - path: "app/dedup/exact.py"
      provides: "exact_group(): nivel-1 hash exacto por empresa+título normalizados"
      contains: "def exact_group"
    - path: "app/dedup/semantic.py"
      provides: "build_embed_text() + semantic_cluster() greedy coseno (numpy)"
      contains: "def semantic_cluster"
    - path: "app/dedup/merger.py"
      provides: "merge_group(): canónica = descripción más larga + urls_alternativas"
      contains: "def merge_group"
    - path: "tests/dedup/test_dedup_units.py"
      provides: "Tests unitarios DEDUP-01..03 + invariante lazy-import"
      min_lines: 50
  key_links:
    - from: "app/dedup/semantic.py"
      to: "app.dedup.embedder.Embedder"
      via: "embedder.embed(texts) inyectado; numpy E@E.T para coseno"
      pattern: "embedder.embed"
    - from: "app/dedup/merger.py"
      to: "Job.urls_alternativas"
      via: "model_copy(update=...) Pydantic v2"
      pattern: "model_copy"
---

<objective>
Construir los MÓDULOS HOJA de deduplicación (sin el facade público, que lo cablea Plan 04):
el `Embedder` (protocolo + BgeM3Embedder con lazy import de sentence-transformers + FakeEmbedder
determinista para tests), el nivel-1 de hash exacto (`exact.py`), el nivel-2 semántico greedy por
coseno con numpy (`semantic.py`), y el merger que elige la oferta canónica y acumula urls
alternativas (`merger.py`). Tests unitarios de cada pieza, incluyendo la invariante crítica:
importar el embedder NO debe arrastrar torch.

Purpose: es la segunda mitad de Phase 2 (el motor de dedup). Implementa DEDUP-01..03 a nivel de
componente. Plan 04 los ensambla en el facade `deduplicate()` y añade el test end-to-end (QA-03).
Output: app/dedup/embedder.py, exact.py, semantic.py, merger.py, tests/dedup/test_dedup_units.py.

Este plan corre en PARALELO con Plan 02 (normalización): no comparten ficheros (Plan 02 crea
__init__.py + normalize.py + mappers.py; este plan NO toca ninguno de esos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-normalizacion-y-deduplicacion/02-RESEARCH.md
@.planning/phases/02-normalizacion-y-deduplicacion/02-PATTERNS.md
@app/models/schemas.py
@app/cv/llm_client.py

<interfaces>
<!-- Schema Job (usar tal cual; urls_alternativas YA existe — NO modificar schemas.py) -->
From app/models/schemas.py:
```python
class Job(BaseModel):
    id: str; title: str; company: str
    location: Optional[str] = None
    remote: RemoteJob = RemoteJob.unknown
    description: str = ""
    url: Optional[str] = None
    source: str
    raw: dict = Field(default_factory=dict)
    urls_alternativas: list[str] = Field(default_factory=list)
```

<!-- normalize_field lo crea Plan 02 (mismo paquete app/dedup/). exact.py lo importa. -->
<!-- Si Plan 02 aún no ha mergeado cuando se ejecute este plan, exact.py debe importar -->
<!-- de app.dedup.normalize igualmente (la firma está fijada en RESEARCH.md Pattern 2): -->
From app/dedup/normalize.py (creado por Plan 02):
```python
def normalize_field(s: str) -> str: ...   # NFD strip + casefold + colapsa espacios
```

<!-- Patrón Phase 1 a replicar: lazy import + seam inyectable (app/cv/llm_client.py) -->
build_instructor_client() importa instructor a nivel de módulo (es dep ligera);
para el embedder, sentence_transformers es PESADA → import SOLO dentro de _ensure_loaded().

<!-- Fixtures disponibles desde Plan 01 (tests/conftest.py) -->
- fake_embedder_identico() -> FakeEmbedder    (vector constante → similitud 1.0)
- fake_embedder_ortogonal() -> FakeEmbedder   (vectores ortogonales → similitud 0.0)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: embedder.py (Embedder Protocol + BgeM3Embedder lazy + FakeEmbedder) con test de invariante</name>
  <read_first>
    - app/cv/llm_client.py (seam inyectable; contraste: ahí los imports SON top-level — aquí van DENTRO del método)
    - .planning/phases/02-normalizacion-y-deduplicacion/02-RESEARCH.md (Pattern 1: Lazy Import for BgeM3Embedder; Pattern 5: normalize_embeddings; Pitfall 1: torch imports at module load; Pitfall 3: model download)
    - .planning/phases/02-normalizacion-y-deduplicacion/02-PATTERNS.md (sección embedder.py — EXACT ANALOG)
  </read_first>
  <files>app/dedup/embedder.py, tests/dedup/test_dedup_units.py</files>
  <behavior>
    - `import app.dedup.embedder` NO importa torch ni sentence_transformers (no aparecen en sys.modules
      tras el import si no estaban antes).
    - FakeEmbedder(default_vector=v).embed(["a","b","c"]) devuelve un np.ndarray shape (3, dim) float32
      donde todas las filas son v (→ similitud coseno 1.0 entre filas).
    - FakeEmbedder(vectors={...}).embed([texto_conocido]) devuelve el vector pre-especificado para ese texto.
    - FakeEmbedder con texto NO mapeado y sin default → vector determinista derivado del hash del texto
      (mismo texto → mismo vector entre llamadas).
    - BgeM3Embedder satisface el Protocol Embedder (tiene método embed) — verificable por estructura,
      SIN instanciar el modelo real (no descargar BGE-M3 en tests).
  </behavior>
  <action>
    Crear `app/dedup/embedder.py` siguiendo Pattern 1 de RESEARCH.md (implementación verificada):
    - `from __future__ import annotations`; `from typing import Protocol`; `import numpy as np`.
      NUNCA importar sentence_transformers a nivel de módulo (Pitfall 1 — invariante crítica).
    - `class Embedder(Protocol)`: método `embed(self, texts: list[str]) -> np.ndarray` (docstring:
      devuelve array (n, dim) float32 L2-normalizado).
    - `class BgeM3Embedder`: `__init__` deja `self._model = None`; `_ensure_loaded()` hace
      `from sentence_transformers import SentenceTransformer  # noqa: PLC0415` y carga
      `SentenceTransformer("BAAI/bge-m3")` la primera vez; `embed()` llama
      `self._model.encode(texts, normalize_embeddings=True, batch_size=32, show_progress_bar=False)`.
    - `class FakeEmbedder`: `__init__(self, vectors=None, default_vector=None, dim=4)`; `embed()` para
      cada texto: si está en `vectors` usa ese vector; elif `default_vector` no es None lo usa; else
      genera vector determinista por `hashlib.sha256(text).hexdigest()` → seed de np.random + L2-normaliza.
      Devuelve `np.array(result, dtype=np.float32)`.
    Docstrings en español, type hints completos.

    Crear `tests/dedup/test_dedup_units.py` con (entre otros) el TEST DE INVARIANTE:
    - test que importa app.dedup.embedder y afirma que "torch" y "sentence_transformers" NO están en
      sys.modules después (capturando el estado previo y comparando). Este test es la red de seguridad
      anti-torch (Pitfall 1).
    - tests de FakeEmbedder: shape correcta, vector constante con default_vector, vector mapeado por dict,
      determinismo del fallback por hash.
    - test estructural de que BgeM3Embedder expone `embed` (sin instanciar el modelo real).
  </action>
  <verify>
    <automated>uv run pytest tests/dedup/test_dedup_units.py -q -k "embedder or invariante or lazy or fake"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class Embedder\|class BgeM3Embedder\|class FakeEmbedder" app/dedup/embedder.py` devuelve 3
    - El import de sentence_transformers NO está a nivel de módulo: `grep -n "import sentence_transformers\|from sentence_transformers" app/dedup/embedder.py` solo muestra líneas INDENTADAS (dentro de _ensure_loaded), nunca en columna 0
    - `grep -c "noqa: PLC0415" app/dedup/embedder.py` devuelve ≥ 1 (lazy import marcado)
    - `grep -c "normalize_embeddings=True" app/dedup/embedder.py` devuelve ≥ 1
    - `uv run python -c "import sys; import app.dedup.embedder; assert 'torch' not in sys.modules and 'sentence_transformers' not in sys.modules"` no falla (invariante lazy)
    - Tests de embedder/invariante pasan en verde
  </acceptance_criteria>
  <done>Embedder Protocol + BgeM3Embedder (lazy, torch fuera del import graph) + FakeEmbedder deterministas; test de invariante anti-torch verde (DEDUP-02 base).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: exact.py + semantic.py + merger.py (niveles de dedup) con tests unitarios</name>
  <read_first>
    - app/cv/cache.py (estilo dict-keyed, imports stdlib, logger)
    - .planning/phases/02-normalizacion-y-deduplicacion/02-RESEARCH.md (Pattern 4: Greedy Cosine Clustering; Pitfall 4: umbral; Pitfall 5: orden greedy)
    - .planning/phases/02-normalizacion-y-deduplicacion/02-PATTERNS.md (secciones exact.py, semantic.py, merger.py + "Pydantic v2 model_copy")
  </read_first>
  <files>app/dedup/exact.py, app/dedup/semantic.py, app/dedup/merger.py, tests/dedup/test_dedup_units.py</files>
  <behavior>
    - exact_group: 3 jobs donde 2 tienen misma empresa+título normalizados (p.ej. "Acme"/"acme",
      "AI Engineer"/"ai engineer") → 2 grupos (uno con 2 jobs, otro con 1).
    - build_embed_text(job, desc_words=50) -> "{title} {company} {primeras 50 palabras de description}";
      con description vacía no peta (degenera a "title company").
    - semantic_cluster con fake_embedder_identico (todos iguales) → 1 solo cluster con todos los índices.
    - semantic_cluster con fake_embedder_ortogonal (similitud 0) y umbral 0.85 → cada job en su propio cluster.
    - semantic_cluster([]) -> [].
    - merge_group con 1 job → ese job sin cambios.
    - merge_group([corto, largo]) → canónica = la de descripción más larga; urls_alternativas contiene
      la url de la descartada; el original NO se muta (model_copy).
  </behavior>
  <action>
    Crear `app/dedup/exact.py` (Pattern de PATTERNS.md exact.py):
    - `exact_group(jobs: list[Job]) -> list[list[Job]]`: agrupa con `defaultdict(list)` por clave
      `normalize_field(job.company) + "|" + normalize_field(job.title)`; importa normalize_field de
      `app.dedup.normalize`. Devuelve `list(groups.values())`.

    Crear `app/dedup/semantic.py` (Pattern 4 de RESEARCH.md — implementación verificada):
    - `import numpy as np`; `from app.dedup.embedder import Embedder` (solo Protocol — no arrastra torch).
    - `build_embed_text(job, desc_words: int = 50) -> str`: `f"{job.title} {job.company} {snippet}".strip()`
      con `snippet = " ".join(job.description.split()[:desc_words])`.
    - `semantic_cluster(jobs, embedder: Embedder, umbral: float = 0.85, desc_words: int = 50) -> list[list[int]]`:
      si `not jobs` → `[]`; construye textos, `embeddings = embedder.embed(texts)`;
      `sim_matrix = embeddings @ embeddings.T`; `np.clip(sim_matrix, -1.0, 1.0, out=sim_matrix)` (Pitfall float);
      greedy: para cada i no asignado abre cluster, añade los j>i no asignados con `sim_matrix[i,j] >= umbral`.
      Devuelve lista de listas de índices.

    Crear `app/dedup/merger.py` (Pattern de PATTERNS.md merger.py):
    - `merge_group(group: list[Job]) -> Job`: si `len(group) == 1` devuelve `group[0]`;
      `canonical = max(group, key=lambda j: len(j.description))`;
      `alt = [j.url for j in group if j is not canonical and j.url]`;
      devuelve `canonical.model_copy(update={"urls_alternativas": list(canonical.urls_alternativas) + alt})`.
      (Pydantic v2 model_copy — NO mutar el original.)

    Ampliar `tests/dedup/test_dedup_units.py` con los tests de behavior arriba, usando las fixtures
    `fake_embedder_identico` y `fake_embedder_ortogonal` de conftest, y construyendo Jobs de prueba
    inline (con stable_job_id o ids fijos). Cubrir exact_group, build_embed_text (incl. description vacía),
    semantic_cluster (idéntico→1, ortogonal→N, vacío→[]), merge_group (1 job, más-larga-gana, urls acumuladas, no-mutación).
  </action>
  <verify>
    <automated>uv run pytest tests/dedup/test_dedup_units.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def exact_group" app/dedup/exact.py` devuelve 1
    - `grep -c "def semantic_cluster\|def build_embed_text" app/dedup/semantic.py` devuelve 2
    - `grep -c "@ embeddings.T\|np.clip" app/dedup/semantic.py` devuelve ≥ 2 (coseno por dot + clip de precisión)
    - `grep -c "def merge_group" app/dedup/merger.py` devuelve 1
    - `grep -c "model_copy" app/dedup/merger.py` devuelve ≥ 1 (Pydantic v2, no mutación)
    - `uv run python -c "import sys; import app.dedup.semantic; assert 'torch' not in sys.modules"` no falla (semantic no arrastra torch)
    - Todos los tests de tests/dedup/test_dedup_units.py pasan en verde
  </acceptance_criteria>
  <done>exact_group (DEDUP-01), semantic_cluster greedy coseno con numpy (DEDUP-02), merge_group descripción-más-larga + urls_alternativas vía model_copy (DEDUP-03); tests unitarios verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HuggingFace Hub → proceso | BgeM3Embedder descarga el modelo (~2.3 GB) en primer uso real; tests usan FakeEmbedder y nunca lo descargan |
| Job (datos ya normalizados) → dedup | Entrada ya validada por Pydantic en Plan 02; el dedup opera sobre objetos Job confiables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Denial of service | Import de torch en cada test / arranque | mitigate | Lazy import de sentence_transformers SOLO dentro de _ensure_loaded(); test de invariante afirma que torch no entra en sys.modules al importar embedder/semantic |
| T-02-08 | Denial of service | Descarga del modelo BGE-M3 disparada por dedup | mitigate | BgeM3Embedder solo carga el modelo en llamada real; tests inyectan FakeEmbedder; carga perezosa evita descargas accidentales en CI |
| T-02-09 | Tampering | Valores de coseno >1.0 por precisión float32 alterando el umbral | mitigate | np.clip(sim_matrix, -1.0, 1.0) antes de comparar contra umbral (Pitfall float documentado) |
</threat_model>

<verification>
- `uv run pytest tests/dedup/test_dedup_units.py -q` verde.
- `uv run pytest -q` (suite completa) verde tras este plan.
- Invariante: `import app.dedup.embedder` y `import app.dedup.semantic` NO introducen torch en sys.modules.
</verification>

<success_criteria>
- Embedder Protocol + BgeM3Embedder (lazy, anti-torch) + FakeEmbedder (DEDUP-02 motor).
- exact_group agrupa por hash exacto (DEDUP-01).
- semantic_cluster greedy coseno con numpy, clip de precisión (DEDUP-02).
- merge_group: descripción más completa como canónica + urls_alternativas acumuladas, sin mutación (DEDUP-03).
- tests/dedup/test_dedup_units.py verde, incluido el test de invariante lazy-import.
</success_criteria>

<output>
After completion, create `.planning/phases/02-normalizacion-y-deduplicacion/02-03-SUMMARY.md`
</output>
