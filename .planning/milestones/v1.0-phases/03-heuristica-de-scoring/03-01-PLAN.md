---
phase: 03-heuristica-de-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/schemas.py
  - tests/conftest.py
  - tests/scoring/__init__.py
autonomous: true
requirements: [SCORE-01]
must_haves:
  truths:
    - "Existe el schema LLMJobAssessment con razonamiento PRIMERO y SIN score_total/encaje_puesto/encaje_ubicacion/recommendation"
    - "PesosScoring rechaza pesos que no suman 1.0 usando tolerancia float (abs(total-1.0) < 1e-6)"
    - "Los tests de scoring pueden importar un mock del cliente LLM y Jobs/CVProfile/UserProfile de ejemplo desde fixtures"
  artifacts:
    - path: "app/models/schemas.py"
      provides: "LLMJobAssessment schema + PesosScoring.check_sum validator"
      contains: "class LLMJobAssessment"
    - path: "tests/scoring/__init__.py"
      provides: "paquete de tests de scoring"
    - path: "tests/conftest.py"
      provides: "fixtures de scoring: make_scoring_client, sample UserProfile/CVProfile/Jobs (remote/onsite-Barcelona/onsite-Madrid/out-of-ranking)"
      contains: "LLMJobAssessment"
  key_links:
    - from: "tests/conftest.py"
      to: "app.models.schemas.LLMJobAssessment"
      via: "import + MagicMock().messages.create.return_value"
      pattern: "LLMJobAssessment"
---

<objective>
Wave 0 de la fase NÚCLEO: poner los cimientos del scoring antes de cualquier lógica.
Añade el schema `LLMJobAssessment` (lo que el LLM evalúa, SIN los campos deterministas) y el
validador de suma=1.0 a `PesosScoring`, ambos en `app/models/schemas.py`. Crea el árbol
`tests/scoring/` y las fixtures de scoring en `tests/conftest.py` (cliente LLM mockeado que
devuelve un `LLMJobAssessment` controlado + Jobs/CVProfile/UserProfile de ejemplo que cubren
remote / onsite-Barcelona / onsite-Madrid / fuera-de-ranking).

Purpose: Sin el schema + las fixtures, ningún test de scoring puede escribirse ni correr. Esta
es la base sobre la que se montan las olas 1 y 2.
Output: schema LLMJobAssessment, PesosScoring.check_sum, tests/scoring/__init__.py, fixtures de
scoring en conftest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-heuristica-de-scoring/03-RESEARCH.md
@.planning/phases/03-heuristica-de-scoring/03-PATTERNS.md

<interfaces>
<!-- Contratos existentes en app/models/schemas.py que el nuevo schema debe respetar. -->
<!-- El executor usa estos directamente — no explorar el codebase. -->

Imports actuales de schemas.py (líneas 5-10):
```python
from __future__ import annotations
from enum import Enum
from typing import Optional
from pydantic import BaseModel, Field
```

PesosScoring actual (líneas 71-76) — SIN validador todavía:
```python
class PesosScoring(BaseModel):
    """Pesos del score_total. Deben sumar 1.0 (se valida al cargar)."""
    puesto: float = 0.35
    skills: float = 0.30
    ubicacion: float = 0.20
    seniority: float = 0.15
```

Desglose existente (líneas 138-142) — usa Field(ge=0, le=100), replicar para sub-scores LLM:
```python
class Desglose(BaseModel):
    encaje_puesto: int = Field(ge=0, le=100)
    encaje_skills: int = Field(ge=0, le=100)
    encaje_ubicacion: int = Field(ge=0, le=100)
    encaje_seniority: int = Field(ge=0, le=100)
```

Job (líneas 122-134): id, title, company, location:Optional[str], remote:RemoteJob,
description:str="", source:str.
RemoteJob enum: remote, hybrid, onsite, unknown.
ModalidadRemoto enum: remote, hybrid, onsite, indiferente.
CVProfile (líneas 106-111): experiencia, skills_tecnicas, formacion, anios_experiencia_total, dominios.
UserProfile (líneas 79-88): datos_personales, preferencias_ubicacion (ciudades_preferidas, pais,
dispuesto_a_reubicarse), preferencia_remoto (modalidad_ideal, acepta_onsite_solo_en),
ranking_puestos (list[PuestoRanking{titulo, sinonimos}]), deal_breakers, pesos:PesosScoring.

Patrón de mock LLM existente en conftest.py (líneas 67-77):
```python
mock_client = MagicMock()
mock_client.messages.create.return_value = EXPECTED_PROFILE
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir LLMJobAssessment + validador de PesosScoring a schemas.py</name>
  <read_first>
    - app/models/schemas.py (completo — modificar PesosScoring línea 71-76, añadir LLMJobAssessment tras JobScore línea 156)
    - .planning/phases/03-heuristica-de-scoring/03-RESEARCH.md Pattern 2 (campos exactos) + Pitfall 3 (float precision) + Pitfall 5 (campos prohibidos)
  </read_first>
  <files>app/models/schemas.py</files>
  <action>
    En app/models/schemas.py:

    (a) Modificar `PesosScoring` (líneas 71-76) para añadir un `@model_validator(mode="after")`
    que valide la suma con tolerancia float. Añadir al bloque de imports de pydantic:
    `from pydantic import BaseModel, Field, model_validator`. Implementar:
    ```python
    @model_validator(mode="after")
    def check_sum(self) -> "PesosScoring":
        total = self.puesto + self.skills + self.ubicacion + self.seniority
        if abs(total - 1.0) > 1e-6:
            raise ValueError(f"Los pesos deben sumar 1.0, suman {total:.6f}")
        return self
    ```
    Usar `abs(total - 1.0) > 1e-6` (NO `== 1.0` — por precisión float; 0.35+0.30+0.20+0.15 da
    0.9999999999999999, verificado en RESEARCH Pitfall 3).

    (b) Añadir la clase `LLMJobAssessment` DESPUÉS de `JobScore` (tras línea 156, antes de
    `ScoredJob`). Campos EXACTOS de RESEARCH.md Pattern 2, con `razonamiento` PRIMERO (fuerza
    chain-of-thought, SCORE-07 honestidad):
    - `razonamiento: str = Field(description="Razonamiento paso a paso: qué puesto detectas, qué skills coinciden, qué faltan, seniority pedido vs experiencia, deal-breakers en el texto. Sé HONESTO, no infles.")`
    - `puesto_detectado: str = Field(description="Nombre del puesto del ranking, usando sinónimos. O 'fuera de ranking' si ninguno encaja.")`
    - `rango_puesto: Optional[int] = Field(default=None, description="Posición en el ranking (1=top, 1-based). None si 'fuera de ranking'.")`
    - `encaje_skills: int = Field(ge=0, le=100, description="0-100: % de requisitos de la oferta que cumple el CVProfile real.")`
    - `encaje_seniority: int = Field(ge=0, le=100, description="0-100: nivel pedido vs experiencia real. Pedir mucho más → bajo (pero no 0 si es razonable).")`
    - `matched_skills: list[str] = Field(default_factory=list, description="Skills del CVProfile que la oferta pide explícitamente.")`
    - `missing_requirements: list[str] = Field(default_factory=list, description="Requisitos que el candidato podría no cumplir. Sin inflar.")`
    - `reasons_for: list[str] = Field(default_factory=list, description="Razones concretas de encaje. Máx 3-4.")`
    - `reasons_against: list[str] = Field(default_factory=list, description="Razones concretas de NO encaje. HONESTO. Máx 3-4.")`
    - `deal_breaker_hit_texto: bool = Field(default=False, description="True si el texto de la oferta activa algún deal-breaker del perfil.")`
    - `deal_breaker_cual_texto: Optional[str] = Field(default=None, description="Cuál deal-breaker se activó. None si deal_breaker_hit_texto=False.")`

    PROHIBIDO (Pitfall 5 — el LLM no controla los deterministas): NO añadir a LLMJobAssessment
    los campos `encaje_ubicacion`, `encaje_puesto`, `score_total` ni `recommendation`. El docstring
    de la clase: `"""Lo que el LLM evalúa. Solo campos que requieren comprensión de texto libre."""`

    Mantener estilo Pydantic v2, type hints, naming en español (per CLAUDE.md).
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo && .venv/bin/python -c "from app.models.schemas import LLMJobAssessment, PesosScoring; import pydantic; LLMJobAssessment(razonamiento='x', puesto_detectado='AI Engineer', rango_puesto=1, encaje_skills=80, encaje_seniority=70); PesosScoring(); 
try:
    PesosScoring(puesto=0.5, skills=0.5, ubicacion=0.5, seniority=0.5); raise SystemExit('FAIL: pesos invalidos aceptados')
except pydantic.ValidationError: pass
assert not any(f in LLMJobAssessment.model_fields for f in ('score_total','encaje_puesto','encaje_ubicacion','recommendation')), 'FAIL: campo determinista prohibido en LLMJobAssessment'
fields=list(LLMJobAssessment.model_fields); assert fields[0]=='razonamiento', 'FAIL: razonamiento debe ir primero'
print('OK')"</automated>
  </verify>
  <done>
    LLMJobAssessment importable con razonamiento como primer campo y sin campos deterministas;
    PesosScoring acepta defaults (0.35/0.30/0.20/0.15) y rechaza sumas != 1.0 con ValidationError.
  </done>
</task>

<task type="auto">
  <name>Task 2: Crear tests/scoring/ + fixtures de scoring en conftest.py</name>
  <read_first>
    - tests/conftest.py (completo — patrón de mock LLM líneas 67-77, patrón de fixtures de payloads)
    - data/profile.yaml (ranking real, deal_breakers, pesos — para construir el sample UserProfile coherente)
    - app/models/schemas.py (tras Task 1 — los schemas que las fixtures construyen)
    - .planning/phases/03-heuristica-de-scoring/03-PATTERNS.md sección "tests/scoring" (qué Jobs cubrir)
  </read_first>
  <files>tests/scoring/__init__.py, tests/conftest.py</files>
  <action>
    (a) Crear `tests/scoring/__init__.py` vacío (paquete de tests, análogo a tests/cv/__init__.py).

    (b) Añadir al FINAL de `tests/conftest.py` (sin tocar lo existente) las fixtures de scoring.
    Importar al inicio del fichero (junto a los imports existentes) los schemas necesarios:
    `from app.models.schemas import (CVProfile, Experiencia, Job, RemoteJob, ModalidadRemoto, UserProfile, DatosPersonales, PreferenciasUbicacion, PreferenciaRemoto, PuestoRanking, PesosScoring, LLMJobAssessment)`.

    Añadir:

    1. Constante módulo `EXPECTED_ASSESSMENT = LLMJobAssessment(...)` controlado y honesto:
       razonamiento="El candidato tiene Python y LLMs; la oferta pide exactamente eso.",
       puesto_detectado="Ingeniero de IA / AI Engineer", rango_puesto=1, encaje_skills=85,
       encaje_seniority=70, matched_skills=["Python","LLMs"], missing_requirements=[],
       reasons_for=["Match en skills principales"],
       reasons_against=["Requiere Kubernetes (no en CV)"],
       deal_breaker_hit_texto=False, deal_breaker_cual_texto=None.

    2. Helper module-level `make_scoring_client(assessment)` que devuelve un MagicMock con
       `mock.messages.create.return_value = assessment` (mismo patrón que mock_llm_client líneas 67-77).

    3. Fixture `mock_scoring_client` (function scope) que devuelve `make_scoring_client(EXPECTED_ASSESSMENT)`.

    4. Fixture `sample_cv_profile` → CVProfile con skills_tecnicas=["Python","LLMs","FastAPI"],
       experiencia=[Experiencia(empresa="Acme Corp", rol="AI Engineer", duracion="2 años",
       tecnologias=["Python","LLMs"])], anios_experiencia_total=2.0, dominios=["IA"].

    5. Fixture `sample_user_profile` → UserProfile coherente con data/profile.yaml:
       ciudades_preferidas=["Barcelona"], pais="España", dispuesto_a_reubicarse=False;
       preferencia_remoto.modalidad_ideal=ModalidadRemoto.remote, acepta_onsite_solo_en=["Barcelona"];
       ranking_puestos = los 4 puestos reales de profile.yaml con sus sinónimos (AI Engineer / ML
       Engineer / Data Engineer / MLOps); deal_breakers=["exige 5+ años de experiencia",
       "presencial fuera de Barcelona"]; pesos=PesosScoring() (defaults). datos_personales mínimos
       válidos (nombre, email, ubicacion_actual="Barcelona, España").

    6. Cuatro fixtures de Job que cubren las ramas de ubicación/ranking (cada uno con id, title,
       company, location, remote, description, source):
       - `sample_job_remote` → remote=RemoteJob.remote, location="Remote", title="AI Engineer".
       - `sample_job_onsite_barcelona` → remote=RemoteJob.onsite, location="Barcelona, España",
         title="AI Engineer".
       - `sample_job_onsite_madrid` → remote=RemoteJob.onsite, location="Madrid, España",
         title="AI Engineer".
       - `sample_job_fuera_ranking` → remote=RemoteJob.remote, location="Remote",
         title="Frontend Designer" (para casos rango_puesto=None).

    Comentar (como en las fixtures de dedup) que estas fixtures son consumidas por
    tests/scoring/test_location.py y test_scorer.py de las olas 1-2.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo && .venv/bin/python -m pytest tests/ -q 2>&1 | tail -5 && .venv/bin/python -c "import importlib.util,os; assert os.path.exists('tests/scoring/__init__.py'); spec=importlib.util.spec_from_file_location('cf','tests/conftest.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); a=m.make_scoring_client(m.EXPECTED_ASSESSMENT); assert a.messages.create.return_value.rango_puesto==1; print('OK fixtures')"</automated>
  </verify>
  <done>
    tests/scoring/__init__.py existe; `pytest tests/ -q` sigue verde (los 87 tests previos no se
    rompen); make_scoring_client + EXPECTED_ASSESSMENT importables desde conftest; las 4 fixtures
    de Job y sample_user_profile/sample_cv_profile disponibles para las olas siguientes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile.yaml → UserProfile | Datos de configuración local del usuario (confiables, pero validados por Pydantic) |
| test fixtures → schemas | Datos de test, no entran a producción |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | LLMJobAssessment sub-scores fuera de rango | mitigate | `Field(ge=0, le=100)` en encaje_skills/encaje_seniority — Instructor reintenta si el LLM devuelve fuera de rango |
| T-03-02 | Elevation | LLM controlando campos deterministas (score_total/ubicacion/puesto) | mitigate | LLMJobAssessment NO contiene esos campos por diseño — arquitectónicamente imposible que el LLM los fije |
| T-03-03 | Tampering | pesos inválidos en profile.yaml dan score_total sesgado | mitigate | PesosScoring.check_sum valida suma=1.0 con tolerancia float al construir el modelo |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/ -q` verde (no regresión de los 87 tests previos).
- LLMJobAssessment importable, razonamiento primero, sin campos deterministas.
- PesosScoring rechaza sumas != 1.0.
- Fixtures de scoring (mock client + sample profile/cv/jobs) importables.
</verification>

<success_criteria>
- LLMJobAssessment añadido a schemas.py con los 11 campos de RESEARCH Pattern 2 y razonamiento primero.
- PesosScoring valida suma=1.0 con tolerancia 1e-6.
- tests/scoring/ creado; fixtures de scoring en conftest cubren remote/onsite-BCN/onsite-Madrid/fuera-ranking.
- Suite completa verde.
</success_criteria>

<output>
After completion, create `.planning/phases/03-heuristica-de-scoring/03-01-SUMMARY.md`
</output>
