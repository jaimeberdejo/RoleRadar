---
phase: 05-observabilidad-robustez-docs
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app/errors.py
  - app/api/main.py
  - app/api/routes/jobs.py
  - app/scoring/llm.py
  - app/cv/llm_client.py
  - tests/api/test_exception_handlers.py
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]
must_haves:
  truths:
    - "Una excepción de capa (CVParseError/NormalizationError/ScoringError/StorageError) que propaga al router se convierte en un JSONResponse con código HTTP claro y envelope {error: {type, message}}"
    - "Una excepción genérica no controlada devuelve 500 con {error: {type, message: 'Internal server error'}} y NO filtra stack trace ni paths internos al cliente"
    - "El log final de /jobs/process incluye el campo llm_calls=N (un incremento por cada score_job exitoso)"
    - "configure_logging() se llama en el lifespan de la app al arrancar"
    - "assess_job y extract_cv_profile están envueltos con trace_llm (no-op por defecto, no rompen el scoring ni el parseo de CV)"
    - "La resiliencia de batch existente sigue intacta: una oferta mala va a errors[], no produce 500"
  artifacts:
    - path: "app/errors.py"
      provides: "Jerarquía de excepciones de dominio por capa (BuscadorError base)"
      contains: "class CVParseError"
    - path: "tests/api/test_exception_handlers.py"
      provides: "tests de cada excepción de capa → JSON limpio + código correcto + sin stack trace"
  key_links:
    - from: "app/api/main.py"
      to: "app/errors.py"
      via: "@app.exception_handler(CVParseError) etc., específicos ANTES del genérico Exception"
      pattern: "@app.exception_handler"
    - from: "app/api/main.py lifespan"
      to: "app/obs/logging_config.py"
      via: "configure_logging() (import deferido con # noqa: PLC0415)"
      pattern: "configure_logging"
    - from: "app/scoring/llm.py:assess_job"
      to: "app/obs/tracing.py:trace_llm"
      via: "with trace_llm('assess_job', ...): return client.messages.create(...)"
      pattern: "with trace_llm"
    - from: "app/api/routes/jobs.py:process_jobs"
      to: "logger.info"
      via: "llm_calls counter incrementado por score_job exitoso, incluido en el log final"
      pattern: "llm_calls"
---

<objective>
Cablear la robustez y la instrumentación sobre el código existente: (1) excepciones tipadas
por capa en app/errors.py (OBS-03), (2) exception handlers de FastAPI en main.py que las mapean
a JSON limpio sin filtrar stack traces, (3) llamar configure_logging() en el lifespan + añadir
el contador llm_calls al log de /jobs/process (OBS-01), y (4) envolver los dos puntos de
llamada LLM (assess_job, extract_cv_profile) con trace_llm (OBS-02). Cambios quirúrgicos sobre
ficheros existentes — la lógica de negocio NO cambia.

Purpose: Hacer el servicio resiliente por capas y observable sin alterar su comportamiento. Los
handlers de capa interceptan errores de infraestructura/config que NO deben caer en el catch
batch-resiliente interno; el no-op de trace_llm garantiza que envolver los call sites es
transparente cuando langfuse no está configurado (el caso por defecto en tests/dev).
Output: app/errors.py nuevo + 4 ficheros modificados + test de exception handlers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-observabilidad-robustez-docs/05-RESEARCH.md
@.planning/phases/05-observabilidad-robustez-docs/05-PATTERNS.md
@app/api/main.py
@app/api/routes/jobs.py
@tests/api/conftest.py

<interfaces>
<!-- app/obs creado en Plan 01 — usar directamente, sin explorar -->
From app/obs/tracing.py (Plan 01):
```python
@contextmanager
def trace_llm(name: str, **meta: Any) -> Generator[None, None, None]: ...  # no-op por defecto
```
From app/obs/logging_config.py (Plan 01):
```python
def configure_logging(level: int = logging.INFO) -> None: ...  # idempotente
```

<!-- app/errors.py a crear — jerarquía verificada en RESEARCH Code Examples -->
```python
class BuscadorError(Exception): ...
class CVParseError(BuscadorError): ...
class NormalizationError(BuscadorError): ...
class ScoringError(BuscadorError): ...
class StorageError(BuscadorError): ...
```

<!-- main.py: lifespan a extender (L26-45) + registrar handlers entre FastAPI(...) (L48-53) y include_router (L55-57) -->
<!-- ORDEN CRÍTICO: handlers específicos ANTES del genérico Exception (FastAPI usa el primero que matchea) -->

<!-- jobs.py process_jobs: loop de scoring L221-243. assess_job hace exactamente 1 client.messages.create por job -->
<!-- assess_job firma: assess_job(job, cv_profile, user_profile, client) -> LLMJobAssessment (app/scoring/llm.py L151) -->
<!-- extract_cv_profile firma: extract_cv_profile(raw_text, client) -> CVProfile (app/cv/llm_client.py L30) -->

<!-- Test scaffolding existente: api_client fixture en tests/api/conftest.py L72-108 (TestClient + dependency_overrides + reset) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: app/errors.py (jerarquía de excepciones por capa) + cableado de tracing y logging en los call sites</name>
  <read_first>
    - .planning/phases/05-observabilidad-robustez-docs/05-RESEARCH.md (Code Examples: app/errors.py + wrapping assess_job)
    - app/scoring/llm.py (call site assess_job L151-199)
    - app/cv/llm_client.py (call site extract_cv_profile L30-61)
    - app/api/routes/jobs.py (loop process_jobs L221-243, log final L237-243)
  </read_first>
  <files>app/errors.py, app/scoring/llm.py, app/cv/llm_client.py, app/api/routes/jobs.py</files>
  <action>
    1. Crear `app/errors.py` con `from __future__ import annotations` y la jerarquía EXACTA
       (RESEARCH Code Examples). `class BuscadorError(Exception):` con docstring "Base exception
       para todos los errores de dominio de BuscadorDeEmpleo." + 4 subclases con solo docstring
       (NO campos extra — str(exc) es suficiente para el handler): `CVParseError`,
       `NormalizationError`, `ScoringError`, `StorageError`, todas heredando de BuscadorError.
       Docstrings en español describiendo la capa de cada una.

    2. Envolver el call site de scoring en `app/scoring/llm.py`: añadir
       `from app.obs.tracing import trace_llm` al top del fichero (junto a los otros imports).
       Dentro de `assess_job`, tras computar `model`, envolver el `return client.messages.create(...)`
       en `with trace_llm("assess_job", job_id=job.id, model=model):` (indentar el return dentro
       del with). La firma de assess_job NO cambia. (RESEARCH Code Examples / PATTERNS app/scoring/llm.py).

    3. Envolver el call site de CV en `app/cv/llm_client.py`: añadir
       `from app.obs.tracing import trace_llm`. Dentro de `extract_cv_profile`, tras computar
       `model`, envolver el `return client.messages.create(...)` en
       `with trace_llm("extract_cv_profile", model=model):`. Firma sin cambios.

    4. Añadir el contador llm_calls en `app/api/routes/jobs.py:process_jobs` (diff mínimo según
       PATTERNS): inicializar `llm_calls = 0` antes del loop (L221); incrementar `llm_calls += 1`
       DENTRO del try, justo tras un `score_job(...)` exitoso (un score_job = exactamente 1 llamada
       LLM — Open Q1 resuelta); añadir `llm_calls=%d` al fmt del `logger.info` final (L237-243) y
       pasar `llm_calls` como argumento posicional en la posición correcta (entre `len(scored)` y
       `len(all_errors)`). NO tocar el orden ya_visto-antes-de-upsert ni la lógica de dedup/persist.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/scoring/ tests/cv/ tests/api/test_process.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class CVParseError(BuscadorError)" app/errors.py` y las 4 subclases + BuscadorError presentes
    - `grep -q "with trace_llm(\"assess_job\"" app/scoring/llm.py`
    - `grep -q "with trace_llm(\"extract_cv_profile\"" app/cv/llm_client.py`
    - `grep -q "llm_calls" app/api/routes/jobs.py` y `grep -q "llm_calls=%d" app/api/routes/jobs.py`
    - `uv run python -m pytest tests/scoring/ tests/cv/ tests/api/test_process.py -q` pasa (scoring/CV no se rompen — trace_llm es no-op en tests)
  </acceptance_criteria>
  <done>app/errors.py existe con la jerarquía; assess_job y extract_cv_profile envueltos con trace_llm sin cambiar firmas; process_jobs cuenta y loguea llm_calls; tests de scoring/cv/process verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exception handlers + configure_logging() en app/api/main.py + test de handlers</name>
  <read_first>
    - .planning/phases/05-observabilidad-robustez-docs/05-RESEARCH.md (Pattern 2: FastAPI Exception Handlers + Pitfalls 4, 5)
    - .planning/phases/05-observabilidad-robustez-docs/05-PATTERNS.md (sección app/api/main.py + TestClient/dependency_overrides)
    - app/api/main.py (lifespan L26-45, app instance L48-57)
    - tests/api/conftest.py (api_client fixture L72-108)
  </read_first>
  <files>app/api/main.py, tests/api/test_exception_handlers.py</files>
  <behavior>
    - Test CVParseError → status 422, body == {"error": {"type": "CVParseError", "message": <str>}}.
    - Test NormalizationError → status 422, envelope con type "NormalizationError".
    - Test ScoringError → status 422, envelope con type "ScoringError".
    - Test StorageError → status 503, envelope con type "StorageError".
    - Test Exception genérica → status 500, body == {"error": {"type": <clase>, "message": "Internal server error"}}; el body NO contiene el str original de la excepción ni ningún path de fichero (anti information disclosure — Pitfall 5).
    - Test regresión batch: /jobs/process con una oferta mala sigue devolviendo 200 con errors[] (NO 500) — los handlers no rompen la resiliencia de batch (Pitfall 4).
  </behavior>
  <action>
    1. En `app/api/main.py`:
       - En el lifespan (L26-45), AÑADIR como PRIMERA acción (antes del import de SQLiteStorage):
         `from app.obs.logging_config import configure_logging  # noqa: PLC0415` y `configure_logging()`.
       - Tras `app = FastAPI(...)` (L48-53) y ANTES de los `app.include_router(...)` (L55-57),
         registrar los exception handlers. Añadir imports: `from fastapi import Request`,
         `from fastapi.responses import JSONResponse`, y `from app.errors import CVParseError,
         NormalizationError, ScoringError, StorageError`. Registrar EN ESTE ORDEN (específicos
         primero, genérico al final — ORDEN CRÍTICO Pitfall 4):
           - `@app.exception_handler(CVParseError)` → JSONResponse(status_code=422, content={"error": {"type": "CVParseError", "message": str(exc)}})
           - `@app.exception_handler(NormalizationError)` → 422, type "NormalizationError"
           - `@app.exception_handler(ScoringError)` → 422, type "ScoringError"
           - `@app.exception_handler(StorageError)` → 503, type "StorageError"
           - `@app.exception_handler(Exception)` (FALLBACK, EL ÚLTIMO) → `logger.exception("Unhandled exception: %s", exc)` (stack trace SOLO al log) y JSONResponse(status_code=500, content={"error": {"type": type(exc).__name__, "message": "Internal server error"}}). NUNCA poner str(exc) ni traceback en el body (Pitfall 5 — anti information disclosure).
       Cada handler es `async def` y recibe `(request: Request, exc: <Tipo>) -> JSONResponse`.

    2. Crear `tests/api/test_exception_handlers.py` con `from __future__ import annotations`.
       Para probar los handlers, los tests necesitan que una excepción de capa PROPAGUE hasta el
       router. Estrategia (usar el patrón dependency_overrides de tests/api/conftest.py): registrar
       rutas de prueba transitorias o sobreescribir un Depends de un endpoint existente para que
       lance la excepción tipada. Recomendado: añadir rutas mínimas de test al `app` dentro del
       test module mediante `@app.get("/__test_raise_cvparse")` que hacen `raise CVParseError("...")`,
       o más limpio: usar `app.add_api_route` en una fixture local y eliminarlas en teardown. Como
       alternativa robusta, sobreescribir `app.dependency_overrides` de un endpoint real
       (p.ej. get_storage de /jobs/history) con una lambda que lance StorageError para el test de
       storage. CRÍTICO: `with TestClient(app, raise_server_exceptions=False)` para el test de la
       Exception genérica (si no, TestClient re-lanza la excepción en lugar de dejar que el handler
       responda 500). SIEMPRE resetear `app.dependency_overrides = {}` y quitar las rutas de test en
       teardown (Pitfall 7 contaminación entre tests). Implementar los 6 tests del bloque <behavior>.
       Para el test anti-leak (genérica): assert que `"Internal server error"` está en el body Y que
       el mensaje real de la excepción NO está en el body.
       Para el test de regresión batch, reutilizar `api_client_with_cv` + un payload con una oferta
       mal formada y assert 200 + errors[] no vacío.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/api/test_exception_handlers.py tests/api/test_process.py tests/api/test_health.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m pytest tests/api/test_exception_handlers.py -q` pasa (6 tests verdes)
    - `grep -q "configure_logging()" app/api/main.py` (cableado en lifespan)
    - `grep -c "@app.exception_handler" app/api/main.py` retorna 5 (4 específicos + genérico)
    - El handler de `Exception` aparece DESPUÉS de los específicos: `python -c "import re,sys; t=open('app/api/main.py').read(); g=t.index('exception_handler(Exception)'); s=t.index('exception_handler(CVParseError)'); sys.exit(0 if g>s else 1)"` retorna 0
    - `grep -q "Internal server error" app/api/main.py` y `grep -q "logger.exception" app/api/main.py` (stack solo a log)
    - `uv run python -m pytest tests/api/test_process.py -q` pasa (regresión batch intacta)
  </acceptance_criteria>
  <done>Los 5 handlers registrados en orden correcto (específicos antes del genérico); configure_logging() en lifespan; el genérico no filtra stack trace; tests de handlers + regresión batch verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente (n8n) → API | inputs no confiables cruzan; las respuestas de error no deben filtrar detalles internos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | app/api/main.py generic_error_handler | mitigate | El handler genérico devuelve {"message": "Internal server error"} fijo; el stack trace va solo a logger.exception, nunca al body. Test anti-leak verifica que el str de la excepción NO aparece en la respuesta |
| T-05-04 | Denial of Service | app/api/routes/jobs.py process_jobs | accept | La resiliencia de batch existente (una oferta mala → errors[]) se preserva; los handlers de capa solo interceptan errores de infra que no deben silenciarse. Test de regresión confirma 200+errors[], no 500 |
</threat_model>

<verification>
- `uv run python -m pytest tests/api/ tests/scoring/ tests/cv/ -q` — capas instrumentadas sin regresiones
- `uv run python -m pytest -q` — suite completa verde (162 baseline + nuevos)
- Handlers específicos registrados antes del genérico (orden verificado por acceptance criteria)
</verification>

<success_criteria>
- app/errors.py con jerarquía de excepciones por capa (OBS-03)
- Exception handlers de FastAPI mapean cada excepción de capa a JSON limpio con código HTTP claro; genérico no filtra stack trace (OBS-03)
- configure_logging() llamado en lifespan; /jobs/process loguea llm_calls=N (OBS-01 completo)
- assess_job y extract_cv_profile envueltos con trace_llm sin cambiar comportamiento (OBS-02 cableado)
- Resiliencia de batch intacta; suite completa verde
</success_criteria>

<output>
After completion, create `.planning/phases/05-observabilidad-robustez-docs/05-02-SUMMARY.md`
</output>
