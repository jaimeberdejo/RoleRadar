---
phase: 05-observabilidad-robustez-docs
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - examples/arbeitnow_offers.json
  - examples/generic_offers.json
  - examples/process_request.json
  - tests/api/test_examples.py
  - README.md
  - .env.example
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]
must_haves:
  truths:
    - "examples/*.json son JSON válido y el payload de examples/process_request.json es aceptado por POST /jobs/process (LLM + embedder mockeados, devuelve 200 con results y errors)"
    - "El README cubre: qué es, instalación con uv, .env (cp .env.example .env), editar data/profile.yaml, subir el CV (POST /cv/parse), arrancar (uv run uvicorn app.api.main:app), correr tests, y el stack actualizado (SQLite local, sin Supabase ni OPENAI_API_KEY)"
    - "El README tiene una sección dedicada 'Integración con n8n' con el flujo (Schedule→APIs empleo→Merge→POST /jobs/process→Filter→entregar), el JSON EXACTO de request y response de /jobs/process, y cómo exponer el servicio"
    - "Un usuario puede probar /jobs/process sin n8n siguiendo un snippet curl del README usando examples/"
  artifacts:
    - path: "examples/arbeitnow_offers.json"
      provides: "ofertas crudas formato Arbeitnow (referencia canónica: conftest sample_arbeitnow_payload)"
    - path: "examples/generic_offers.json"
      provides: "ofertas crudas formato genérico (referencia: conftest sample_generic_payload)"
    - path: "examples/process_request.json"
      provides: "payload completo {sources:[...]} para POST /jobs/process"
    - path: "tests/api/test_examples.py"
      provides: "test que valida los JSON de examples/ y que /jobs/process los acepta"
    - path: "README.md"
      provides: "README de portfolio: instalación, config, run, tests, stack + sección n8n"
      contains: "Integración con n8n"
  key_links:
    - from: "examples/process_request.json"
      to: "POST /jobs/process (ProcessRequest)"
      via: "test_examples.py lo postea con api_client_with_cv y assert 200"
      pattern: "process_request.json"
    - from: "README.md sección n8n"
      to: "contrato real de /jobs/process"
      via: "JSON request/response copiado de los modelos ProcessRequest/ProcessResponse reales"
      pattern: "jobs/process"
---

<objective>
Cerrar el milestone con la documentación y los ejemplos: (1) crear examples/ con ofertas crudas
Arbeitnow + genéricas y un payload completo para /jobs/process (DOC-03), (2) un test que valida
que esos JSON son válidos y aceptados por el endpoint con mocks (DOC-03 verificación), y (3)
reescribir README.md con instalación/config/run/tests/stack + una sección dedicada a integración
con n8n con el JSON exacto del contrato real (DOC-01, DOC-02). Actualizar .env.example si hace
falta (ya está limpio; el README debe alinearse).

Purpose: El servicio es pieza de portfolio y herramienta real — cualquiera debe poder instalarlo,
configurarlo, probarlo sin n8n y conectarlo a n8n sin leer el código. El README actual está
desactualizado (menciona Supabase, OPENAI_API_KEY, fases "en construcción"); esta es la verdad
final del proyecto completado.
Output: examples/ (3 JSON) + test de aceptación + README reescrito + .env.example alineado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-observabilidad-robustez-docs/05-RESEARCH.md
@.planning/phases/05-observabilidad-robustez-docs/05-PATTERNS.md
@README.md
@.env.example
@tests/api/conftest.py

<interfaces>
<!-- Shape canónico Arbeitnow (tests/conftest.py sample_arbeitnow_payload — verificado 2026-05-22) -->
```json
{"slug": "...", "company_name": "...", "title": "...", "description": "<html>", "remote": false,
 "url": "...", "tags": ["..."], "job_types": ["..."], "location": "Berlin", "created_at": 1716350400}
```
<!-- Shape canónico genérico (tests/conftest.py sample_generic_payload) -->
```json
{"employer": "...", "job_title": "...", "city": "...", "body": "...", "apply_url": "...", "is_remote": true}
```

<!-- Contrato REAL de /jobs/process (leído de app/api/routes/jobs.py L55-84) -->
<!-- Request ProcessRequest: {"sources": [{"source": "arbeitnow"|"<cualquiera>", "offers": [<dict>...]}]} -->
<!-- Response ProcessResponse: {"results": [{"job": <Job>, "score": <JobScore>, "ya_visto": bool}], "errors": [{"job_id": str, "error": str}]} -->
<!-- JobScore (response) usa deal_breaker_hit + deal_breaker_cual (NO _texto) — verificado en app/models/schemas.py L152-163 -->
<!-- Job.remote enum: "remote"|"hybrid"|"onsite"|"unknown". recommendation enum: "strong_fit"|"good_fit"|"maybe"|"skip" -->
<!-- Prerequisito: POST /cv/parse debe llamarse antes; sin CVProfile cacheado /jobs/process → 404 -->

<!-- Test fixture: api_client_with_cv (tests/api/conftest.py L111-128) — TestClient con CVProfile cacheado + mocks -->

<!-- .env.example actual YA está limpio (sin OPENAI_API_KEY, sin Supabase). El README es el desactualizado. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: examples/ con 3 JSON + test de aceptación (DOC-03)</name>
  <read_first>
    - .planning/phases/05-observabilidad-robustez-docs/05-RESEARCH.md (Exact /jobs/process Contract — request)
    - .planning/phases/05-observabilidad-robustez-docs/05-PATTERNS.md (secciones examples/*.json)
    - tests/conftest.py (sample_arbeitnow_payload L194-216, sample_generic_payload L218-235)
    - tests/api/conftest.py (api_client_with_cv L111-128)
  </read_first>
  <files>examples/arbeitnow_offers.json, examples/generic_offers.json, examples/process_request.json, tests/api/test_examples.py</files>
  <action>
    1. Crear `examples/arbeitnow_offers.json`: un array JSON con 2 ofertas en formato Arbeitnow,
       usando el shape canónico del fixture sample_arbeitnow_payload (campos: slug, company_name,
       title, description con HTML, remote bool, url, tags, job_types, location, created_at Unix int).
       Hacerlas realistas y relevantes al perfil (ej. una "AI Engineer" remota y una "ML Engineer"
       onsite Berlin) para que el digest tenga sentido al probar.

    2. Crear `examples/generic_offers.json`: array JSON con 2 ofertas en formato genérico usando el
       shape de sample_generic_payload (campos: employer, job_title, city, body, apply_url, is_remote).

    3. Crear `examples/process_request.json`: el payload completo para POST /jobs/process con la
       forma `{"sources": [{"source": "arbeitnow", "offers": [<contenido de arbeitnow_offers.json>]},
       {"source": "generic", "offers": [<contenido de generic_offers.json>]}]}`. Las offers deben
       ser los MISMOS objetos que en los dos ficheros anteriores (consistencia).

    4. Crear `tests/api/test_examples.py` con `from __future__ import annotations`, `import json`,
       `from pathlib import Path`. Implementar:
       - `test_examples_son_json_valido`: cada uno de los 3 ficheros existe, `json.loads` no lanza,
         arbeitnow/generic son list, process_request es dict con clave "sources".
       - `test_process_request_aceptado_por_endpoint(api_client_with_cv)`: cargar
         examples/process_request.json, `api_client_with_cv.post("/jobs/process", json=payload)`,
         assert `resp.status_code == 200`, assert "results" in body y "errors" in body. (LLM +
         embedder ya mockeados por la fixture; cero red.)
       Usar rutas relativas a la raíz del repo (Path("examples/...")) — los tests corren desde la raíz.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/api/test_examples.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import json; [json.load(open(f)) for f in ['examples/arbeitnow_offers.json','examples/generic_offers.json','examples/process_request.json']]"` retorna 0 (los 3 son JSON válido)
    - `python -c "import json; d=json.load(open('examples/process_request.json')); assert 'sources' in d and len(d['sources'])>=2"` retorna 0
    - `uv run python -m pytest tests/api/test_examples.py -q` pasa (validez + aceptación por /jobs/process)
  </acceptance_criteria>
  <done>examples/ con 3 JSON consistentes y válidos; el payload completo es aceptado por /jobs/process con mocks; test verde.</done>
</task>

<task type="auto">
  <name>Task 2: Reescribir README.md (DOC-01) + sección "Integración con n8n" (DOC-02) + alinear .env.example</name>
  <read_first>
    - .planning/phases/05-observabilidad-robustez-docs/05-RESEARCH.md (Exact /jobs/process Contract — request + response + curl; Pitfalls 6, 7)
    - README.md (estado actual — desactualizado: menciona Supabase, OPENAI_API_KEY, fases en construcción)
    - .env.example (estado actual — ya limpio)
    - CLAUDE.md (secciones "Sección README: Integración con n8n" y "Calidad de ingeniería")
  </read_first>
  <files>README.md, .env.example</files>
  <action>
    Reescribir `README.md` completo, tono de portfolio (la calidad de ingeniería importa), en
    español. Secciones requeridas (DOC-01):
    1. **Qué es**: servicio FastAPI headless que normaliza, deduplica semánticamente y puntúa
       ofertas contra el perfil real de Jaime (CV + ranking + preferencias); orquestación diaria
       por n8n por fuera. Mencionar el doble propósito (herramienta real + portfolio).
    2. **Instalación (uv)**: `uv sync` (o `uv add` ya hecho), Python 3.11+.
    3. **Configuración .env**: `cp .env.example .env`, poner `ANTHROPIC_API_KEY` (requerida).
       Mencionar variables opcionales (ANTHROPIC_MODEL_*, SQLITE_DB_PATH, y LANGFUSE_PUBLIC_KEY/
       LANGFUSE_SECRET_KEY como OPCIONALES para activar el tracing — el servicio funciona sin ellas).
    4. **Editar perfil**: cómo editar `data/profile.yaml` (ranking de puestos, preferencias,
       deal-breakers, pesos de scoring) SIN tocar código.
    5. **Subir el CV**: `POST /cv/parse` con el CV en PDF (snippet curl con -F "file=@...").
    6. **Arrancar el servicio**: `uv run uvicorn app.api.main:app --reload` (dev), nota de host/port.
    7. **Correr tests**: `uv run pytest -q`.
    8. **Stack** ACTUALIZADO (Pitfall 6): Python 3.11+ · uv · FastAPI · Pydantic v2 + Instructor ·
       Anthropic (scoring/CV) · BGE-M3 local (dedup) · SQLite local (stdlib sqlite3) · pytest.
       NO mencionar Supabase ni pgvector ni OPENAI_API_KEY (eliminados del alcance).
    9. **Endpoints**: tabla breve de los endpoints (POST /cv/parse, GET /profile, POST /jobs/normalize,
       POST /jobs/score, POST /jobs/process, GET /jobs/history).
    10. **Observabilidad** (breve): logging key=value de cada run; stub Langfuse opcional (activable
        con LANGFUSE_* + `uv add langfuse`); manejo de errores por capa.

    Sección dedicada **"Integración con n8n"** (DOC-02), accionable:
    - Qué nodos de n8n llaman a qué endpoints.
    - Flujo: `Schedule → HTTP a APIs de empleo (Arbeitnow / JSearch / FlyByAPIs) → Merge → HTTP
      Request a POST /jobs/process → Filter por score → entregar (email/telegram)`.
    - El **JSON EXACTO** de request de /jobs/process (forma {"sources":[{"source","offers"}]}) —
      copiar de RESEARCH "Exact /jobs/process Contract"; referenciar examples/process_request.json
      como ejemplo listo para usar.
    - El **JSON EXACTO** de response (forma {"results":[{"job","score","ya_visto"}],"errors":[]}) —
      copiar de RESEARCH; recordar el campo `ya_visto` (para que n8n no repita ofertas) y que
      `results` viene ordenado por score_total DESC.
    - Prerequisito: llamar a POST /cv/parse una vez antes (sin CVProfile cacheado → 404).
    - Cómo exponer el servicio para n8n: localhost en dev (`http://localhost:8000`); nota sobre
      desplegarlo accesible en prod (reverse proxy / host, fuera del scope del código).
    - Un snippet **curl** para probar /jobs/process sin n8n usando `examples/process_request.json`:
      `curl -X POST http://localhost:8000/jobs/process -H "Content-Type: application/json" -d @examples/process_request.json`
      (precedido del curl de /cv/parse).

    Alinear `.env.example`: ya está limpio (sin OPENAI_API_KEY ni Supabase). AÑADIR las dos
    variables opcionales de Langfuse comentadas con una nota de que son opcionales para activar el
    tracing (Pitfall 7 / consistencia con OBS-02 y el README):
    `# Observabilidad opcional (OBS-02) — activa el tracing de las llamadas LLM con Langfuse:`
    `# LANGFUSE_PUBLIC_KEY=pk-lf-...`
    `# LANGFUSE_SECRET_KEY=sk-lf-...`
  </action>
  <verify>
    <automated>grep -q "Integración con n8n" README.md && grep -q "jobs/process" README.md && grep -vi supabase README.md >/dev/null && ! grep -qi "OPENAI_API_KEY" README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Integración con n8n" README.md` (sección n8n presente)
    - `grep -q "uv run uvicorn app.api.main:app" README.md` (comando de arranque)
    - `grep -q "cp .env.example .env" README.md` (config .env)
    - `grep -q "data/profile.yaml" README.md` y `grep -q "/cv/parse" README.md`
    - `grep -q "examples/process_request.json" README.md` (snippet curl referencia ejemplos)
    - `grep -q '"sources"' README.md` y `grep -q '"results"' README.md` (JSON exacto request+response)
    - `! grep -qi "supabase" README.md` y `! grep -qi "OPENAI_API_KEY" README.md` (stack actualizado — Pitfalls 6, 7)
    - `grep -q "LANGFUSE_PUBLIC_KEY" .env.example` (Langfuse opcional documentado)
  </acceptance_criteria>
  <done>README de portfolio completo (instalación/config/perfil/CV/run/tests/stack actualizado + observabilidad) con sección n8n accionable (flujo, JSON exacto request/response, curl con examples/, cómo exponer); .env.example incluye las vars opcionales de Langfuse; sin menciones obsoletas a Supabase/OPENAI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo público (portfolio) → lector | .env.example y README son visibles; no deben contener secretos reales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | .env.example / README.md | mitigate | .env.example solo contiene placeholders (sk-ant-..., pk-lf-...); .env está en .gitignore; el README nunca muestra claves reales. Verificado: las claves Langfuse se documentan como placeholders comentados |
| T-05-06 | Information Disclosure | examples/*.json | accept | Los ejemplos contienen datos ficticios de ofertas (empresas/URLs de muestra), sin PII real ni secretos |
</threat_model>

<verification>
- `uv run python -m pytest tests/api/test_examples.py -q` — ejemplos válidos y aceptados
- `uv run python -m pytest -q` — suite completa verde, sin regresiones
- `grep -q "Integración con n8n" README.md` y JSON exacto request/response presentes
- README sin menciones obsoletas (Supabase, OPENAI_API_KEY)
</verification>

<success_criteria>
- examples/ con ofertas Arbeitnow + genéricas + payload completo de /jobs/process, validados y aceptados por el endpoint con mocks (DOC-03)
- README de portfolio completo: instalación, .env, profile.yaml, CV, run, tests, stack actualizado (DOC-01)
- Sección "Integración con n8n" con flujo, JSON EXACTO request/response, curl de prueba con examples/, y cómo exponer el servicio (DOC-02)
- .env.example alineado (Langfuse opcional); sin Supabase/OPENAI; suite completa verde
</success_criteria>

<output>
After completion, create `.planning/phases/05-observabilidad-robustez-docs/05-03-SUMMARY.md`
</output>
