---
phase: 01-parseo-de-cv
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - app/__init__.py
  - app/cv/__init__.py
  - app/models/__init__.py
  - tests/__init__.py
  - tests/cv/__init__.py
  - tests/fixtures/__init__.py
  - tests/conftest.py
  - .gitignore
autonomous: true
requirements: [QA-01]
must_haves:
  truths:
    - "pytest discovers and collects tests from tests/ with app/ importable"
    - "A reusable in-memory sample CV PDF fixture (bytes) is available to all tests"
    - "A reusable mocked instructor client fixture returns a deterministic CVProfile"
    - "Core deps (pymupdf, instructor, anthropic, python-dotenv) and dev dep pytest are installed via uv"
  artifacts:
    - path: "pyproject.toml"
      provides: "Dependencies + [tool.pytest.ini_options] config (testpaths, pythonpath)"
      contains: "tool.pytest.ini_options"
    - path: "tests/conftest.py"
      provides: "sample_pdf_bytes + EXPECTED_PROFILE + mock_llm_client fixtures"
      min_lines: 30
    - path: "tests/cv/__init__.py"
      provides: "tests.cv test package marker"
  key_links:
    - from: "pyproject.toml [tool.pytest.ini_options]"
      to: "app/ and tests/ packages"
      via: "pythonpath = [\".\"] so `import app...` resolves under pytest"
      pattern: "pythonpath"
    - from: "tests/conftest.py sample_pdf_bytes"
      to: "pymupdf"
      via: "pymupdf.open() + page.insert_text() + doc.tobytes()"
      pattern: "pymupdf\\.open\\(\\)"
---

<objective>
Establish the Wave 0 test and dependency scaffolding for Phase 1 so every later wave
can run `uv run pytest tests/cv -q` with zero setup. Installs the verified phase deps,
adds a minimal pytest config, creates the `tests/` package tree, and provides the shared
fixtures (in-memory CV PDF + mocked instructor client) that the extractor, cache, and
parser tests will consume.

Purpose: RESEARCH.md and VALIDATION.md flag that NO test infra exists (no pytest config,
no tests/, no installed deps). Subsequent plans (02, 03, 04) all depend on this being in
place. Without it, no `<automated>` verify can run (Nyquist gate).

Output: Installed deps, pytest config in pyproject.toml, `tests/` package tree with
`tests/conftest.py` exposing `sample_pdf_bytes`, `EXPECTED_PROFILE`, and `mock_llm_client`
fixtures, plus package `__init__.py` files making `app/` importable under pytest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parseo-de-cv/01-RESEARCH.md
@.planning/phases/01-parseo-de-cv/01-VALIDATION.md
@.planning/phases/01-parseo-de-cv/01-PATTERNS.md
@CLAUDE.md
@pyproject.toml
@app/models/schemas.py

<interfaces>
<!-- Schema the mocked client fixture must construct. Verified from app/models/schemas.py. -->
<!-- Use these exact field names. Spanish field names, Pydantic v2, Field(default_factory=list). -->

From app/models/schemas.py:
```python
class Experiencia(BaseModel):
    empresa: str
    rol: str
    duracion: Optional[str] = None
    tecnologias: list[str] = Field(default_factory=list)
    logros: list[str] = Field(default_factory=list)

class Formacion(BaseModel):
    titulo: str
    institucion: Optional[str] = None
    anio: Optional[int] = None

class CVProfile(BaseModel):
    experiencia: list[Experiencia] = Field(default_factory=list)
    skills_tecnicas: list[str] = Field(default_factory=list)
    formacion: list[Formacion] = Field(default_factory=list)
    anios_experiencia_total: Optional[float] = None
    dominios: list[str] = Field(default_factory=list)
```

Note: `app/` currently has NO `__init__.py` files (works via PEP 420 namespace packages
when cwd is on sys.path). Under pytest this requires `pythonpath = ["."]`. This plan adds
explicit `__init__.py` files to `app/`, `app/models/`, `app/cv/` AND sets `pythonpath`,
so imports resolve robustly regardless of invocation directory.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps and add pytest config</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current contents — only pydantic + pyyaml deps, no test config)
    - .planning/phases/01-parseo-de-cv/01-RESEARCH.md (Standard Stack table + Installation section: exact versions and uv commands)
  </read_first>
  <action>
    Install the phase dependencies using uv (NOT manual pyproject editing for deps — use `uv add`
    so the lockfile updates). Run, from the project root:

      uv add pymupdf instructor anthropic python-dotenv
      uv add --dev pytest

    These map to RESEARCH.md verified versions (pymupdf 1.26.7, instructor 1.15.1,
    anthropic 0.104.0, python-dotenv 1.2.2, pytest 9.0.3) — let uv resolve to current
    compatible versions, do not pin manually. Do NOT add reportlab; the PDF fixture is
    generated with pymupdf (already a core dep), per RESEARCH.md Open Question 2.

    Then, in pyproject.toml, append a pytest config table so tests discover correctly and
    `import app...` resolves under pytest from the project root. Add exactly:

      [tool.pytest.ini_options]
      testpaths = ["tests"]
      pythonpath = ["."]
      addopts = "-q"

    `pythonpath = ["."]` is REQUIRED: app/ uses namespace packages and tests import
    `from app.cv.parser import parse_cv` — without it pytest cannot resolve `app`.
  </action>
  <acceptance_criteria>
    - `grep -q 'pymupdf' pyproject.toml` succeeds (dep added)
    - `grep -q 'instructor' pyproject.toml` succeeds
    - `grep -q 'anthropic' pyproject.toml` succeeds
    - `grep -q 'python-dotenv' pyproject.toml` succeeds
    - `grep -q 'tool.pytest.ini_options' pyproject.toml` succeeds
    - `grep -q 'pythonpath' pyproject.toml` succeeds
    - `uv run python -c "import pymupdf, instructor, anthropic, dotenv"` exits 0
    - `grep -q 'reportlab' pyproject.toml` returns NON-zero (reportlab NOT added)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import pymupdf, instructor, anthropic, dotenv; print('deps ok')"</automated>
  </verify>
  <done>Deps installed via uv (lockfile updated); pyproject.toml has [tool.pytest.ini_options] with testpaths, pythonpath=["."], addopts.</done>
</task>

<task type="auto">
  <name>Task 2: Create package tree and conftest with shared fixtures</name>
  <files>app/__init__.py, app/models/__init__.py, app/cv/__init__.py, tests/__init__.py, tests/cv/__init__.py, tests/fixtures/__init__.py, tests/conftest.py</files>
  <read_first>
    - app/models/schemas.py (CVProfile/Experiencia/Formacion exact fields + docstring/import style to mirror)
    - .planning/phases/01-parseo-de-cv/01-PATTERNS.md (conftest.py pattern lines 293-321; test isolation pattern lines 509-518)
    - .planning/phases/01-parseo-de-cv/01-RESEARCH.md (Pattern 6 fixture lines 350-374; Pattern 5 mock lines 311-348)
  </read_first>
  <action>
    Create the package marker files (each just needs to exist; add a one-line Spanish
    docstring in app/cv/__init__.py only — but do NOT yet import parser here, parser.py
    does not exist until Plan 04, so keep app/cv/__init__.py empty except a docstring to
    avoid an import error):

    - app/__init__.py            → empty file (package marker)
    - app/models/__init__.py     → empty file (package marker)
    - app/cv/__init__.py         → only a module docstring: `"""Módulo de parseo de CV (FASE 1)."""`
                                    (Plan 04 will add the `from app.cv.parser import parse_cv` export)
    - tests/__init__.py          → empty
    - tests/cv/__init__.py       → empty
    - tests/fixtures/__init__.py → empty

    Create tests/conftest.py with three shared fixtures (start with `from __future__ import
    annotations`):

    1. `sample_pdf_bytes` (scope="session") — generates a minimal in-memory CV PDF using
       pymupdf (NOT reportlab). Open `pymupdf.open()`, add a page, `page.insert_text((72, 72), ...)`
       with KNOWN Spanish/English CV text including the literal "AI Engineer", "Acme Corp",
       "Python", "LLMs", and return `doc.tobytes()`. This text is asserted on by the
       extractor tests in Plan 02.

    2. `EXPECTED_PROFILE` — a module-level CVProfile constant (not a fixture) used by mocks:
         EXPECTED_PROFILE = CVProfile(
             experiencia=[Experiencia(empresa="Acme Corp", rol="AI Engineer", duracion="2 años")],
             skills_tecnicas=["Python", "LLMs"],
             anios_experiencia_total=2.0,
             dominios=["IA"],
         )

    3. `mock_llm_client` (function scope) — returns a MagicMock whose
       `.messages.create.return_value` is EXPECTED_PROFILE. This mirrors the
       `instructor.from_anthropic()` client surface (`client.messages.create(...)`),
       so parser tests in Plan 04 can patch `app.cv.parser.build_instructor_client`.

    Use Spanish docstrings/comments, full type hints, `from __future__ import annotations`,
    stdlib-before-third-party import order (mirror app/models/schemas.py conventions).
  </action>
  <acceptance_criteria>
    - All seven files exist: `test -f app/__init__.py app/models/__init__.py app/cv/__init__.py tests/__init__.py tests/cv/__init__.py tests/fixtures/__init__.py tests/conftest.py`
    - `grep -q 'def sample_pdf_bytes' tests/conftest.py` succeeds
    - `grep -q 'EXPECTED_PROFILE' tests/conftest.py` succeeds
    - `grep -q 'def mock_llm_client' tests/conftest.py` succeeds
    - `grep -q 'pymupdf' tests/conftest.py` succeeds (fixture uses pymupdf not reportlab)
    - `grep -q 'messages.create' tests/conftest.py` succeeds (mock surface matches instructor)
    - `uv run pytest --collect-only` exits 0 (collection succeeds even with no test functions yet)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest --collect-only -q</automated>
  </verify>
  <done>Package tree exists; tests/conftest.py exposes sample_pdf_bytes, EXPECTED_PROFILE, mock_llm_client; pytest collects with no errors.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm cache dir is gitignored</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current contents — already has `.cache/`)
    - .planning/phases/01-parseo-de-cv/01-CONTEXT.md (cache section: data/.cache/ location)
  </read_first>
  <action>
    The cache writes to `data/.cache/` (CV_CACHE_DIR default). The existing .gitignore
    already contains a `.cache/` line which matches `data/.cache/` at any depth. Verify
    this line is present. If, and only if, `grep -q '\.cache/' .gitignore` fails, append
    a line `data/.cache/` to .gitignore. Do NOT duplicate an existing ignore rule.
    No other change needed — the extracted CVProfile cache must never be committed.
  </action>
  <acceptance_criteria>
    - `grep -q '\.cache/' .gitignore` succeeds
    - `git check-ignore data/.cache/test.json` prints the path (confirms it is ignored)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '\.cache/' .gitignore && echo "cache ignored"</automated>
  </verify>
  <done>.gitignore ignores data/.cache/ (no duplicate rule added if already present).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev shell → uv/PyPI | Dependency resolution pulls remote packages |
| test process → filesystem | Fixtures write transient PDFs in memory only (no disk writes from conftest) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | data/.cache/ extracted CVProfile | mitigate | `.gitignore` includes `.cache/`; Task 3 verifies; extracted CV (PII) never committed |
| T-01-02 | Tampering | uv dependency resolution | accept | Versions verified in RESEARCH.md against pip registry; uv.lock pins resolved hashes; local personal tool |
</threat_model>

<verification>
- `uv run python -c "import pymupdf, instructor, anthropic, dotenv"` exits 0
- `uv run pytest --collect-only` exits 0 (infra collects, no import errors)
- `grep -q 'tool.pytest.ini_options' pyproject.toml` and `grep -q 'pythonpath' pyproject.toml` succeed
- `git check-ignore data/.cache/x.json` confirms cache is ignored
</verification>

<success_criteria>
- Phase deps installed via uv; lockfile updated
- pyproject.toml has pytest config with testpaths + pythonpath=["."]
- tests/ package tree exists with conftest exposing the three shared fixtures
- pytest collection succeeds with zero errors
- data/.cache/ confirmed gitignored
</success_criteria>

<output>
After completion, create `.planning/phases/01-parseo-de-cv/01-01-SUMMARY.md`
</output>
