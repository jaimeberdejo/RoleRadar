---
phase: 01-parseo-de-cv
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - app/cv/extractor.py
  - app/cv/cache.py
  - tests/cv/test_extractor.py
  - tests/cv/test_cache.py
autonomous: true
requirements: [CV-01, CV-02, CV-03]
must_haves:
  truths:
    - "PDF bytes are converted to concatenated multi-page text in reading order"
    - "A PDF with no extractable text raises a clear Spanish ValueError (no OCR attempt)"
    - "A CVProfile round-trips to/from a JSON cache file keyed by sha256 of the PDF bytes"
    - "Cache location is configurable via CV_CACHE_DIR (default data/.cache) for test isolation"
  artifacts:
    - path: "app/cv/extractor.py"
      provides: "extract_text_from_pdf(pdf_bytes: bytes) -> str"
      contains: "def extract_text_from_pdf"
    - path: "app/cv/cache.py"
      provides: "pdf_hash, load_cached_profile, save_cached_profile + CV_CACHE_DIR"
      contains: "def pdf_hash"
    - path: "tests/cv/test_extractor.py"
      provides: "extractor unit tests (string out, known content, empty-PDF raises)"
    - path: "tests/cv/test_cache.py"
      provides: "cache roundtrip + miss-returns-None tests"
  key_links:
    - from: "app/cv/extractor.py"
      to: "pymupdf"
      via: "pymupdf.open(stream=pdf_bytes, filetype='pdf')"
      pattern: "pymupdf\\.open\\(stream="
    - from: "app/cv/cache.py"
      to: "data/.cache (or CV_CACHE_DIR)"
      via: "sha256 hex filename + CVProfile.model_dump_json / model_validate_json"
      pattern: "model_(dump|validate)_json"
---

<objective>
Build the two LLM-free leaf modules of the CV parser: `extractor.py` (pymupdf bytes → text)
and `cache.py` (sha256-keyed JSON cache of CVProfile). Both are pure I/O with no Anthropic
dependency, so they can be implemented and tested in full isolation in parallel with the
LLM client (Plan 03). Together they satisfy CV-03 (cache so the CV is not re-parsed every run).

Purpose: These are the deterministic foundations the parser orchestrator (Plan 04) wires
together. Implementing them first with their own tests means the orchestrator test only
needs to verify wiring, not re-test extraction or caching.

Output: `app/cv/extractor.py`, `app/cv/cache.py`, and their unit tests in tests/cv/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parseo-de-cv/01-RESEARCH.md
@.planning/phases/01-parseo-de-cv/01-PATTERNS.md
@CLAUDE.md
@app/models/schemas.py

<interfaces>
<!-- Exact contracts these modules expose, consumed by parser.py in Plan 04. -->
<!-- Implement these signatures verbatim — Plan 04 imports them by name. -->

app/cv/extractor.py exposes:
```python
def extract_text_from_pdf(pdf_bytes: bytes) -> str: ...
```

app/cv/cache.py exposes:
```python
CACHE_DIR: Path  # = Path(os.getenv("CV_CACHE_DIR", "data/.cache"))
def pdf_hash(pdf_bytes: bytes) -> str: ...
def load_cached_profile(content_hash: str) -> CVProfile | None: ...
def save_cached_profile(content_hash: str, profile: CVProfile) -> None: ...
```

CVProfile (from app/models/schemas.py — serialization target):
```python
class CVProfile(BaseModel):
    experiencia: list[Experiencia] = Field(default_factory=list)
    skills_tecnicas: list[str] = Field(default_factory=list)
    formacion: list[Formacion] = Field(default_factory=list)
    anios_experiencia_total: Optional[float] = None
    dominios: list[str] = Field(default_factory=list)
```
Serialize with `profile.model_dump_json(indent=2)`; read with `CVProfile.model_validate_json(text)`.
NEVER `.dict()` / `json.dumps`.
</interfaces>

<wave_0_dependency>
This plan depends on Plan 01 (Wave 0). The fixture `sample_pdf_bytes` lives in
tests/conftest.py and the package tree + pytest config must exist before these tests run.
</wave_0_dependency>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement app/cv/extractor.py + tests</name>
  <files>app/cv/extractor.py, tests/cv/test_extractor.py</files>
  <read_first>
    - app/models/schemas.py (import/style/type-hint conventions to mirror)
    - .planning/phases/01-parseo-de-cv/01-RESEARCH.md (Pattern 1 lines 183-206; empty-text detection lines 441-450; fitz anti-pattern line 378)
    - .planning/phases/01-parseo-de-cv/01-PATTERNS.md (extractor.py section lines 121-152; test_extractor.py pattern lines 324-352)
    - tests/conftest.py (the sample_pdf_bytes fixture this test consumes)
  </read_first>
  <behavior>
    - extract_text_from_pdf(sample_pdf_bytes) returns a non-empty str
    - returned text contains the literal "AI Engineer" (known fixture content)
    - extract_text_from_pdf on a blank-page PDF (no text layer) raises ValueError matching "capa de texto"
  </behavior>
  <action>
    Create app/cv/extractor.py with `from __future__ import annotations` then `import pymupdf`
    (NEVER `import fitz`). Implement exactly:

      def extract_text_from_pdf(pdf_bytes: bytes) -> str:
          """Extrae todo el texto de un PDF multi-página, en orden de lectura."""
          with pymupdf.open(stream=pdf_bytes, filetype="pdf") as doc:
              pages_text = [page.get_text() for page in doc]
          text = "\n\n".join(pages_text).strip()
          if not text:
              raise ValueError(
                  "El PDF no contiene capa de texto extraíble. "
                  "PDFs escaneados (solo imagen) no están soportados en v1."
              )
          return text

    No try/except around pymupdf — let the caller decide. Spanish docstring/comments, full
    type hints.

    Then create tests/cv/test_extractor.py with three tests (per behavior block above):
    use the `sample_pdf_bytes` conftest fixture; for the empty-PDF test, build a blank PDF
    inline (`pymupdf.open(); doc.new_page(); doc.tobytes()`) and assert
    `pytest.raises(ValueError, match="capa de texto")`.
  </action>
  <acceptance_criteria>
    - `grep -q 'def extract_text_from_pdf' app/cv/extractor.py` succeeds
    - `grep -q 'import pymupdf' app/cv/extractor.py` succeeds
    - `grep -c 'import fitz' app/cv/extractor.py` returns 0 (no legacy import)
    - `grep -q 'pymupdf.open(stream=' app/cv/extractor.py` succeeds
    - `uv run pytest tests/cv/test_extractor.py -q` passes (3 tests green)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/cv/test_extractor.py -q</automated>
  </verify>
  <done>extractor.py extracts multi-page text and raises a clear ValueError on text-less PDFs; 3 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement app/cv/cache.py + tests (CV-03)</name>
  <files>app/cv/cache.py, tests/cv/test_cache.py</files>
  <read_first>
    - app/models/schemas.py (CVProfile/Experiencia exact fields for the test profile)
    - .planning/phases/01-parseo-de-cv/01-RESEARCH.md (Pattern 3 lines 253-282; pitfall #5 cache isolation lines 427-431; pitfall #6 serialization lines 432-436)
    - .planning/phases/01-parseo-de-cv/01-PATTERNS.md (cache.py section lines 211-254; test_cache.py pattern lines 356-393)
  </read_first>
  <behavior>
    - save_cached_profile then load_cached_profile returns an equal CVProfile (roundtrip; skills_tecnicas and anios_experiencia_total preserved)
    - load_cached_profile for a hash with no file returns None
    - CACHE_DIR honors CV_CACHE_DIR env var (tests set it to tmp_path)
  </behavior>
  <action>
    Create app/cv/cache.py. Start with `from __future__ import annotations`, then stdlib
    imports (`hashlib`, `os`, `pathlib.Path`), then `from app.models.schemas import CVProfile`.
    Implement (per CV-03 — cache so CV is not re-parsed unless content changes):

      CACHE_DIR = Path(os.getenv("CV_CACHE_DIR", "data/.cache"))

      def pdf_hash(pdf_bytes: bytes) -> str:
          return hashlib.sha256(pdf_bytes).hexdigest()

      def load_cached_profile(content_hash: str) -> CVProfile | None:
          cache_file = CACHE_DIR / f"{content_hash}.json"
          if cache_file.exists():
              return CVProfile.model_validate_json(cache_file.read_text())
          return None

      def save_cached_profile(content_hash: str, profile: CVProfile) -> None:
          CACHE_DIR.mkdir(parents=True, exist_ok=True)
          cache_file = CACHE_DIR / f"{content_hash}.json"
          cache_file.write_text(profile.model_dump_json(indent=2))

    IMPORTANT: CV_CACHE_DIR must be read at module import via `os.getenv`. For the env var
    to be honored after `monkeypatch.setenv` in tests, the test must set the env var BEFORE
    importing cache, OR cache must read CACHE_DIR lazily inside each function. Choose the
    lazy-read approach to be test-robust: define a private `_cache_dir() -> Path` that
    returns `Path(os.getenv("CV_CACHE_DIR", "data/.cache"))` and call it inside
    load/save; keep a module-level `CACHE_DIR` constant too for the documented default.
    Use `profile.model_dump_json()` / `CVProfile.model_validate_json()` ONLY (never .dict()).

    Then create tests/cv/test_cache.py: a `sample_profile` fixture building a CVProfile with
    Experiencia(empresa="Acme Corp", rol="AI Engineer", duracion="2 años"),
    skills_tecnicas=["Python", "LLMs"], anios_experiencia_total=2.0, dominios=["IA"].
    Every test does `monkeypatch.setenv("CV_CACHE_DIR", str(tmp_path))` first. Tests:
    (a) roundtrip save→load equals on skills_tecnicas and anios_experiencia_total;
    (b) load of a nonexistent hash returns None.
  </action>
  <acceptance_criteria>
    - `grep -q 'def pdf_hash' app/cv/cache.py` succeeds
    - `grep -q 'def load_cached_profile' app/cv/cache.py` succeeds
    - `grep -q 'def save_cached_profile' app/cv/cache.py` succeeds
    - `grep -q 'CV_CACHE_DIR' app/cv/cache.py` succeeds (configurable cache dir)
    - `grep -q 'model_dump_json' app/cv/cache.py` and `grep -q 'model_validate_json' app/cv/cache.py` succeed
    - `grep -c '\.dict()' app/cv/cache.py` returns 0 (no Pydantic v1 API)
    - `uv run pytest tests/cv/test_cache.py -q` passes (2 tests green)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/cv/test_cache.py -q</automated>
  </verify>
  <done>cache.py round-trips CVProfile by sha256 key, honors CV_CACHE_DIR, returns None on miss; 2 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → extractor | Untrusted PDF bytes enter pymupdf parsing |
| extractor/cache → filesystem | Cache reads/writes JSON under CV_CACHE_DIR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service | extractor.py parsing malformed/huge PDF | mitigate | pymupdf isolates parsing; empty-text PDFs raise a clear ValueError instead of hanging; no OCR fallback that could loop |
| T-01-04 | Tampering | malicious PDF (e.g., embedded scripts) | accept | pymupdf does not execute PDF content; local single-user personal tool, low-value target |
| T-01-05 | Information Disclosure | cached CVProfile (PII) on disk | mitigate | written under data/.cache which Plan 01 confirms is gitignored; never logged |
</threat_model>

<verification>
- `uv run pytest tests/cv/test_extractor.py tests/cv/test_cache.py -q` all green
- extractor uses `import pymupdf` (not fitz) and `pymupdf.open(stream=...)`
- cache uses `model_dump_json` / `model_validate_json` and honors CV_CACHE_DIR
- no `.dict()` anywhere in cache.py
</verification>

<success_criteria>
- extract_text_from_pdf returns reading-order multi-page text; raises clear ValueError on text-less PDFs
- cache.py persists/loads CVProfile by sha256, configurable via CV_CACHE_DIR (CV-03 satisfied)
- 5 unit tests green (3 extractor + 2 cache), runnable via `uv run pytest tests/cv -q`
</success_criteria>

<output>
After completion, create `.planning/phases/01-parseo-de-cv/01-02-SUMMARY.md`
</output>
