---
phase: 02-normalizacion-y-deduplicacion
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - app/dedup/__init__.py
  - tests/dedup/test_dedup.py
autonomous: true
requirements: [DEDUP-01, DEDUP-02, DEDUP-03, QA-03]
must_haves:
  truths:
    - "deduplicate(jobs, embedder, umbral, desc_words) ensambla nivel-1 exacto + nivel-2 semántico + merge"
    - "Con un embedder inyectado (FakeEmbedder), deduplicate colapsa duplicados exactos y semánticos sin descargar el modelo"
    - "El resultado conserva la oferta con descripción más completa y acumula urls_alternativas"
    - "Existe un test end-to-end: 3 jobs (2 duplicados + 1 único) → 2 jobs de salida"
  artifacts:
    - path: "app/dedup/__init__.py"
      provides: "Facade público completo: normalize_jobs() + deduplicate()"
      exports: ["normalize_jobs", "deduplicate"]
    - path: "tests/dedup/test_dedup.py"
      provides: "Test end-to-end de deduplicate() con FakeEmbedder (QA-03)"
      min_lines: 40
  key_links:
    - from: "app/dedup/__init__.py"
      to: "app.dedup.exact.exact_group / app.dedup.semantic.semantic_cluster / app.dedup.merger.merge_group"
      via: "imports deferidos dentro del cuerpo de deduplicate()"
      pattern: "from app.dedup.semantic import semantic_cluster"
    - from: "tests/dedup/test_dedup.py"
      to: "app.dedup.deduplicate"
      via: "inyección directa de FakeEmbedder vía parámetro embedder="
      pattern: "deduplicate\\(.*embedder="
---

<objective>
ENSAMBLAR el pipeline de deduplicación: añadir el facade público `deduplicate()` a
`app/dedup/__init__.py`, cableando los módulos hoja de Plan 03 (exact → semantic → merger),
y escribir el test end-to-end (QA-03) que verifica el flujo completo con un FakeEmbedder
inyectado — duplicados exactos y semánticos colapsan, se conserva la descripción más completa
y se acumulan las urls alternativas, todo sin descargar el modelo BGE-M3.

Purpose: cierra Phase 2 uniendo normalización (Plan 02) y dedup (Plan 03) bajo un único facade
público que consumirán el scoring (Fase 3) y los endpoints (Fase 4). Implementa el cableado de
DEDUP-01..03 y el test integrador QA-03.
Output: app/dedup/__init__.py (ampliado con deduplicate), tests/dedup/test_dedup.py.

Wave 3: depende de Plan 02 (creó __init__.py con normalize_jobs) y Plan 03 (creó exact/semantic/merger).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-normalizacion-y-deduplicacion/02-RESEARCH.md
@.planning/phases/02-normalizacion-y-deduplicacion/02-PATTERNS.md
@app/models/schemas.py
@app/cv/parser.py
@app/dedup/__init__.py
@app/dedup/exact.py
@app/dedup/semantic.py
@app/dedup/merger.py
@app/dedup/embedder.py

<interfaces>
<!-- Firmas ya creadas por Plans 02 y 03 (usar tal cual; NO reescribirlas) -->
From app/dedup/__init__.py (Plan 02):
```python
def normalize_jobs(raw_offers: list[dict], source: str) -> tuple[list[Job], list[dict]]: ...
```
From app/dedup/exact.py (Plan 03):
```python
def exact_group(jobs: list[Job]) -> list[list[Job]]: ...
```
From app/dedup/semantic.py (Plan 03):
```python
def semantic_cluster(jobs, embedder, umbral=0.85, desc_words=50) -> list[list[int]]: ...
```
From app/dedup/merger.py (Plan 03):
```python
def merge_group(group: list[Job]) -> Job: ...
```
From app/dedup/embedder.py (Plan 03):
```python
class Embedder(Protocol): def embed(self, texts: list[str]) -> np.ndarray: ...
class BgeM3Embedder: ...      # lazy
class FakeEmbedder: ...       # tests
```

<!-- Patrón facade con imports deferidos dentro del cuerpo (app/cv/parser.py importa de submódulos; -->
<!-- aquí los imports de exact/semantic/merger van DENTRO de deduplicate() para evitar arrastrar -->
<!-- numpy/torch en el import top-level del paquete — ver RESEARCH.md Code Examples: deduplicate facade) -->

<!-- Fixtures disponibles (Plan 01): fake_embedder_identico, fake_embedder_ortogonal -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir el facade deduplicate() a app/dedup/__init__.py</name>
  <read_first>
    - app/dedup/__init__.py (preservar normalize_jobs creado por Plan 02 — AMPLIAR, no reescribir)
    - app/cv/parser.py (patrón facade: orquesta submódulos)
    - .planning/phases/02-normalizacion-y-deduplicacion/02-RESEARCH.md (Code Examples: deduplicate() facade verificado)
    - .planning/phases/02-normalizacion-y-deduplicacion/02-PATTERNS.md (sección __init__.py — imports deferidos)
  </read_first>
  <files>app/dedup/__init__.py</files>
  <action>
    AMPLIAR `app/dedup/__init__.py` (NO borrar normalize_jobs) añadiendo `deduplicate()` siguiendo
    el Code Example verificado de RESEARCH.md:

    - Firma: `deduplicate(jobs: list[Job], embedder: "Embedder | None" = None, umbral: float = 0.85, desc_words: int = 50) -> list[Job]`.
    - Imports DEFERIDOS dentro del cuerpo (evita arrastrar numpy/torch al importar el paquete):
      `from app.dedup.exact import exact_group`
      `from app.dedup.semantic import semantic_cluster`
      `from app.dedup.merger import merge_group`
      `from app.dedup.embedder import BgeM3Embedder`
    - Lógica:
      1. `if not jobs: return []`.
      2. Nivel 1 — `groups = exact_group(jobs)`; log `logger.info("dedup level-1 (exact): %d jobs -> %d groups", len(jobs), len(groups))`.
      3. `representatives = [merge_group(g) for g in groups]` (fusiona cada grupo exacto a su canónica).
      4. `if embedder is None: embedder = BgeM3Embedder()` (lazy — solo en uso real, nunca en tests).
      5. Nivel 2 — `clusters = semantic_cluster(representatives, embedder, umbral, desc_words)`;
         log `logger.info("dedup level-2 (semantic): %d reps -> %d clusters", ...)`.
      6. `result = [merge_group([representatives[i] for i in cluster]) for cluster in clusters]`.
      7. `return result`.
    - Añadir `deduplicate` a `__all__` junto a `normalize_jobs`.
    Docstring en español explicando los dos niveles + que embedder=None usa BgeM3Embedder perezoso.
  </action>
  <verify>
    <automated>uv run python -c "from app.dedup import normalize_jobs, deduplicate; print('ok')"</automated>
    <automated>uv run python -c "import sys; import app.dedup; assert 'torch' not in sys.modules and 'sentence_transformers' not in sys.modules"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def deduplicate" app/dedup/__init__.py` devuelve 1
    - `grep -c "def normalize_jobs" app/dedup/__init__.py` devuelve 1 (preservado de Plan 02)
    - `grep -c "from app.dedup.semantic import semantic_cluster\|from app.dedup.exact import exact_group\|from app.dedup.merger import merge_group" app/dedup/__init__.py` devuelve ≥ 3 (imports deferidos)
    - `grep -c "if embedder is None" app/dedup/__init__.py` devuelve ≥ 1 (BgeM3Embedder lazy)
    - `uv run python -c "from app.dedup import normalize_jobs, deduplicate"` no falla
    - Importar app.dedup NO arrastra torch ni sentence_transformers (segunda verificación arriba)
  </acceptance_criteria>
  <done>app/dedup expone normalize_jobs + deduplicate; deduplicate ensambla exact → merge → semantic → merge con embedder inyectable (default BgeM3Embedder perezoso); import del paquete sigue libre de torch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test end-to-end de deduplicate() con FakeEmbedder (QA-03)</name>
  <read_first>
    - tests/cv/test_parser.py (patrón de test integrador con inyección)
    - .planning/phases/02-normalizacion-y-deduplicacion/02-PATTERNS.md (sección test_dedup.py — inyección directa vía parámetro)
    - .planning/phases/02-normalizacion-y-deduplicacion/02-RESEARCH.md (Phase Requirements → Test Map: QA-03 = 3 jobs (2 dupes + 1 unique) → 2 output)
  </read_first>
  <files>tests/dedup/test_dedup.py</files>
  <behavior>
    - deduplicate de 2 jobs con MISMA empresa+título normalizados (duplicado EXACTO) + 1 job distinto
      → 2 jobs de salida (el exacto colapsado), incluso con un embedder ortogonal (la fusión ya ocurrió en nivel 1).
    - deduplicate de 2 jobs DISTINTOS en hash exacto pero semánticamente idénticos (con fake_embedder_identico
      que da similitud 1.0) + 1 job ortogonal → el embedder fuerza la fusión semántica; salida colapsa los dos.
    - deduplicate con fake_embedder_ortogonal (similitud 0) sobre 3 jobs distintos → 3 jobs (nada se fusiona).
    - En una fusión, el Job resultante conserva la descripción MÁS LARGA y su urls_alternativas contiene
      la url de la descartada (DEDUP-03 end-to-end).
    - deduplicate([], embedder=fake) -> [].
    - El test NUNCA instancia BgeM3Embedder (siempre inyecta FakeEmbedder vía embedder=).
  </behavior>
  <action>
    Crear `tests/dedup/test_dedup.py` (test integrador del facade):
    - Construir Jobs de prueba inline usando `stable_job_id` (de app.dedup.normalize) para ids realistas,
      o ids fijos; variar company/title/description/url según cada caso de behavior.
    - Usar inyección DIRECTA: `deduplicate(jobs, embedder=fake_embedder_identico, umbral=0.85)` — preferir
      el parámetro explícito sobre patch() (el seam es un parámetro, como dicta PATTERNS.md).
    - Para forzar fusión semántica controlada, construir el FakeEmbedder con vectores cuyas CLAVES
      coincidan con `build_embed_text(job)` (`f"{title} {company} {primeras N palabras desc}"`), o usar
      `fake_embedder_identico` cuando se quiera similitud 1.0 global.
    - Cubrir TODOS los comportamientos arriba: dup exacto, dup semántico, sin fusión, descripción-más-larga
      + urls_alternativas, lista vacía.
    - El caso central QA-03: lista de 3 (2 duplicados + 1 único) → assert len(resultado) == 2.
  </action>
  <verify>
    <automated>uv run pytest tests/dedup/test_dedup.py -q</automated>
    <automated>uv run pytest tests/dedup -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "deduplicate(" tests/dedup/test_dedup.py` devuelve ≥ 3 (varios casos)
    - `grep -c "embedder=" tests/dedup/test_dedup.py` devuelve ≥ 3 (inyección directa, no patch)
    - `grep -c "BgeM3Embedder" tests/dedup/test_dedup.py` devuelve 0 (nunca el embedder real → no descarga modelo)
    - `grep -c "urls_alternativas" tests/dedup/test_dedup.py` devuelve ≥ 1 (verifica DEDUP-03 end-to-end)
    - Todos los tests de tests/dedup/test_dedup.py pasan en verde
    - La suite completa de dedup pasa: `uv run pytest tests/dedup -q`
  </acceptance_criteria>
  <done>Test end-to-end de deduplicate() con FakeEmbedder: dup exacto + dup semántico + sin-fusión + descripción-más-larga + urls_alternativas + vacío, todo verde sin descargar BGE-M3 (QA-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Job (normalizado) → deduplicate | Entrada ya validada por Pydantic en Plan 02; objetos confiables |
| HuggingFace Hub → proceso | deduplicate(embedder=None) instancia BgeM3Embedder (descarga en uso real); tests siempre inyectan FakeEmbedder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Denial of service | deduplicate() instanciando el embedder real en tests/CI | mitigate | Tests inyectan FakeEmbedder vía parámetro embedder=; gate `grep -c BgeM3Embedder tests == 0` asegura que ningún test descarga el modelo |
| T-02-11 | Denial of service | Import del paquete app.dedup arrastrando torch | mitigate | Imports de submódulos pesados (semantic→numpy, embedder→st) son DEFERIDOS dentro de deduplicate(); verificación afirma torch ausente de sys.modules tras `import app.dedup` |
</threat_model>

<verification>
- `uv run pytest tests/dedup -q` verde (toda la suite de Phase 2).
- `uv run pytest -q` (suite completa del proyecto) verde.
- `from app.dedup import normalize_jobs, deduplicate` importa sin torch.
- Ningún test instancia BgeM3Embedder.
</verification>

<success_criteria>
- deduplicate() ensambla nivel-1 exacto + nivel-2 semántico + merge, con embedder inyectable.
- Importar app.dedup expone ambos facades y NO arrastra torch.
- test_dedup.py cubre QA-03 end-to-end: 3 jobs (2 dupes + 1 único) → 2 salida; descripción más completa conservada; urls_alternativas acumuladas.
- Toda la suite de Phase 2 verde sin descargar el modelo.
</success_criteria>

<output>
After completion, create `.planning/phases/02-normalizacion-y-deduplicacion/02-04-SUMMARY.md`
</output>
