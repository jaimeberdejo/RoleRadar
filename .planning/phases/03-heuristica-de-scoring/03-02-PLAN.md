---
phase: 03-heuristica-de-scoring
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app/config/__init__.py
  - app/config/loader.py
  - app/scoring/ranking.py
  - tests/scoring/test_loader.py
  - tests/scoring/test_ranking.py
autonomous: true
requirements: [SCORE-02, SCORE-06]
must_haves:
  truths:
    - "load_user_profile lee data/profile.yaml y devuelve un UserProfile validado; errores claros si la ruta no existe o el yaml es inválido"
    - "encaje_puesto_desde_rango(rango) es monotónico decreciente: rango 1 → 100, rango 2 → 85, rango 3 → 70, rango 4 → 55; rango None → suelo 20 (> 0, no descarta sola)"
  artifacts:
    - path: "app/config/loader.py"
      provides: "load_user_profile(path) -> UserProfile (yaml.safe_load + model_validate)"
      contains: "def load_user_profile"
    - path: "app/scoring/ranking.py"
      provides: "encaje_puesto_desde_rango(rango, *, paso, suelo, maximo) -> int (decay lineal)"
      contains: "def encaje_puesto_desde_rango"
    - path: "tests/scoring/test_loader.py"
      provides: "tests del loader: ruta válida, inexistente, pesos inválidos, profile.yaml real"
    - path: "tests/scoring/test_ranking.py"
      provides: "tests del decay: valores exactos, monotonía, suelo > 0 para rango None"
  key_links:
    - from: "app/config/loader.py"
      to: "app.models.schemas.UserProfile"
      via: "UserProfile.model_validate(yaml.safe_load(...))"
      pattern: "UserProfile\\.model_validate"
    - from: "tests/scoring/test_loader.py"
      to: "data/profile.yaml"
      via: "load_user_profile carga el perfil real"
      pattern: "load_user_profile"
---

<objective>
Wave 1 (paralelo con Plan 03): los dos módulos DETERMINISTAS más independientes de la fase.
(1) `app/config/loader.py`: cargar y validar el `UserProfile` desde `data/profile.yaml`
(yaml.safe_load → model_validate, errores claros). Es la fuente configurable de los pesos
(SCORE-06) y del ranking/preferencias/deal_breakers que consume el scorer.
(2) `app/scoring/ranking.py`: la función de DECAIMIENTO `encaje_puesto_desde_rango` que convierte
el rango del puesto (que decide el LLM) en `encaje_puesto` (que calcula Python) — "el ranking
como peso graduado, no filtro binario" (SCORE-02). Ambos puramente deterministas y testeables
SIN LLM.

Purpose: Aislar la lógica determinista pura que no depende del LLM para que sea trivialmente
testeable y auditable — el corazón de por qué la heurística no es una caja negra.
Output: loader.py + ranking.py + sus tests unitarios (decay monotónico, suelo, loader válido/inválido).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-heuristica-de-scoring/03-RESEARCH.md
@.planning/phases/03-heuristica-de-scoring/03-PATTERNS.md
@data/profile.yaml

<interfaces>
<!-- Contratos que el executor usa directamente — no explorar el codebase. -->

UserProfile (app/models/schemas.py líneas 79-88) — lo que devuelve el loader:
```python
class UserProfile(BaseModel):
    datos_personales: DatosPersonales
    preferencias_ubicacion: PreferenciasUbicacion
    preferencia_remoto: PreferenciaRemoto
    ranking_puestos: list[PuestoRanking]   # índice 0 = puesto nº1
    expectativas: Optional[Expectativas] = None
    deal_breakers: list[str]
    pesos: PesosScoring                      # valida suma=1.0 (Plan 01)
    dedup_umbral: float = 0.85
```

Patrón de loader (RESEARCH Pattern 1 — autoritativo, sin analog en codebase):
```python
from __future__ import annotations
import logging
from pathlib import Path
import yaml
from pydantic import ValidationError
from app.models.schemas import UserProfile
logger = logging.getLogger(__name__)

def load_user_profile(path: str = "data/profile.yaml") -> UserProfile:
    try:
        with open(Path(path)) as f:
            data = yaml.safe_load(f)
    except FileNotFoundError:
        raise FileNotFoundError(f"No se encontró profile.yaml en: {path}")
    try:
        return UserProfile.model_validate(data)
    except ValidationError as e:
        raise ValueError(f"profile.yaml inválido:\n{e}") from e
```

Decay (RESEARCH Pattern 3 — verificado aritméticamente: paso=15, suelo=20, maximo=100):
fórmula `max(suelo, maximo - (rango-1)*paso)`; rango=None → suelo.
Tabla: 1→100, 2→85, 3→70, 4→55, 5→40, None→20.

Patrón de facade público de paquete (app/cv/__init__.py): docstring + re-export + __all__.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app/scoring/ranking.py — decay lineal del ranking (SCORE-02)</name>
  <read_first>
    - app/dedup/normalize.py (líneas 1-22 — estilo de módulo de funciones puras: docstring, from __future__, logger)
    - .planning/phases/03-heuristica-de-scoring/03-RESEARCH.md Pattern 3 (fórmula + tabla verificada)
    - .planning/phases/03-heuristica-de-scoring/03-PATTERNS.md sección ranking.py
  </read_first>
  <files>app/scoring/ranking.py, tests/scoring/test_ranking.py</files>
  <behavior>
    - encaje_puesto_desde_rango(1) == 100 (rango top → máximo)
    - encaje_puesto_desde_rango(2) == 85; (3) == 70; (4) == 55 (decay lineal paso=15)
    - encaje_puesto_desde_rango(None) == 20 (suelo; fuera de ranking NO se descarta sola → > 0)
    - Monotonía: para todo N, encaje(N) >= encaje(N+1)
    - Suelo respetado: rango muy alto (p.ej. 10) no baja de suelo=20
    - Parámetros configurables: paso/suelo/maximo overridables por keyword
  </behavior>
  <action>
    Crear `app/scoring/ranking.py` (módulo de funciones puras, stdlib only, estilo de
    app/dedup/normalize.py: docstring de responsabilidades, `from __future__ import annotations`,
    `import logging` + `logger = logging.getLogger(__name__)`).

    Implementar EXACTAMENTE (RESEARCH Pattern 3):
    ```python
    def encaje_puesto_desde_rango(
        rango: int | None,
        *,
        paso: int = 15,
        suelo: int = 20,
        maximo: int = 100,
    ) -> int:
        """Score de encaje_puesto a partir del rango del puesto (1-based).

        - rango=1 → maximo (100 por defecto)
        - rango=N → max(suelo, maximo - (N-1)*paso)
        - rango=None (fuera de ranking) → suelo (no 0 — no se descarta solo)
        """
        if rango is None:
            return suelo
        score = maximo - (rango - 1) * paso
        return max(suelo, score)
    ```
    Docstring en español. NO leer profile.yaml para los parámetros — defaults en código,
    configurables por keyword (decisión RESOLVED de RESEARCH Open Question 1).

    Tests en `tests/scoring/test_ranking.py` (análogo a tests/dedup/test_normalize.py, parametrize):
    - `@pytest.mark.parametrize` con (1,100),(2,85),(3,70),(4,55),(None,20) → asserts exactos.
    - `test_monotonia`: scores = [encaje_puesto_desde_rango(i) for i in range(1,6)]; asegurar
      cada valor >= el siguiente.
    - `test_suelo_mayor_que_cero`: assert encaje_puesto_desde_rango(None) > 0.
    - `test_suelo_respetado_rango_alto`: assert encaje_puesto_desde_rango(10) == 20.
    - `test_parametros_configurables`: assert encaje_puesto_desde_rango(2, paso=20, maximo=100) == 80.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo && .venv/bin/python -m pytest tests/scoring/test_ranking.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    test_ranking.py verde; decay devuelve 100/85/70/55 para rangos 1-4, suelo 20 para None y
    rangos altos, monotonía garantizada, parámetros configurables por keyword.
  </done>
</task>

<task type="auto">
  <name>Task 2: app/config/loader.py + facade — cargar UserProfile desde profile.yaml (SCORE-06)</name>
  <read_first>
    - app/cv/__init__.py (líneas 1-9 — patrón de facade público de paquete)
    - app/dedup/normalize.py (líneas 1-22 — estilo de imports + logger)
    - data/profile.yaml (el fichero real que el loader debe cargar)
    - .planning/phases/03-heuristica-de-scoring/03-RESEARCH.md Pattern 1 + Code Examples (loader completo)
  </read_first>
  <files>app/config/loader.py, app/config/__init__.py, tests/scoring/test_loader.py</files>
  <action>
    (a) Crear `app/config/loader.py` exactamente como el patrón de RESEARCH Pattern 1 (ver
    <interfaces>): `from __future__ import annotations`, logging, `pathlib.Path`, `yaml`,
    `pydantic.ValidationError`, `UserProfile`. Función `load_user_profile(path="data/profile.yaml")
    -> UserProfile`:
    - `yaml.safe_load` (NUNCA `yaml.load` sin loader — seguridad).
    - `FileNotFoundError` → re-lanzar con mensaje claro que incluya la ruta.
    - `ValidationError` → re-lanzar como `ValueError(f"profile.yaml inválido:\n{e}") from e`.
    - NO usar `except Exception` genérico — dejar propagar otros errores.
    Docstring en español.

    (b) Crear `app/config/__init__.py` (facade, análogo a app/cv/__init__.py):
    docstring del módulo + `from app.config.loader import load_user_profile` + `__all__ = ["load_user_profile"]`.

    (c) Tests en `tests/scoring/test_loader.py` (análogo a tests/cv/test_cache.py — usa tmp_path):
    - `test_carga_perfil_real`: `load_user_profile("data/profile.yaml")` → UserProfile;
      assert profile.ranking_puestos[0].titulo contiene "AI Engineer";
      assert "presencial fuera de Barcelona" in profile.deal_breakers;
      assert profile.pesos.puesto == 0.35.
    - `test_ruta_inexistente_lanza_filenotfound`: `with pytest.raises(FileNotFoundError)`.
    - `test_yaml_invalido_pesos_lanza_valueerror`: escribir un yaml en tmp_path con pesos que
      no suman 1.0 (copiar profile.yaml válido y poner pesos puesto:0.9/skills:0.9/...) →
      `with pytest.raises(ValueError)`. (El validador de PesosScoring del Plan 01 lo dispara.)
    - `test_carga_perfil_valido_minimo`: escribir un profile.yaml mínimo válido en tmp_path con
      todos los campos requeridos → UserProfile válido.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo && .venv/bin/python -m pytest tests/scoring/test_loader.py -q 2>&1 | tail -5 && .venv/bin/python -c "from app.config import load_user_profile; p=load_user_profile('data/profile.yaml'); assert p.pesos.puesto==0.35; assert 'presencial fuera de Barcelona' in p.deal_breakers; print('OK loader')"</automated>
  </verify>
  <done>
    load_user_profile importable desde `app.config`, carga data/profile.yaml a UserProfile,
    lanza FileNotFoundError para rutas inexistentes y ValueError para yaml/pesos inválidos;
    test_loader.py verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/profile.yaml → UserProfile | Fichero de config local; contenido validado por Pydantic al cargar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | YAML deserialization (ejecución de código vía yaml.load) | mitigate | Usar `yaml.safe_load` exclusivamente — nunca `yaml.load` sin SafeLoader |
| T-03-05 | DoS | profile.yaml malformado tumba el arranque sin mensaje útil | mitigate | FileNotFoundError y ValidationError envueltos con mensaje claro que incluye ruta/campo |
| T-03-03 | Tampering | pesos inválidos sesgan score_total | mitigate | PesosScoring.check_sum (Plan 01) valida suma=1.0 al model_validate dentro del loader |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/scoring/test_ranking.py tests/scoring/test_loader.py -q` verde.
- `load_user_profile('data/profile.yaml')` devuelve un UserProfile coherente con el perfil real.
- decay monotónico con suelo > 0 para rango None.
- `pytest tests/ -q` sin regresión.
</verification>

<success_criteria>
- encaje_puesto_desde_rango: 1→100, 2→85, 3→70, 4→55, None→20, monotónico, parámetros configurables.
- load_user_profile carga/valida profile.yaml con errores claros; usa safe_load.
- Tests de ranking y loader verdes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-heuristica-de-scoring/03-02-SUMMARY.md`
</output>
