---
phase: 03-heuristica-de-scoring
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app/scoring/location.py
  - tests/scoring/test_location.py
autonomous: true
requirements: [SCORE-03]
must_haves:
  truths:
    - "evaluar_ubicacion devuelve (encaje:int, deal_breaker:str|None) determinista según Job.remote + location vs UserProfile"
    - "remote permitido → alto; onsite/hybrid en ciudad aceptada → alto; onsite/hybrid fuera + no reubicarse → muy bajo + deal-breaker; unknown → neutro"
    - "'Barcelona, España' matchea 'Barcelona' por substring NFD normalizado (no igualdad exacta)"
  artifacts:
    - path: "app/scoring/location.py"
      provides: "evaluar_ubicacion(job, profile) -> tuple[int, str|None] + helpers NFD de ciudad"
      contains: "def evaluar_ubicacion"
    - path: "tests/scoring/test_location.py"
      provides: "tests de las 8 ramas del mapa de ubicación + matching de ciudad con sufijo país"
  key_links:
    - from: "app/scoring/location.py"
      to: "app.models.schemas.Job/UserProfile/RemoteJob/ModalidadRemoto"
      via: "reglas deterministas sobre Job.remote + location vs preferencias"
      pattern: "RemoteJob\\."
---

<objective>
Wave 1 (paralelo con Plan 02): el evaluador DETERMINISTA de ubicación/remoto.
`app/scoring/location.py::evaluar_ubicacion(job, profile)` mapea `(Job.remote, Job.location)`
contra las preferencias del `UserProfile` a `(encaje_ubicacion: int, deal_breaker_loc: str|None)`
mediante reglas booleanas puras (SCORE-03). Incluye matching de ciudad accent-insensitive y por
substring (NFD), reutilizando el algoritmo ya establecido en `app/dedup/normalize.py` para que
"Barcelona, España" matchee "Barcelona".

Purpose: La ubicación es uno de los tres ejes deterministas auditables de la heurística y la
fuente de uno de los dos tipos de deal-breaker (el de ubicación). Aislarlo en una función pura
testeable sin LLM es clave para "la ubicación se evalúa correcto" (criterio de éxito 3 + 5).
Output: location.py + test_location.py cubriendo las 8 ramas del mapa de decisión + el caso de
ciudad con sufijo de país.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-heuristica-de-scoring/03-RESEARCH.md
@.planning/phases/03-heuristica-de-scoring/03-PATTERNS.md

<interfaces>
<!-- Contratos que el executor usa directamente — no explorar el codebase. -->

Job (app/models/schemas.py líneas 122-134): id, title, company, location:Optional[str],
remote:RemoteJob (default unknown), description:str, source:str.
RemoteJob enum: remote, hybrid, onsite, unknown.
ModalidadRemoto enum: remote, hybrid, onsite, indiferente.

UserProfile relevante:
- profile.preferencias_ubicacion.ciudades_preferidas: list[str]  (ej. ["Barcelona"])
- profile.preferencias_ubicacion.dispuesto_a_reubicarse: bool   (perfil real: False)
- profile.preferencia_remoto.modalidad_ideal: ModalidadRemoto   (perfil real: remote)
- profile.preferencia_remoto.acepta_onsite_solo_en: list[str]   (perfil real: ["Barcelona"])

NFD normalize ya establecido en app/dedup/normalize.py líneas 50-55 (convención del proyecto:
filtrar por `unicodedata.category(c) != "Mn"`, NO `combining`):
```python
s = unicodedata.normalize("NFD", s)
s = "".join(c for c in s if unicodedata.category(c) != "Mn")
return re.sub(r"\s+", " ", s.casefold().strip())
```

Mapa de decisión COMPLETO (RESEARCH Pattern 4 — 8 ramas + unknown):
| job.remote | ciudad_aceptada | dispuesto_a_reubicarse | encaje | deal_breaker |
| remote + modal in (remote,hybrid,indiferente) | any | any | 100 | None |
| remote + modal=onsite | any | any | 60 | None |
| hybrid | True | any | 80 | None |
| hybrid | False | False | 10 | "Trabajo híbrido fuera de ciudades aceptadas: {city}" |
| hybrid | False | True | 50 | None |
| onsite | True | any | 90 | None |
| onsite | False | False | 10 | "Presencial fuera de ciudades aceptadas: {city}" |
| onsite | False | True | 40 | None |
| unknown | any | any | 50 | None |

"ciudades aceptadas" = ciudades_preferidas + acepta_onsite_solo_en (concatenar ambas listas).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app/scoring/location.py — evaluar_ubicacion determinista (SCORE-03)</name>
  <read_first>
    - app/dedup/normalize.py (líneas 1-55 — algoritmo NFD a replicar; convención category != "Mn")
    - .planning/phases/03-heuristica-de-scoring/03-RESEARCH.md Pattern 4 + Code Examples (city normalization) + Pitfall 6 (substring match)
    - .planning/phases/03-heuristica-de-scoring/03-PATTERNS.md sección location.py (matriz de 8 casos)
  </read_first>
  <files>app/scoring/location.py, tests/scoring/test_location.py</files>
  <behavior>
    - remote + modalidad_ideal=remote → (100, None)
    - remote + modalidad_ideal=onsite → (60, None)  (remote es flexible aunque prefiera onsite)
    - onsite + location "Barcelona, España", ciudad aceptada → (90, None)
    - onsite + location "Madrid, España", no aceptada, dispuesto_a_reubicarse=False → (10, str que menciona "Madrid")
    - onsite + location "Madrid", no aceptada, dispuesto_a_reubicarse=True → (40, None)
    - hybrid + ciudad aceptada → (80, None)
    - hybrid + ciudad NO aceptada + no reubicarse → (10, str)
    - hybrid + ciudad NO aceptada + sí reubicarse → (50, None)
    - unknown → (50, None)
    - "Barcelona, España" matchea "Barcelona" (substring NFD, Pitfall 6)
    - location=None con onsite no aceptada → trata como no-aceptada (no crashea)
  </behavior>
  <action>
    Crear `app/scoring/location.py`:
    - `from __future__ import annotations`, `import logging`, `import unicodedata`, importar
      `Job, ModalidadRemoto, RemoteJob, UserProfile` de app.models.schemas, `logger = logging.getLogger(__name__)`.
    - NO importar app.dedup (Pitfall 7 — arrastraría torch/BGE-M3 a los tests de scoring).
      Replicar el algoritmo NFD localmente (es trivial, evita el acoplamiento).

    Helpers privados:
    ```python
    def _normalize_city(name: str) -> str:
        nfd = unicodedata.normalize("NFD", name.lower().strip())
        return "".join(c for c in nfd if unicodedata.category(c) != "Mn")

    def _ciudad_aceptada(location: str | None, ciudades: list[str]) -> bool:
        if not location:
            return False
        loc_norm = _normalize_city(location)
        return any(_normalize_city(c) in loc_norm for c in ciudades)
    ```
    (substring match, Pitfall 6 — NO igualdad exacta).

    Función pública:
    ```python
    def evaluar_ubicacion(job: Job, profile: UserProfile) -> tuple[int, str | None]:
        """Devuelve (encaje_ubicacion: 0-100, deal_breaker_descripcion | None)."""
    ```
    Implementar el mapa de decisión COMPLETO de las 8 ramas + unknown (ver <interfaces>):
    - `ciudades_aceptadas = profile.preferencias_ubicacion.ciudades_preferidas + profile.preferencia_remoto.acepta_onsite_solo_en`
    - `aceptada = _ciudad_aceptada(job.location, ciudades_aceptadas)`
    - `reubicar = profile.preferencias_ubicacion.dispuesto_a_reubicarse`
    - `modal = profile.preferencia_remoto.modalidad_ideal`
    - RemoteJob.remote: si modal in (remote, hybrid, indiferente) → (100, None); si modal==onsite → (60, None)
    - RemoteJob.hybrid: aceptada → (80, None); no aceptada y not reubicar → (10, f"Trabajo híbrido fuera de ciudades aceptadas: {job.location}"); no aceptada y reubicar → (50, None)
    - RemoteJob.onsite: aceptada → (90, None); no aceptada y not reubicar → (10, f"Presencial fuera de ciudades aceptadas: {job.location}"); no aceptada y reubicar → (40, None)
    - RemoteJob.unknown (o cualquier otro) → (50, None)
    Loggear con logger.debug el encaje calculado. Docstrings en español.

    Tests en `tests/scoring/test_location.py` (análogo a tests/dedup/test_normalize.py):
    - Una función de test por cada rama (≥9 tests). Usar el fixture `sample_user_profile` del
      conftest (perfil real: modalidad_ideal=remote, acepta_onsite_solo_en=["Barcelona"],
      dispuesto_a_reubicarse=False) y construir Jobs ad-hoc o usar los fixtures de Job del conftest.
    - Para las ramas que requieren `dispuesto_a_reubicarse=True`, construir un UserProfile variante
      con `model_copy(update=...)` o reconstruirlo (usar el fixture como base).
    - Test explícito Pitfall 6: Job onsite con location="Barcelona, España" → encaje==90 (matchea
      "Barcelona" por substring).
    - Test robustez: Job onsite con location=None y ciudad no aceptada → (10, str) sin crash.
    - Cada test verifica AMBOS valores de la tupla (encaje exacto + deal_breaker None o str).
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo && .venv/bin/python -m pytest tests/scoring/test_location.py -q 2>&1 | tail -6</automated>
  </verify>
  <done>
    test_location.py verde con un test por rama del mapa (≥9); evaluar_ubicacion devuelve
    (encaje, deal_breaker) correcto para remote/hybrid/onsite/unknown; "Barcelona, España" matchea
    "Barcelona"; location=None no crashea; el deal-breaker de ubicación solo se dispara cuando
    onsite/hybrid fuera de ciudad aceptada y no dispuesto a reubicarse.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Job.location (de fuente externa vía n8n) → evaluar_ubicacion | Texto no confiable; solo se usa para comparación de substring normalizado, nunca se ejecuta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | DoS | Job.location=None o vacío rompe el matching | mitigate | `_ciudad_aceptada` retorna False si location es falsy; rama unknown/no-aceptada cubre el caso sin crash |
| T-03-07 | Tampering | encaje_ubicacion fuera de 0-100 | accept | Todos los valores del mapa son constantes literales 10/40/50/60/80/90/100 — imposible salir de rango |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/scoring/test_location.py -q` verde (≥9 tests, una rama cada uno).
- "Barcelona, España" matchea "Barcelona" (substring NFD).
- onsite Madrid + no reubicarse → (10, deal-breaker str); remote → (100, None); unknown → (50, None).
- `pytest tests/ -q` sin regresión.
</verification>

<success_criteria>
- evaluar_ubicacion cubre las 8 ramas + unknown del mapa de RESEARCH Pattern 4.
- City matching accent-insensitive por substring (NFD, convención del proyecto).
- Deal-breaker de ubicación solo en onsite/hybrid fuera de ciudad aceptada sin reubicación.
- test_location.py verde.
</success_criteria>

<output>
After completion, create `.planning/phases/03-heuristica-de-scoring/03-03-SUMMARY.md`
</output>
