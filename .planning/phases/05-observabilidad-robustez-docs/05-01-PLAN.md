---
phase: 05-observabilidad-robustez-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/obs/__init__.py
  - app/obs/__init__.py
  - app/obs/logging_config.py
  - app/obs/tracing.py
  - tests/obs/test_logging.py
  - tests/obs/test_tracing.py
autonomous: true
requirements: [OBS-01, OBS-02]
must_haves:
  truths:
    - "configure_logging() establece un formatter key=value y es idempotente (no duplica handlers si se llama dos veces)"
    - "trace_llm() es un context manager que ejecuta el bloque envuelto sin lanzar cuando langfuse no está instalado/configurado (no-op transparente, el caso por defecto)"
    - "Importar app.obs.tracing NO importa langfuse (la app sigue torch-free y langfuse-free)"
  artifacts:
    - path: "app/obs/logging_config.py"
      provides: "configure_logging(level=INFO) idempotente, stdlib logging"
      contains: "def configure_logging"
    - path: "app/obs/tracing.py"
      provides: "trace_llm(name, **meta) context manager no-op por defecto"
      contains: "def trace_llm"
    - path: "tests/obs/test_logging.py"
      provides: "test de idempotencia de configure_logging"
    - path: "tests/obs/test_tracing.py"
      provides: "tests del no-op de trace_llm (sin env vars, sin langfuse)"
  key_links:
    - from: "app/obs/tracing.py"
      to: "os.environ LANGFUSE_PUBLIC_KEY/SECRET_KEY"
      via: "_LANGFUSE_CONFIGURED evaluado una vez al importar"
      pattern: "_LANGFUSE_CONFIGURED"
---

<objective>
Crear los módulos de observabilidad de Fase 5 (sin tocar el resto de la app): el helper
de configuración de logging estructurado (OBS-01) y el stub de tracing de Langfuse (OBS-02),
con sus tests. Ambos son ficheros nuevos y autocontenidos — no modifican ningún call site
todavía (eso es el Plan 02).

Purpose: Dejar listas las dos piezas de observabilidad antes de cablearlas. El stub de
tracing es el ítem de mayor riesgo de la fase (API Langfuse v3 vs v4); aislarlo en su propio
plan con tests dedicados garantiza que el no-op por defecto (caso CI/dev) nunca rompa nada.
Output: app/obs/ (paquete) + tests/obs/ (paquete de tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-observabilidad-robustez-docs/05-RESEARCH.md
@.planning/phases/05-observabilidad-robustez-docs/05-PATTERNS.md

<interfaces>
<!-- Patrón canónico de import perezoso/opcional (analog: app/dedup/embedder.py L76-79) -->
<!-- El executor debe copiar el comentario `# noqa: PLC0415` verbatim — convención del proyecto -->

Analog de import perezoso de dep pesada (app/dedup/embedder.py):
```python
def _ensure_loaded(self) -> None:
    if self._model is None:
        from sentence_transformers import SentenceTransformer  # noqa: PLC0415
        self._model = SentenceTransformer("BAAI/bge-m3")
```

Analog de package init con docstring + re-exports (app/dedup/__init__.py):
```python
"""Docstring del paquete..."""
from __future__ import annotations
__all__ = ["normalize_jobs", "deduplicate"]
```

Formato de log estructurado existente en el proyecto (app/api/routes/jobs.py L122-128):
```python
logger.info("normalize_endpoint: source=%s in=%d ok=%d errors=%d", source, ...)
```

API Langfuse 4.x (verificada en Context7 — NO usar @observe de v3):
`Langfuse().start_as_current_observation(name=..., as_type="generation", metadata=meta)` como context manager.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear paquetes app/obs/ y tests/obs/ con sus __init__.py (Wave 0)</name>
  <read_first>
    - app/dedup/__init__.py (analog de docstring + __all__ de paquete)
    - tests/__init__.py (analog de __init__ de paquete de tests vacío)
  </read_first>
  <files>app/obs/__init__.py, tests/obs/__init__.py</files>
  <action>
    Crear el paquete `app/obs/` con un `__init__.py` que tenga un docstring breve en español
    describiendo el paquete ("Paquete de observabilidad: configuración de logging estructurado
    (OBS-01) y stub de tracing de Langfuse (OBS-02)."), `from __future__ import annotations`, y
    re-exports de los símbolos públicos `configure_logging` y `trace_llm` con `__all__ =
    ["configure_logging", "trace_llm"]`. Los imports de los re-exports van al final del fichero
    DESPUÉS de crear los módulos del Task 2/3 — si el executor crea __init__.py antes, dejar los
    re-exports y crear los módulos en el mismo plan (todos en este plan, sin riesgo de import roto
    al final). Seguir la convención del proyecto: __init__.py no vacío con docstring claro.

    Crear `tests/obs/__init__.py` vacío (o con un docstring de una línea), idéntico al patrón de
    `tests/__init__.py` — es el paquete de tests de observabilidad requerido por Wave 0.
  </action>
  <verify>
    <automated>test -f app/obs/__init__.py && test -f tests/obs/__init__.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/obs/__init__.py` retorna 0
    - `test -f tests/obs/__init__.py` retorna 0
    - `grep -q "from __future__ import annotations" app/obs/__init__.py`
    - `grep -q "__all__" app/obs/__init__.py`
  </acceptance_criteria>
  <done>Ambos paquetes existen y son importables; app/obs/__init__.py re-exporta configure_logging y trace_llm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: configure_logging() en app/obs/logging_config.py (OBS-01) + test de idempotencia</name>
  <read_first>
    - .planning/phases/05-observabilidad-robustez-docs/05-RESEARCH.md (Pattern 3: Structured Logging + Pitfall 3 handlers duplicados)
    - app/api/main.py (formato de logger y patrón de logging existente)
  </read_first>
  <files>app/obs/logging_config.py, tests/obs/test_logging.py</files>
  <behavior>
    - Test idempotencia: llamar configure_logging() dos veces NO añade un segundo StreamHandler al root logger (Pitfall 3). Asegurar el conteo de handlers no crece tras la segunda llamada.
    - Test formato: el formatter del handler instalado incluye los tokens `level=` y `logger=` en su fmt (formato key=value grep-able).
    - Test nivel: tras configure_logging(level=logging.DEBUG) el root logger queda con level DEBUG.
  </behavior>
  <action>
    Crear `app/obs/logging_config.py` con `from __future__ import annotations`, `import logging`,
    `import sys`. Definir `def configure_logging(level: int = logging.INFO) -> None:` con docstring
    en español. Implementación EXACTA según RESEARCH Pattern 3:
    - `root = logging.getLogger()`
    - `if root.handlers: return` (idempotente — Pitfall 3, evita handlers duplicados en tests/reload)
    - crear `handler = logging.StreamHandler(sys.stdout)`
    - `handler.setFormatter(logging.Formatter(fmt="%(asctime)s level=%(levelname)s logger=%(name)s %(message)s", datefmt="%Y-%m-%dT%H:%M:%S"))`
    - `root.addHandler(handler)` y `root.setLevel(level)`
    Type hints en todo. stdlib logging ÚNICAMENTE — NO structlog ni deps nuevas (decisión OBS-01).

    Crear `tests/obs/test_logging.py` con `from __future__ import annotations`, `import logging`,
    `import pytest`, e importar configure_logging. CUIDADO con el estado global del root logger:
    los tests deben guardar/restaurar `root.handlers` y `root.level` (usar una fixture que haga
    `saved = root.handlers[:]; saved_level = root.level; yield; root.handlers[:] = saved;
    root.setLevel(saved_level)`) para no contaminar otros tests ni que el estado de pytest
    interfiera. Implementar los tres tests del bloque <behavior> arriba. El test de idempotencia
    debe partir de un root SIN handlers (limpiarlos en la fixture antes del test) para verificar
    que la 1ª llamada añade exactamente 1 handler y la 2ª no añade ninguno más.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/obs/test_logging.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m pytest tests/obs/test_logging.py -q` pasa (todos los tests verdes)
    - `grep -q "if root.handlers" app/obs/logging_config.py` (guard de idempotencia presente)
    - `grep -q "level=%(levelname)s" app/obs/logging_config.py` (formato key=value)
    - `grep -v '^#' app/obs/logging_config.py | grep -cE "structlog|loguru" | grep -q '^0$'` (sin deps de logging nuevas)
  </acceptance_criteria>
  <done>configure_logging() es idempotente, usa formato key=value con stdlib logging; test de idempotencia + formato + nivel verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: trace_llm() stub no-op en app/obs/tracing.py (OBS-02) + tests del no-op</name>
  <read_first>
    - .planning/phases/05-observabilidad-robustez-docs/05-RESEARCH.md (Pattern 1: Optional Import No-Op Stub + Pitfalls 1, 2)
    - .planning/phases/05-observabilidad-robustez-docs/05-PATTERNS.md (sección app/obs/tracing.py — analog embedder.py)
    - app/dedup/embedder.py (analog de import perezoso con # noqa: PLC0415)
  </read_first>
  <files>app/obs/tracing.py, tests/obs/test_tracing.py</files>
  <behavior>
    - Test no-op sin env vars: con LANGFUSE_PUBLIC_KEY y LANGFUSE_SECRET_KEY ausentes, `with trace_llm("op"):` ejecuta el bloque interno sin lanzar y sin tocar langfuse. El cuerpo del with se ejecuta exactamente una vez.
    - Test no-op aunque langfuse esté "configurado" pero no instalado: con las env vars puestas pero langfuse ausente del entorno (que es el caso real — langfuse NO está en pyproject.toml), `with trace_llm("op"):` sigue siendo no-op (try/except ImportError → yield) y NO lanza.
    - Test import-safety: `import app.obs.tracing` no importa langfuse (verificar que "langfuse" no está en sys.modules tras importar el módulo, partiendo de un entorno donde langfuse no está instalado).
  </behavior>
  <action>
    Crear `app/obs/tracing.py` con docstring en español que declare el invariante explícitamente
    (espejando app/dedup/embedder.py): "Importar este módulo NO debe importar langfuse. langfuse
    es una dependencia OPCIONAL — jamás se añade a pyproject.toml; el import es perezoso dentro del
    context manager." Implementación EXACTA según RESEARCH Pattern 1:
    - `from __future__ import annotations`, `import os`, `from contextlib import contextmanager`,
      `from typing import Any, Generator`.
    - `_LANGFUSE_CONFIGURED = bool(os.environ.get("LANGFUSE_PUBLIC_KEY")) and bool(os.environ.get("LANGFUSE_SECRET_KEY"))`
      — evaluado UNA VEZ al importar el módulo (Anti-pattern: no re-evaluar en cada llamada).
    - `@contextmanager def trace_llm(name: str, **meta: Any) -> Generator[None, None, None]:` con docstring.
      Cuerpo: si `not _LANGFUSE_CONFIGURED` → `yield; return` (no-op). Si configurado:
      `try: from langfuse import Langfuse  # noqa: PLC0415` `except ImportError: yield; return`.
      Luego `lf = Langfuse()` y `with lf.start_as_current_observation(name=name, as_type="generation", metadata=meta): yield`.
    - Doble guard: env vars PRIMERO, import DESPUÉS (Pitfall 2 — evita AuthenticationError al
      instanciar Langfuse() sin credenciales). El `# noqa: PLC0415` verbatim.

    Crear `tests/obs/test_tracing.py` con `from __future__ import annotations`. Implementar los
    tres tests del bloque <behavior>. Para los tests que manipulan env vars usar `monkeypatch.setenv`
    / `monkeypatch.delenv(..., raising=False)` + `importlib.reload(app.obs.tracing)` para
    re-evaluar `_LANGFUSE_CONFIGURED` (el flag se fija al importar). Para el test import-safety,
    verificar `"langfuse" not in sys.modules` (es seguro porque langfuse no está instalado en el
    entorno — confirmado ausente de pyproject.toml). NO mockear ni instalar langfuse: el objetivo
    es probar el CAMINO POR DEFECTO (no-op), que es el caso de CI/dev. CERO red.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/obs/test_tracing.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m pytest tests/obs/test_tracing.py -q` pasa (todos los tests verdes)
    - `grep -q "_LANGFUSE_CONFIGURED" app/obs/tracing.py` y aparece a nivel de módulo (no dentro de la función)
    - `grep -q "# noqa: PLC0415" app/obs/tracing.py` (import perezoso con convención del proyecto)
    - `grep -q "except ImportError" app/obs/tracing.py` (segundo guard contra langfuse no instalado)
    - `grep -v '^#' pyproject.toml | grep -ci langfuse | grep -q '^0$'` (langfuse NO añadido como dependencia)
    - `python -c "import app.obs.tracing, sys; assert 'langfuse' not in sys.modules"` retorna 0
  </acceptance_criteria>
  <done>trace_llm es un no-op transparente por defecto (sin env vars o sin langfuse); importar el módulo no arrastra langfuse; tests del no-op verdes; langfuse ausente de pyproject.toml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entorno → app (env vars) | LANGFUSE_PUBLIC_KEY/SECRET_KEY se leen del entorno; nunca hardcoded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | app/obs/tracing.py | mitigate | Credenciales Langfuse solo desde os.environ (env vars), jamás en código; verificado por grep en acceptance criteria |
| T-05-02 | Tampering | app/obs/tracing.py | accept | El stub es no-op por defecto y no abre superficie de ataque nueva (no red, no escritura) cuando langfuse no está configurado — el caso por defecto |
</threat_model>

<verification>
- `uv run python -m pytest tests/obs/ -q` — todos los tests de obs verdes
- `uv run python -m pytest -q` — suite completa sigue verde (162 baseline + nuevos, sin regresiones)
- `python -c "import app.obs.tracing, sys; assert 'langfuse' not in sys.modules"` — import torch/langfuse-free
</verification>

<success_criteria>
- configure_logging() existe, es idempotente y emite formato key=value con stdlib logging (OBS-01 parcial — el cableado en lifespan y el contador llm_calls son Plan 02)
- trace_llm() existe como context manager no-op por defecto y nunca lanza cuando langfuse no está instalado/configurado (OBS-02 stub)
- app.obs es importable sin arrastrar langfuse; langfuse NO está en pyproject.toml
- tests/obs/ existe con tests verdes para ambos módulos; suite completa sin regresiones
</success_criteria>

<output>
After completion, create `.planning/phases/05-observabilidad-robustez-docs/05-01-SUMMARY.md`
</output>
