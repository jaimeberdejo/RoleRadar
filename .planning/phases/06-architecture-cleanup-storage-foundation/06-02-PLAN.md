---
phase: 06-architecture-cleanup-storage-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - "06-01"
files_modified:
  - docker-compose.yml
autonomous: false
requirements:
  - MIG-03

must_haves:
  truths:
    - "The literal string 'dfae5ddf3fmshc64763c72ab92c1p17c53fjsnee4d98fb1148' does not appear anywhere in docker-compose.yml"
    - "uv run pytest tests/test_no_secrets.py -q exits 0"
    - "The user has been prompted to rotate the leaked RapidAPI key on the RapidAPI dashboard"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Secret-free compose file"
      contains: "RAPIDAPI_KEY: dfae"
      note: "must NOT contain the above string after this plan"
  key_links:
    - from: "docker-compose.yml"
      to: ".env"
      via: "env_file: .env injection"
      pattern: "env_file"
---

<objective>
Remove the hardcoded RAPIDAPI_KEY from docker-compose.yml (line 92) and prompt the user to rotate the leaked key on the RapidAPI dashboard.

Purpose: The literal API key 'dfae5ddf3fmshc64763c72ab92c1p17c53fjsnee4d98fb1148' was visible in the working tree and was exposed in chat. This is a HIGH severity information disclosure (T-6-secret). The code change is simple (remove the RAPIDAPI_KEY line from the n8n environment block) — the key is already injected via env_file. The user must rotate the key out-of-band; Claude cannot do this for them.

Output: docker-compose.yml with the literal key removed. tests/test_no_secrets.py turns GREEN.

Note: This plan has autonomous: false because it includes a checkpoint requiring the user to confirm they have rotated the key on the RapidAPI dashboard before proceeding. Docker restructure (MIG-02/MIG-04) happens in Plan 05 — this plan only removes the literal secret from the n8n environment block.
</objective>

<execution_context>
@/Users/jaimeberdejosanchez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaimeberdejosanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo/.planning/PROJECT.md
@/Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo/.planning/ROADMAP.md
@/Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo/.planning/STATE.md

<interfaces>
<!-- docker-compose.yml current state — line 92 is the target. -->

From docker-compose.yml (n8n service environment block, lines 88-93):
```yaml
      RAPIDAPI_KEY: dfae5ddf3fmshc64763c72ab92c1p17c53fjsnee4d98fb1148
      API_KEY: ${API_KEY:-}
```

The RAPIDAPI_KEY line must be removed entirely. The remaining n8n environment block continues with API_KEY.

Note: In Plan 05, the entire n8n service block will be removed from docker-compose.yml. This plan only removes the literal secret from the existing n8n block so the secret is not committed. Both changes are necessary — this one is safety-critical and happens first.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove literal RAPIDAPI_KEY from docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (full — to locate line 92 and understand surrounding context)
  </read_first>
  <action>
In docker-compose.yml, inside the `n8n:` service's `environment:` block, remove the line:

```yaml
      RAPIDAPI_KEY: dfae5ddf3fmshc64763c72ab92c1p17c53fjsnee4d98fb1148
```

Do NOT change any other line. Do NOT restructure the compose file yet (that happens in Plan 05).

After removing the line, update the comment block at lines 88-91 to remove the RAPIDAPI_KEY reference:

Replace:
```yaml
      # Claves para los nodos HTTP, interpoladas desde .env (NO hardcodeadas).
      # Añade RAPIDAPI_KEY=... (y API_KEY=... si activas la auth) a tu .env.
      # El ":-" deja la var vacía si no está en .env, sin romper el arranque.
      RAPIDAPI_KEY: dfae5ddf3fmshc64763c72ab92c1p17c53fjsnee4d98fb1148
      API_KEY: ${API_KEY:-}
```

With:
```yaml
      # Claves para los nodos HTTP interpoladas desde .env (NO hardcodeadas).
      # Añade API_KEY=... a tu .env si activaste la auth del servicio FastAPI.
      # El ":-" deja la var vacía si no está en .env, sin romper el arranque.
      # NOTA: RAPIDAPI_KEY retirada de aquí — inyectada vía env_file: .env
      API_KEY: ${API_KEY:-}
```

The .env file already contains (or will contain) RAPIDAPI_KEY and env_file: .env on the n8n service already injects it. The variable does not need to be re-declared in the environment block.
  </action>
  <verify>
    <automated>uv run pytest tests/test_no_secrets.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_no_secrets.py -q` exits 0 (tests GREEN)
    - `grep -c "dfae5ddf3fmshc64763c72ab92c1p17c53fjsnee4d98fb1148" docker-compose.yml` returns 0
    - `grep -c "env_file" docker-compose.yml` returns at least 1 (env_file injection still present for the n8n service)
    - `uv run pytest tests/storage/ tests/test_import_cleanliness.py -q` exits 0 (no regressions from Wave 0 tests that were GREEN)
  </acceptance_criteria>
  <done>Literal RAPIDAPI_KEY removed from docker-compose.yml; test_no_secrets.py GREEN</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Removed the literal RAPIDAPI_KEY value from docker-compose.yml. The key 'dfae5ddf3fmshc64763c72ab92c1p17c53fjsnee4d98fb1148' no longer appears in any committed file.</what-built>
  <how-to-verify>
SECURITY ACTION REQUIRED — Please rotate the leaked RapidAPI key:

1. Go to https://rapidapi.com and log in
2. Navigate to "My Apps" → your JSearch subscription → "Security" or "API Keys"
3. Regenerate / rotate the API key (this invalidates the old key 'dfae5ddf3f...')
4. Copy the new key value
5. Update your local .env file: set RAPIDAPI_KEY=&lt;new-key-value&gt;
6. Verify no other place uses the old key: `grep -r "dfae5ddf3f" .` (should return nothing)

Once the key is rotated and your .env is updated, type "approved" to continue.

Note: This rotation is out-of-band (only you can do it on the RapidAPI dashboard). The code change above has already been made — this checkpoint exists solely to ensure the old key is invalidated before Phase 8 uses the new key to call JSearch.
  </how-to-verify>
  <resume-signal>Type "approved" after rotating the key on RapidAPI, or "skip" if you will rotate it separately before Phase 8 begins</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker-compose.yml → container env | env vars injected at runtime via env_file; no literal values |
| .env file → host filesystem | .env is gitignored and dockerignored; never committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-secret | Information Disclosure | RAPIDAPI_KEY literal in docker-compose.yml | mitigate | Remove literal from compose; inject via env_file from gitignored .env; user must rotate key out-of-band (checkpoint task) |
| T-6-02 | Information Disclosure | Old key still valid after code fix | mitigate | Checkpoint:human-verify forces rotation confirmation before proceeding; key is useless once rotated even if cached in git history (key was never committed per REQUIREMENTS.md) |
| T-6-03 | Tampering | Parameterized queries in settings table | mitigate | Plan 04 uses `?` placeholders exclusively; no string formatting of SQL (verified in PATTERNS.md code excerpts) |
</threat_model>

<verification>
```bash
# Verify secret removed
grep -c "dfae5ddf3fmshc64763c72ab92c1p17c53fjsnee4d98fb1148" docker-compose.yml
# must return 0

# Automated test
uv run pytest tests/test_no_secrets.py -q
# must exit 0

# No regressions
uv run pytest tests/storage/ tests/test_import_cleanliness.py -q
```
</verification>

<success_criteria>
- tests/test_no_secrets.py::test_docker_compose_no_literal_rapidapi_key passes (GREEN)
- The string 'dfae5ddf3fmshc64763c72ab92c1p17c53fjsnee4d98fb1148' does not appear in any file tracked by git
- User has confirmed key rotation (checkpoint approved) or acknowledged it is pending before Phase 8
</success_criteria>

<output>
After completion, create `.planning/phases/06-architecture-cleanup-storage-foundation/06-02-SUMMARY.md`
</output>
