---
phase: 06-architecture-cleanup-storage-foundation
plan: 05
type: execute
wave: 2
depends_on:
  - "06-02"
  - "06-03"
  - "06-04"
files_modified:
  - docker-compose.yml
  - Dockerfile
  - worker.py
  - .env.example
autonomous: true
requirements:
  - MIG-02
  - MIG-04
  - DOC-05

must_haves:
  truths:
    - "docker compose config shows exactly two services: ui and worker — no buscadordeempleo service, no n8n service"
    - "Both ui and worker services use image: buscadordeempleo:latest from the same build"
    - "Both services mount the same named volumes: data and hf_cache"
    - "worker.py exists at the repo root and can be imported without error"
    - "worker.py starts a BlockingScheduler with ZoneInfo('Europe/Madrid') timezone"
    - ".env.example contains RAPIDAPI_KEY, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID, SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASSWORD and does NOT contain API_KEY= or any FastAPI variables as active (uncommented) lines"
    - "Dockerfile CMD no longer references app.api.main or uvicorn"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Two-service compose: ui (Streamlit) + worker (APScheduler)"
      contains: "service: ui"
    - path: "worker.py"
      provides: "Worker entrypoint stub — BlockingScheduler with run_pipeline stub"
      contains: "BlockingScheduler"
    - path: ".env.example"
      provides: "v2.0 env var documentation"
      contains: "RAPIDAPI_KEY"
    - path: "Dockerfile"
      provides: "Neutral CMD (not uvicorn)"
  key_links:
    - from: "docker-compose.yml ui service"
      to: "ui/app.py"
      via: "command: uv run streamlit run ui/app.py"
      pattern: "ui/app.py"
    - from: "docker-compose.yml worker service"
      to: "worker.py"
      via: "command: uv run python worker.py"
      pattern: "worker.py"
    - from: "worker.py"
      to: "apscheduler.schedulers.blocking.BlockingScheduler"
      via: "BlockingScheduler(timezone=ZoneInfo('Europe/Madrid'))"
      pattern: "BlockingScheduler"
---

<objective>
Restructure docker-compose.yml to two services (ui + worker), update Dockerfile CMD, create worker.py stub, and rewrite .env.example for v2.0.

Purpose: This plan completes the infrastructure shell for the v2.0 Streamlit+APScheduler architecture. After this plan, `docker compose up` starts only the ui and worker containers from a single image — no FastAPI service, no n8n. The worker container stays alive via the BlockingScheduler stub (Phase 8 will implement the actual pipeline). The .env.example is the v2.0 documentation artifact that operators copy to configure the tool.

Output:
- docker-compose.yml rewritten to ui+worker two-service structure (n8n service and n8n_data volume removed)
- Dockerfile CMD changed from uvicorn to neutral print statement
- worker.py stub created at repo root with BlockingScheduler + run_pipeline stub
- .env.example rewritten with v2.0 vars (RAPIDAPI_KEY, TELEGRAM_*, SMTP_*, OPENAI optional; API_KEY removed)
- ui/ placeholder directory created (actual UI in Phase 10)
</objective>

<execution_context>
@/Users/jaimeberdejosanchez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaimeberdejosanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo/.planning/PROJECT.md
@/Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo/.planning/ROADMAP.md
@/Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo/.planning/STATE.md
@/Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo/.planning/phases/06-architecture-cleanup-storage-foundation/06-02-SUMMARY.md
@/Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo/.planning/phases/06-architecture-cleanup-storage-foundation/06-03-SUMMARY.md
@/Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo/.planning/phases/06-architecture-cleanup-storage-foundation/06-04-SUMMARY.md

<interfaces>
<!-- Exact compose v2 two-service pattern from RESEARCH.md Pattern 3 -->

Target docker-compose.yml structure (verified from Docker Compose docs):
```yaml
services:
  ui:
    build: .                            # build once; worker reuses same image
    image: buscadordeempleo:latest
    command: ["uv", "run", "streamlit", "run", "ui/app.py", "--server.port", "8501"]
    ports:
      - "8501:8501"
    env_file: .env
    environment:
      SQLITE_DB_PATH: /app/data/jobs.db
      HF_HOME: /app/.cache/huggingface
    volumes:
      - data:/app/data
      - hf_cache:/app/.cache/huggingface
    restart: unless-stopped

  worker:
    image: buscadordeempleo:latest      # reuses image built by ui service
    command: ["uv", "run", "python", "worker.py"]
    depends_on: [ui]
    env_file: .env
    environment:
      SQLITE_DB_PATH: /app/data/jobs.db
      HF_HOME: /app/.cache/huggingface
    volumes:
      - data:/app/data
      - hf_cache:/app/.cache/huggingface
    restart: unless-stopped

volumes:
  data:
    driver: local
  hf_cache:
    driver: local
# n8n_data volume REMOVED
```

Target worker.py (from PATTERNS.md lines 294-331):
```python
"""Worker BuscadorDeEmpleo — proceso de fondo con APScheduler.

Punto de entrada del contenedor `worker` en docker-compose.yml.
En Fase 8 se implementará el pipeline completo; esta versión es un stub
que arranca el scheduler vacío para que el contenedor permanezca activo.

NO instanciar APScheduler dentro de Streamlit — este módulo corre en un
proceso separado.
"""
from __future__ import annotations

import logging
from zoneinfo import ZoneInfo

from apscheduler.schedulers.blocking import BlockingScheduler

logger = logging.getLogger(__name__)


def run_pipeline() -> None:
    """Pipeline principal — implementado en Fase 8."""
    logger.info("Pipeline stub: no implementado aún (Fase 8)")


if __name__ == "__main__":
    logging.basicConfig(level=logging.INFO)
    logger.info("Worker arrancando (stub — Fase 8 implementa el pipeline)")
    scheduler = BlockingScheduler(
        timezone=ZoneInfo("Europe/Madrid"),
        job_defaults={
            "coalesce": True,
            "misfire_grace_time": 3600,
            "max_instances": 1,
        },
    )
    scheduler.add_job(run_pipeline, "interval", hours=6, id="pipeline")
    scheduler.start()
```

Target Dockerfile CMD (from PATTERNS.md lines 225-231):
Replace:
```dockerfile
CMD ["uv", "run", "uvicorn", "app.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
```
With:
```dockerfile
CMD ["uv", "run", "python", "-c", "print('Use: docker compose up')"]
```
Also remove: `EXPOSE 8000` line (or leave it — docker compose overrides port anyway; removing is cleaner).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docker-compose.yml, update Dockerfile, create worker.py and ui/ placeholder</name>
  <files>docker-compose.yml, Dockerfile, worker.py, ui/.gitkeep</files>
  <read_first>
    - docker-compose.yml (full — verify current state; Plan 02 removed the RAPIDAPI_KEY line from n8n block)
    - Dockerfile (full — 47 lines; only last two lines change)
    - .planning/phases/06-architecture-cleanup-storage-foundation/06-RESEARCH.md (lines 259-302 — Pattern 3, exact compose yaml)
    - .planning/phases/06-architecture-cleanup-storage-foundation/06-PATTERNS.md (lines 183-332 — compose + Dockerfile + worker.py patterns)
  </read_first>
  <action>
STEP 1 — Rewrite docker-compose.yml completely.

The entire file must be replaced with the two-service structure. Use the exact YAML from the interfaces section above. Add a header comment block explaining the new architecture:

```yaml
# ── BuscadorDeEmpleo v2.0 — Docker Compose ───────────────────────────────────
#
# Dos servicios desde una sola imagen:
#   ui     — Streamlit en http://localhost:8501
#   worker — APScheduler (BlockingScheduler) en proceso separado
#
# Uso:
#   docker compose up --build        # primera vez (build + arranque)
#   docker compose up -d             # arranque en background
#   docker compose down              # parar contenedores
#   docker compose restart worker    # reiniciar solo el worker
#
# SEGURIDAD: ninguna clave se hardcodea en este fichero.
# Secretos (RAPIDAPI_KEY, OPENAI_API_KEY, TELEGRAM_*, SMTP_*) viven en .env
# (gitignoreado) e inyectados en runtime vía env_file.
# .env NO se copia en la imagen (ver .dockerignore).
```

Then the services and volumes blocks as shown in the interfaces section.

STEP 2 — Update Dockerfile.

Read the current Dockerfile. Make two changes:
a) Remove or comment out the `EXPOSE 8000` line (line 29)
b) Replace the CMD line (line 44) from:
   `CMD ["uv", "run", "uvicorn", "app.api.main:app", "--host", "0.0.0.0", "--port", "8000"]`
   With:
   `CMD ["uv", "run", "python", "-c", "print('Use: docker compose up')"]`

Also add a COPY line for worker.py after the `COPY app/ ./app/` line:
```dockerfile
COPY worker.py ./
```

And create the ui/ directory placeholder in the image:
```dockerfile
RUN mkdir -p ui
```

(These additions ensure the worker and ui paths exist in the image before compose command overrides run.)

STEP 3 — Create worker.py at repo root.

Use the exact code from the interfaces section above verbatim. This is critical: the file must be importable and the `if __name__ == "__main__":` block must use `BlockingScheduler` with `ZoneInfo("Europe/Madrid")` — NOT pytz.

STEP 4 — Create ui/ placeholder directory.

```bash
mkdir -p /Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo/ui
touch /Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo/ui/.gitkeep
```

The actual Streamlit app (ui/app.py) is created in Phase 10. This placeholder ensures the directory exists for the Docker COPY step and the compose `command:` reference.

ANTI-PATTERN to avoid: Do NOT put APScheduler inside Streamlit. worker.py is a standalone process — it must only be run via `python worker.py` (or the Docker `command:` override), never imported from UI code.
  </action>
  <verify>
    <automated>grep -c "service: ui\|  ui:\|  worker:" /Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo/docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v "^#" docker-compose.yml | grep -c "  ui:"` returns 1
    - `grep -v "^#" docker-compose.yml | grep -c "  worker:"` returns 1
    - `grep -v "^#" docker-compose.yml | grep "  n8n:\|  buscadordeempleo:"` returns nothing (old services removed)
    - `grep -v "^#" docker-compose.yml | grep "n8n_data"` returns nothing (volume removed)
    - `grep -c "dfae5ddf3fmshc64763c72ab92c1p17c53fjsnee4d98fb1148" docker-compose.yml` returns 0 (key still absent from Plan 02)
    - `test -f worker.py` exits 0
    - `grep -c "BlockingScheduler" worker.py` returns 1
    - `grep -c "ZoneInfo" worker.py` returns 1
    - `uv run python -c "import worker; print('OK')"` exits 0 (worker.py is importable)
    - `grep -c "uvicorn\|app.api.main" Dockerfile` returns 0 (old CMD removed)
    - `test -d ui` exits 0
    - `uv run pytest tests/test_no_secrets.py -q` exits 0 (secret test still GREEN)
  </acceptance_criteria>
  <done>docker-compose.yml has ui+worker services; worker.py exists and is importable; Dockerfile CMD updated; ui/ placeholder created</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite .env.example for v2.0</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (full — already in context; 22 lines to replace)
    - .planning/phases/06-architecture-cleanup-storage-foundation/06-PATTERNS.md (lines 240-277 — exact v2.0 .env.example content)
  </read_first>
  <action>
Replace the entire content of .env.example with the v2.0 variable documentation. The new file must:
- Document RAPIDAPI_KEY (required for worker JSearch calls)
- Document OPENAI_API_KEY as OPTIONAL (scoring works without it in v2.0)
- Document TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID (Phase 9 notifications)
- Document SMTP_* vars (Phase 9 email fallback)
- Keep LANGFUSE_* (optional observability)
- Remove API_KEY and any FastAPI authentication variables entirely
- Keep all secrets commented out (prefixed with #) — the file is an example, not a live config

Full content to write:

```
# BuscadorDeEmpleo v2.0 — configuración de entorno.
# cp .env.example .env    y rellena los valores.
# NUNCA commitees el fichero .env.

# ── LLM (opcional en v2.0) ───────────────────────────────────────────────────
# Scoring funciona 100% local (BGE-M3 + reglas) sin esta clave.
# Si se define: añade reasons_for/reasons_against y matched/missing skills.
# OPENAI_API_KEY=sk-proj-...

# Modelos opcionales (defaults en código):
# OPENAI_MODEL_CV=gpt-4o-mini
# OPENAI_MODEL_SCORING=gpt-4o

# ── Persistencia ─────────────────────────────────────────────────────────────
# SQLite local. Ruta opcional — default: data/jobs.db (relativo al CWD).
# SQLITE_DB_PATH=data/jobs.db

# ── Fuentes de empleo ────────────────────────────────────────────────────────
# JSearch via RapidAPI. Requerida para que el worker busque ofertas.
# IMPORTANTE: rotar la clave anterior en https://rapidapi.com si la clave
# anterior estuvo expuesta. Ver STATE.md blocker MIG-03.
# RAPIDAPI_KEY=

# ── Notificaciones Telegram (opcional — Fase 9) ───────────────────────────────
# Si se definen ambas vars, el digest de nuevas ofertas se envía por Telegram.
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=

# ── Notificaciones email SMTP (opcional — Fase 9) ────────────────────────────
# Fallback si no hay Telegram. Ambas vars (HOST + credenciales) requeridas.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=

# ── Observabilidad Langfuse (opcional) ───────────────────────────────────────
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...
```

Key verification: the file must contain `RAPIDAPI_KEY` and must NOT contain `API_KEY=` as an uncommented active line (the old FastAPI auth variable).
  </action>
  <verify>
    <automated>grep -c "RAPIDAPI_KEY" /Users/jaimeberdejosanchez/projects/BuscadorDeEmpleo/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RAPIDAPI_KEY" .env.example` returns at least 1 (variable documented)
    - `grep -c "TELEGRAM_BOT_TOKEN" .env.example` returns at least 1
    - `grep -c "SMTP_HOST" .env.example` returns at least 1
    - `grep -v "^#" .env.example | grep -c "^API_KEY="` returns 0 (no active FastAPI auth var)
    - `grep -v "^#" .env.example | grep -c "^fastapi\|^uvicorn"` returns 0 (no FastAPI vars)
    - All values are commented out (grep for active uncommented secrets): `grep -vE "^#|^$" .env.example` returns 0 lines (file is all comments + blank lines)
  </acceptance_criteria>
  <done>.env.example documents all v2.0 env vars; no FastAPI variables; all values commented out (example only)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker-compose.yml → container env | env vars injected via env_file from gitignored .env; no literals in compose |
| worker.py → APScheduler | BlockingScheduler is isolated to worker process; never imported from Streamlit UI |
| .env.example → repository | All values commented out; file is documentation only, not a live config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-05-01 | Information Disclosure | .env.example containing real secret values | mitigate | All variable values in .env.example are commented-out placeholders; acceptance criterion verifies no active uncommented lines with values |
| T-6-05-02 | Information Disclosure | RAPIDAPI_KEY in docker-compose.yml (regression) | mitigate | Plan 02 removed the literal; acceptance criterion in this plan re-verifies `grep -c "dfae..."` returns 0; test_no_secrets.py is still run |
| T-6-05-03 | Denial of Service | worker.py imported inside Streamlit (BlockingScheduler blocks UI thread) | mitigate | worker.py uses `if __name__ == "__main__":` guard — the scheduler.start() only runs when the file is executed directly, not on import; documented in module docstring |
| T-6-05-04 | Tampering | APScheduler v4 imported instead of 3.x | mitigate | pyproject.toml pins `apscheduler>=3.11.2,<4.0` (done in Plan 03); worker.py imports `from apscheduler.schedulers.blocking import BlockingScheduler` which does not exist in v4 — would fail fast at startup |
</threat_model>

<verification>
```bash
# Two services, correct image
grep -v "^#" docker-compose.yml | grep -E "  ui:|  worker:"
docker compose config --services
# must output exactly: ui\nworker

# No n8n remnants
grep -v "^#" docker-compose.yml | grep "n8n" && echo "FAIL: n8n found" || echo "OK"

# worker.py importable
uv run python -c "import worker; print('worker import OK')"

# worker.py APScheduler + ZoneInfo
grep "BlockingScheduler\|ZoneInfo" worker.py

# Dockerfile CMD clean
grep -c "uvicorn\|app.api" Dockerfile

# .env.example has v2.0 vars
grep "RAPIDAPI_KEY\|TELEGRAM_BOT_TOKEN\|SMTP_HOST" .env.example

# No active secret values in .env.example
grep -vE "^#|^$" .env.example

# Full test suite
uv run pytest tests/ -q

# Secret test still GREEN
uv run pytest tests/test_no_secrets.py -q
```
</verification>

<success_criteria>
- `docker compose config --services` outputs exactly `ui` and `worker` (no n8n, no buscadordeempleo)
- Both services in docker-compose.yml use `image: buscadordeempleo:latest`
- Both services mount `data:/app/data` and `hf_cache:/app/.cache/huggingface`
- worker.py exists, is importable without error, and contains BlockingScheduler with ZoneInfo("Europe/Madrid")
- Dockerfile CMD does not reference uvicorn or app.api.main
- .env.example contains RAPIDAPI_KEY, TELEGRAM_BOT_TOKEN, SMTP_HOST, does not contain API_KEY= as an active line
- uv run pytest tests/ -q exits 0 (full suite green)
- Phase 6 success criteria 1-5 from ROADMAP.md are all verifiable
</success_criteria>

<output>
After completion, create `.planning/phases/06-architecture-cleanup-storage-foundation/06-05-SUMMARY.md`
</output>
