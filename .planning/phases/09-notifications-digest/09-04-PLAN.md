---
phase: 09-notifications-digest
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
autonomous: true
requirements: [NOTIF-02]
must_haves:
  truths:
    - "python-telegram-bot is no longer a declared dependency in pyproject.toml"
    - "uv.lock no longer pins python-telegram-bot (or its now-orphaned transitive deps)"
    - "The project still imports and the full test suite still collects with PTB gone"
  artifacts:
    - path: "pyproject.toml"
      provides: "dependency list without python-telegram-bot"
      contains: "httpx"
  key_links:
    - from: "pyproject.toml [project] dependencies"
      to: "uv.lock"
      via: "uv sync regenerates the lock without PTB"
      pattern: "python-telegram-bot"
---

<objective>
Remove the now-unused `python-telegram-bot>=22.7` dependency from `pyproject.toml`
and regenerate `uv.lock`. Phase 9 sends Telegram via raw `httpx.Client` (RESEARCH
confirmed PTB v22.7 is async-first and wrong for the sync BlockingScheduler context),
so PTB is dead weight and a misleading dependency that implies async Telegram usage.

Purpose: honest dependencies. NOTIF-02 is implemented with httpx + stdlib only; keeping
PTB (which pulls ~486 modules incl. asyncio on import) in the manifest misrepresents the
architecture. RESEARCH §"Removal Recommended" + Open Question 1 mandate this removal.

Output: modified `pyproject.toml` (PTB line gone) + regenerated `uv.lock`. The project
still imports cleanly and the full suite still collects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-notifications-digest/09-RESEARCH.md

<interfaces>
Current pyproject.toml [project] dependencies (line 13 is the target):
```toml
dependencies = [
    "apscheduler>=3.11.2,<4.0",
    "httpx>=0.28.1",
    "instructor>=1.15.1",
    "openai>=2.38.0",
    "pydantic>=2.13.4",
    "pymupdf>=1.27.2.3",
    "python-dotenv>=1.2.2",
    "python-telegram-bot>=22.7",   # <-- REMOVE this line
    "pyyaml>=6.0.3",
    "sentence-transformers>=5.5.1",
    "streamlit>=1.57.0",
]
```
RESEARCH verified: no source file imports `telegram` after Phase 9 (Telegram is raw httpx).
"Bot de Telegram interactivo" is explicitly out of scope in REQUIREMENTS.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm no live PTB import, remove the dep line, regenerate uv.lock</name>
  <read_first>
    - pyproject.toml (the [project] dependencies block — line 13 python-telegram-bot)
    - .planning/phases/09-notifications-digest/09-RESEARCH.md (§"Removal Recommended"; §"Critical Tension: PTB vs Raw httpx — RESOLVED"; Open Question 1)
  </read_first>
  <files>pyproject.toml, uv.lock</files>
  <action>
1. SAFETY CHECK first — confirm no source module imports the `telegram` package (only the
   removed dep should reference it). Run:
   `grep -rn "import telegram\b\|from telegram\b\|python-telegram-bot" app/ worker.py 2>/dev/null`
   Expected: NO matches in app/ or worker.py. (telegram.py under app/notifications/ is OUR
   module — it imports `httpx`, not the `telegram` package. Confirm by reading its imports if
   it exists.) If any real `import telegram` from the PTB package is found in live code, STOP
   and report — do not remove the dep.

2. Edit `pyproject.toml`: delete the single line `    "python-telegram-bot>=22.7",` from the
   `[project] dependencies` array. Leave all other deps untouched (httpx, apscheduler,
   streamlit, etc. all stay).

3. Regenerate the lockfile to drop PTB and its now-orphaned transitive deps:
   `uv sync` (or `uv lock` if the environment forbids installing). This rewrites `uv.lock`.
   If `uv` is unavailable in the sandbox, run `uv lock --no-install` / report that the lock
   could not be regenerated and leave a note in the SUMMARY — but the pyproject.toml edit
   MUST still land.

4. Verify the project still imports: `python -c "import app.pipeline; import app.notifications"`
   must succeed (notifications uses httpx, not PTB).
  </action>
  <verify>
    <automated>grep -c 'python-telegram-bot' pyproject.toml | awk '{ if ($1 == 0) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'python-telegram-bot' pyproject.toml` returns 0
    - `grep -c 'httpx' pyproject.toml` returns >= 1 (httpx still present)
    - `grep -c 'apscheduler' pyproject.toml` returns >= 1 (apscheduler untouched)
    - `grep -rn 'import telegram\b\|from telegram\b' app/ worker.py` returns nothing (no live PTB import)
    - `grep -c 'python-telegram-bot' uv.lock` returns 0 after `uv sync`/`uv lock` (if uv ran; if uv unavailable, SUMMARY documents the deferred lock regen)
    - `python -c "import app.pipeline; import app.notifications"` exits 0
    - `python -m pytest --collect-only -q` exits 0 (suite still collects)
  </acceptance_criteria>
  <done>python-telegram-bot removed from pyproject.toml; uv.lock regenerated (or regen documented if uv unavailable); project imports + suite collects with PTB gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependency manifest → installed env | removing a dep must not silently break a live import path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Denial of Service (broken build) | pyproject.toml dependency removal | mitigate | Pre-removal grep confirms no live `import telegram`; post-removal `python -c "import app.pipeline; import app.notifications"` + `pytest --collect-only` prove the project still loads |
</threat_model>

<verification>
- `grep -c 'python-telegram-bot' pyproject.toml` returns 0
- `python -c "import app.pipeline; import app.notifications"` exits 0
- `python -m pytest --collect-only -q` exits 0
</verification>

<success_criteria>
- PTB removed from pyproject.toml and uv.lock (NOTIF-02 implemented with httpx only)
- No live `import telegram` from the PTB package anywhere in app/ or worker.py
- Project still imports; full suite still collects
</success_criteria>

<output>
After completion, create `.planning/phases/09-notifications-digest/09-04-SUMMARY.md`
</output>
