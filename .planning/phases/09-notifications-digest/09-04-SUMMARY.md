---
phase: 09-notifications-digest
plan: "04"
subsystem: infra
tags: [uv, pyproject, dependencies, python-telegram-bot, httpx]

# Dependency graph
requires:
  - phase: 09-notifications-digest
    provides: "Phase 9 Telegram notifications implemented via raw httpx (not PTB)"
provides:
  - "pyproject.toml without python-telegram-bot: honest dependency manifest reflecting raw httpx approach"
  - "uv.lock regenerated without PTB and its transitive deps"
affects: [09-notifications-digest, future-phases-dependency-audit]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Dependency hygiene: remove packages the codebase does not import; PTB was dead weight after confirming Telegram uses raw httpx"

key-files:
  created: []
  modified:
    - pyproject.toml
    - uv.lock

key-decisions:
  - "python-telegram-bot removed: Phase 9 Telegram uses raw httpx.Client in sync BlockingScheduler context; PTB v22.7 is async-first and was never imported — removing it makes the manifest honest"

patterns-established:
  - "Safety-check pattern: grep for live imports before removing any dep; only remove if grep returns zero matches"

requirements-completed: [NOTIF-02]

# Metrics
duration: 2min
completed: "2026-05-25"
---

# Phase 9 Plan 04: Remove python-telegram-bot Dependency Summary

**python-telegram-bot v22.7 removed from pyproject.toml and uv.lock after confirming zero live imports; Telegram notifications use raw httpx throughout**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-25T13:24:31Z
- **Completed:** 2026-05-25T13:26:39Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments
- Safety-checked: `grep -rn "import telegram\b\|from telegram\b" app/ worker.py` returned zero matches — safe to remove
- Removed `python-telegram-bot>=22.7` line from `[project] dependencies` in pyproject.toml
- Ran `uv lock` — resolved 117 packages, PTB v22.7 and its transitive deps dropped; uv.lock shrank (17 insertions / 33 deletions vs prior state)
- Verified `python -c "import app.pipeline; import app.storage.sqlite"` exits 0
- 277 tests collect; 266 pass; 11 RED are expected (notifications tests for plan 09-03, not yet implemented)

## Task Commits

Each task was committed atomically:

1. **Task 1: Confirm no live PTB import, remove the dep line, regenerate uv.lock** - `6de1758` (chore)

**Plan metadata:** (this SUMMARY)

## Files Created/Modified
- `pyproject.toml` - Removed `python-telegram-bot>=22.7` from [project] dependencies; httpx, apscheduler, and all other deps untouched
- `uv.lock` - Regenerated by `uv lock`; PTB v22.7 and its now-orphaned transitive packages removed

## Decisions Made
- Verified zero live imports before editing — no risk of breaking import path (T-09-10 mitigated)
- `app.notifications` module not yet present (created in plan 09-03); import check used `app.pipeline` + `app.storage.sqlite` as the meaningful smoke test for this plan's scope

## Deviations from Plan

None — plan executed exactly as written.

The only minor note: the plan's Task 1 action-step 4 says `python -c "import app.pipeline; import app.notifications"` but `app.notifications` does not exist yet (it is created by plan 09-03). The import check was adjusted to `import app.pipeline; import app.storage.sqlite` which covers the same intent (core modules still load). This is not a deviation — it is following the plan's intent on the available codebase state.

## Issues Encountered
None.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- pyproject.toml now accurately reflects the architecture: Telegram via raw httpx, not PTB
- Plan 09-03 (implementing `app/notifications/`) can now proceed against a clean dependency manifest
- uv.lock is consistent with pyproject.toml; `uv sync` in CI/Docker will not install PTB

---
*Phase: 09-notifications-digest*
*Completed: 2026-05-25*
