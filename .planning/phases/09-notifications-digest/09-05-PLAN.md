---
phase: 09-notifications-digest
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - app/pipeline.py
  - tests/notifications/test_notifications.py
  - tests/test_pipeline.py
autonomous: true
requirements: [NOTIF-01, NOTIF-02, NOTIF-03]
must_haves:
  truths:
    - "run_pipeline calls send_digest as a final step via deferred import, after fetch/score/persist"
    - "record_run is called AFTER send_digest and records the digest channel + notified count (D-14)"
    - "A send_digest failure NEVER aborts the pipeline or crashes the worker — it is caught and collected into result.errors (D-13)"
    - "import app.pipeline still pulls in no streamlit and no apscheduler (SC5 preserved)"
    - "End-to-end: telegram-only env delivers; smtp-only delivers; neither → silent; failure leaves seen=0 and reappears next run; success marks seen=1 and is absent next run across a restart; a multi-chunk partial failure marks only the delivered chunk (D-11)"
  artifacts:
    - path: "app/pipeline.py"
      provides: "step 11 send_digest wiring + record_run with channel/notified"
      contains: "from app.notifications import send_digest"
    - path: "tests/test_pipeline.py"
      provides: "pipeline-level tests: notify-failure-isolation, import-cleanliness still holds, record_run gets channel/notified"
      contains: "send_digest"
  key_links:
    - from: "app/pipeline.py run_pipeline"
      to: "app.notifications.send_digest"
      via: "deferred import in step 11 wrapped in try/except"
      pattern: "from app.notifications import send_digest"
    - from: "app/pipeline.py run_pipeline"
      to: "storage.record_run"
      via: "called after send_digest with channel=digest_result.channel, notified=digest_result.delivered"
      pattern: "notified="
---

<objective>
Wire the digest into the shared pipeline and prove the four phase success criteria
end-to-end. Add step 11 to `run_pipeline()`: a deferred import of `send_digest`, wrapped
in try/except so a notification failure never aborts the run or crashes the worker (D-13).
Move `record_run` to AFTER `send_digest` so the run row captures the digest `channel` +
`notified` count in a single INSERT (D-14, RESEARCH Open Question 3). Then add the
end-to-end pipeline + SC tests.

Wave 3 (depends_on [01, 02, 03]): this plan wires + tests everything above it. Phase 9
wave order: Wave 0 = 09-01 (RED tests); Wave 1 = 09-02 (storage query/migration) + 09-04
(pyproject PTB removal, disjoint files); Wave 2 = 09-03 (notifications pkg, needs 09-02's
get_undelivered_qualifying); Wave 3 = this plan (pipeline wiring + full-suite SC gate,
needs all of 01/02/03).

Purpose: SC1-SC4 plus SC5 import-cleanliness. The wiring point is the single place both the
worker and the future Phase 10 "Run now" button get notifications for free. The end-to-end
tests are the acceptance gates: telegram-only → telegram, smtp-only → email, neither →
silent, failure → job stays seen=0 and reappears, success → seen=1 and absent next run even
after a simulated worker restart.

Output: modified `app/pipeline.py` + new pipeline-level tests in tests/test_pipeline.py +
the end-to-end SC tests appended to tests/notifications/test_notifications.py. Full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-notifications-digest/09-CONTEXT.md
@.planning/phases/09-notifications-digest/09-RESEARCH.md
@.planning/phases/09-notifications-digest/09-PATTERNS.md

<interfaces>
<!-- The wiring contract. send_digest + DigestResult delivered by plan 09-03; record_run extended by plan 09-02. -->

From app/notifications/__init__.py (plan 09-03):
```python
@dataclass
class DigestResult:
    channel: str | None = None
    delivered: int = 0
    errors: list[str] = field(default_factory=list)

def send_digest(storage, settings: dict[str, str]) -> DigestResult: ...
```

From app/storage/sqlite.py record_run (extended by plan 09-02):
```python
def record_run(self, *, started_at, finished_at, fetched, deduped, scored, new_seen,
               errors: list[str] | None = None,
               channel: str | None = None, notified: int = 0) -> None: ...
```

Current run_pipeline structure (app/pipeline.py) — the relevant tail:
```python
# 9. Upsert scored jobs (lines 235-240) → stores seen=0
# 10. Record run metrics (lines 242-254):
finished_at = _now_iso()
storage.record_run(started_at=started_at, finished_at=finished_at,
                   fetched=result.fetched, deduped=result.deduped,
                   scored=result.scored, new_seen=result.new_seen,
                   errors=result.errors if result.errors else None)
# 11. Structured log summary (lines 256-266)
return result
```
`settings` is available in scope (line 167, the get_settings() result with the
date_posted_override key merged in). `started_at` is set at line 176.

Pipeline import-cleanliness invariant (tests/test_pipeline.py test_pipeline_no_streamlit_apscheduler):
importing app.pipeline must not pull streamlit/apscheduler. send_digest is import-clean (D-02,
verified in plan 09-03), and it is imported LAZILY inside run_pipeline, so this holds.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add step 11 send_digest wiring; move record_run after it with channel/notified</name>
  <read_first>
    - app/pipeline.py (steps 9-11, lines 235-267 — the region to edit; existing deferred-import pattern lines 148-162; settings in scope line 167; started_at line 176)
    - app/notifications/__init__.py (send_digest + DigestResult — the contract, from plan 09-03)
    - app/storage/sqlite.py (extended record_run signature, from plan 09-02)
    - .planning/phases/09-notifications-digest/09-PATTERNS.md (§"app/pipeline.py — add step 11" — exact wrapper + the "defer record_run until after step 11" note)
    - .planning/phases/09-notifications-digest/09-RESEARCH.md (D-12/D-13 send_digest deferred import code example; Open Question 3 record_run-after-digest recommendation)
  </read_first>
  <files>app/pipeline.py</files>
  <behavior>
    - run_pipeline computes a digest_result by calling send_digest(storage, settings) inside a try/except
    - The send_digest call is a deferred import (`from app.notifications import send_digest  # noqa: PLC0415`)
    - If send_digest raises, the exception is caught, logged, appended to result.errors, and the pipeline still returns normally
    - record_run is called AFTER send_digest and receives channel=digest_result.channel and notified=digest_result.delivered (defaulting to None/0 if send_digest threw before producing a result)
    - importing app.pipeline still does not import streamlit or apscheduler
  </behavior>
  <action>
Edit `app/pipeline.py`. Replace the current step-10 (record_run) so the order becomes:
upsert (step 9) → send_digest (NEW step 10) → record_run (step 11, now AFTER digest) → log.

Specifically, between the upsert block (ends ~line 240, `result.new_seen = len(scored_list)`)
and the existing `record_run` call, insert the digest step, then have `record_run` consume the
digest result:

```python
    # ------------------------------------------------------------------
    # 10. Send digest notification (deferred import — keeps pipeline import-clean, D-12)
    #     Failures here NEVER abort the run or crash the worker (D-13).
    # ------------------------------------------------------------------
    digest_channel: str | None = None
    digest_notified = 0
    try:
        from app.notifications import send_digest  # noqa: PLC0415

        digest_result = send_digest(storage, settings)
        digest_channel = digest_result.channel
        digest_notified = digest_result.delivered
        if digest_result.errors:
            result.errors.extend(digest_result.errors)
    except Exception as exc:  # noqa: BLE001
        logger.error("send_digest failed: %s", exc)
        result.errors.append(f"notify: {exc}")

    # ------------------------------------------------------------------
    # 11. Record run metrics (AFTER digest so channel + notified land in one INSERT, D-14)
    # ------------------------------------------------------------------
    finished_at = _now_iso()
    storage.record_run(
        started_at=started_at,
        finished_at=finished_at,
        fetched=result.fetched,
        deduped=result.deduped,
        scored=result.scored,
        new_seen=result.new_seen,
        errors=result.errors if result.errors else None,
        channel=digest_channel,
        notified=digest_notified,
    )
```

Remove the OLD step-10 record_run call (the one without channel/notified) so record_run is
called exactly once. Renumber the trailing "Structured log summary" comment to step 12 (or
leave the log block as-is; the comment number is cosmetic). Keep `started_at` where it is
(set before fetch). Do NOT move the `finished_at = _now_iso()` line above send_digest — the
run is "finished" after notification.

CRITICAL: `settings` must be in scope at the send_digest call (it is — assigned line 167/169).
The notification_min_score / notification_channel keys are part of get_settings()'s result.
  </action>
  <verify>
    <automated>python -m pytest tests/test_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/pipeline.py` contains `from app.notifications import send_digest` with `# noqa: PLC0415` on the same line
    - `grep -c 'send_digest' app/pipeline.py` returns >= 1
    - `grep -c 'storage.record_run' app/pipeline.py` returns exactly 1 (record_run called once, after digest)
    - `app/pipeline.py` record_run call contains `channel=digest_channel` and `notified=digest_notified`
    - The send_digest call is wrapped: `grep -c 'notify:' app/pipeline.py` returns >= 1 (the error-append path)
    - `python -m pytest tests/test_pipeline.py -q` exits 0 (existing pipeline tests stay green, including test_pipeline_no_streamlit_apscheduler)
    - `python -c "import sys; [sys.modules.pop(m) for m in list(sys.modules) if 'streamlit' in m or 'apscheduler' in m]; import app.pipeline; assert 'streamlit' not in sys.modules and 'apscheduler' not in sys.modules"` exits 0
  </acceptance_criteria>
  <done>run_pipeline wires send_digest as a try/except-wrapped deferred-import step; record_run called once after digest with channel+notified; pipeline import-cleanliness preserved; pipeline tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pipeline-level digest tests (failure isolation, record_run accounting, import-cleanliness)</name>
  <read_first>
    - tests/test_pipeline.py (existing helpers _make_scored_job lines 40-69; FakeEmbedder usage lines 107; patch("app.pipeline._fetch_all") pattern; SQLiteStorage on tmp_path lines 105-110)
    - app/pipeline.py (the new step 10/11 just written)
    - app/notifications/__init__.py (send_digest — to know what to patch: patch("app.notifications.send_digest") or patch the channel resolution)
  </read_first>
  <files>tests/test_pipeline.py</files>
  <behavior>
    - A send_digest that raises does not propagate out of run_pipeline; result is returned and result.errors contains a "notify:" entry
    - After a run with a configured channel, the latest runs row has channel + notified populated
    - run_pipeline still does not import streamlit/apscheduler (already covered; assert still passes)
  </behavior>
  <action>
Append to `tests/test_pipeline.py` (do not delete existing tests):

1. `test_run_pipeline_notify_failure_does_not_abort(tmp_path)` — real SQLiteStorage on
   tmp_path + FakeEmbedder. patch("app.pipeline._fetch_all", return_value=[]) so no scoring
   happens (we only test the wiring). patch("app.notifications.send_digest",
   side_effect=RuntimeError("boom")). Call run_pipeline(storage=..., embedder=...). Assert it
   RETURNS a PipelineResult (no exception) and any("notify:" in e for e in result.errors) is
   True. Also assert a runs row was still written (storage.get_recent_runs(1) non-empty) —
   D-13: the run is recorded as successful even when notify fails.

   NOTE: patch the name `app.notifications.send_digest` (the deferred import resolves it from
   the package at call time, so patching the package attribute works).

2. `test_run_pipeline_records_channel_and_notified(tmp_path, monkeypatch)` — set up so a
   digest is actually sent: monkeypatch TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID; set
   notification_channel="telegram" via storage.set_setting. Pre-seed ONE qualifying job into
   the DB (upsert a good_fit score 80 _make_scored_job) so get_undelivered_qualifying returns
   it. patch("app.pipeline._fetch_all", return_value=[]) (no new fetch). patch
   "app.notifications.telegram.httpx.Client" so .post returns a 200 MagicMock. Call
   run_pipeline. Then storage.get_recent_runs(1)[0] → assert row["channel"] == "telegram" and
   row["notified"] == 1. (D-14 end-to-end through the pipeline.)

3. `test_run_pipeline_still_import_clean()` — re-affirm: scrub sys.modules of
   streamlit/apscheduler, `import app.pipeline`, assert neither present. (Cheap regression
   guard now that pipeline imports send_digest lazily.)

Use the existing module imports (FakeEmbedder, SQLiteStorage, schema classes,
MagicMock/patch already imported at the top of test_pipeline.py). Add `import os` if needed.
  </action>
  <verify>
    <automated>python -m pytest tests/test_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def test_run_pipeline_notify_failure_does_not_abort' tests/test_pipeline.py` returns 1
    - `grep -c 'def test_run_pipeline_records_channel_and_notified' tests/test_pipeline.py` returns 1
    - `tests/test_pipeline.py` contains the literal `notify:` (asserting the error-collection path)
    - `python -m pytest tests/test_pipeline.py -q` exits 0 (all pipeline tests green)
  </acceptance_criteria>
  <done>Pipeline-level digest tests added and green: notify failure isolated + run still recorded (D-13); runs row captures channel+notified (D-14); import-cleanliness regression guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add end-to-end SC tests (SC1-SC4) to test_notifications.py + finalize VALIDATION map</name>
  <read_first>
    - tests/notifications/test_notifications.py (existing 13 tests + helpers from plan 09-01 — reuse _make_scored_job / _make_200_response / _make_error_response; note test_telegram_partial_chunk_failure_marks_only_delivered already covers the D-11 multi-chunk path)
    - app/notifications/__init__.py (send_digest — the SUT)
    - app/storage/sqlite.py (get_undelivered_qualifying, mark_seen — for restart assertions)
    - .planning/phases/09-notifications-digest/09-VALIDATION.md (Per-Task Verification Map to populate)
    - .planning/phases/09-notifications-digest/09-CONTEXT.md (§"Specific Ideas" — the 7 scenarios + chunking test; D-11 per-chunk atomicity)
  </read_first>
  <files>tests/notifications/test_notifications.py, .planning/phases/09-notifications-digest/09-VALIDATION.md</files>
  <behavior>
    - SC1: telegram env set + channel "telegram"/"auto" → message sent containing required fields for each qualifying offer
    - SC2: only SMTP env → email sent; neither → no error + log + nothing marked seen
    - SC3: send failure → job stays seen=0 → next get_undelivered_qualifying still returns it
    - SC4: send success → seen=1 → absent from next digest even after a NEW storage instance on the same db file (restart)
    - D-11 multi-chunk partial-failure is already locked by test_telegram_partial_chunk_failure_marks_only_delivered (09-01); the SC suite below + the full-suite gate exercise it as part of `pytest tests/notifications/`
  </behavior>
  <action>
1. Append end-to-end SC tests to `tests/notifications/test_notifications.py` (these complement
   the unit tests from plan 09-01 by exercising the FULL send_digest path with a real DB and a
   simulated restart). Some overlap with the 13 unit tests is fine; these are explicitly the
   SC acceptance gates:

   - `test_sc1_telegram_message_contains_required_fields(tmp_path, monkeypatch)` — telegram
     env + real storage + 1 qualifying job. Patch httpx.Client; capture the `json=` payload
     passed to .post (instance.post.call_args). Assert the sent `text` contains the job title,
     company, str(score_total), recommendation value, ≥1 reason_for, and the url. (SC1/NOTIF-01)

   - `test_sc2_email_path_when_only_smtp(tmp_path, monkeypatch)` — only SMTP env (delenv
     telegram). channel "auto". Patch smtplib.SMTP. send_digest → channel=="email",
     mock_smtp.send_message.called True. (SC2)

   - `test_sc2_neither_channel_silent_no_mark(tmp_path, monkeypatch)` — delenv all telegram +
     smtp. 1 qualifying job. send_digest → channel is None, delivered 0, job still seen=0
     (assert via get_undelivered_qualifying still returns it). No exception. (SC2)

   - `test_sc3_failure_keeps_job_for_next_run(tmp_path, monkeypatch)` — telegram env, .post →
     _make_error_response(400). send_digest → errors non-empty, delivered 0. Call
     get_undelivered_qualifying again → job still present. Then a SECOND send_digest with
     .post → 200 delivers it (proves it was retryable). (SC3)

   - `test_sc4_success_absent_after_restart(tmp_path, monkeypatch)` — telegram env, .post →
     200. send_digest → delivered 1, seen=1. Create a NEW SQLiteStorage(same db path) +
     init_db (simulated worker restart). get_undelivered_qualifying(70, [...]) on the new
     instance → [] (job never re-appears). (SC4)

   Reuse the helper factories from plan 09-01. Use monkeypatch.delenv(..., raising=False) to
   guarantee clean env per test. The D-11 multi-chunk partial-failure scenario is already a
   dedicated test from 09-01 (test_telegram_partial_chunk_failure_marks_only_delivered) — do
   NOT duplicate it; the full-suite gate below runs it.

2. Populate the Per-Task Verification Map table in
   `.planning/phases/09-notifications-digest/09-VALIDATION.md` (replace the "_to be filled by
   planner_" row). Add one row per Phase 9 task across plans 01-05 with columns: Task ID
   (e.g. 09-01-T1, 09-02-T1, 09-03-T1...09-05-T3), Plan, Wave (use the corrected waves:
   09-01=0, 09-02=1, 09-04=1, 09-03=2, 09-05=3), Requirement (NOTIF-01/02/03 / D-XX), Threat
   Ref (T-09-04/06/07/08 where relevant), Secure Behavior (1-line), Test Type (unit / e2e),
   Automated Command (the `python -m pytest ...` from each task's verify), File Exists (✅),
   Status (✅ green once the task passes). Include a row for the D-11 guard
   (test_telegram_partial_chunk_failure_marks_only_delivered, 09-01-T1, D-11). Set frontmatter
   `nyquist_compliant: true` and `wave_0_complete: true` and check the Validation Sign-Off
   boxes that now hold.
  </action>
  <verify>
    <automated>python -m pytest tests/notifications/ tests/storage/ tests/test_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def test_sc1_\|def test_sc2_\|def test_sc3_\|def test_sc4_' tests/notifications/test_notifications.py` returns >= 5
    - `tests/notifications/test_notifications.py` references `call_args` (SC1 payload capture)
    - `tests/notifications/test_notifications.py` still contains `def test_telegram_partial_chunk_failure_marks_only_delivered` (the D-11 guard from 09-01 is preserved)
    - `python -m pytest tests/notifications/ tests/storage/ tests/test_pipeline.py -q` exits 0
    - `.planning/phases/09-notifications-digest/09-VALIDATION.md` no longer contains the literal `_to be filled by planner_`
    - `.planning/phases/09-notifications-digest/09-VALIDATION.md` frontmatter has `nyquist_compliant: true`
    - FULL suite green: `python -m pytest -q` exits 0
  </acceptance_criteria>
  <done>SC1-SC4 end-to-end tests added and green (incl. simulated-restart SC4 and the preserved D-11 multi-chunk partial-failure guard); VALIDATION Per-Task Map populated with corrected waves; nyquist_compliant set true; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → notifications | run_pipeline invokes send_digest; a failure there must not crash the worker process |
| run metadata → runs table | channel + notified counts persisted; channel is a non-secret enum string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Denial of Service (worker crash) | run_pipeline step 10 | mitigate | send_digest is wrapped in try/except; exceptions are logged + collected into result.errors; run_pipeline returns normally and record_run still runs (D-13). Test test_run_pipeline_notify_failure_does_not_abort proves it |
| T-09-12 | Information Disclosure | record_run channel/notified | mitigate | Only the channel NAME (telegram/email/None) + an integer count reach the runs row and the log — no token or recipient secret (inherits T-09-03/T-09-06 from plans 02/03) |
</threat_model>

<verification>
- `python -m pytest -q` exits 0 (FULL suite green — 259 baseline + new Phase 9 tests)
- `python -c "import sys; [sys.modules.pop(m) for m in list(sys.modules) if 'streamlit' in m or 'apscheduler' in m]; import app.pipeline; assert 'streamlit' not in sys.modules and 'apscheduler' not in sys.modules"` exits 0
- `grep -c 'storage.record_run' app/pipeline.py` returns 1 (single call, after digest)
- All four SCs have a passing automated test; the D-11 multi-chunk partial-failure guard runs green in the suite
</verification>

<success_criteria>
- send_digest wired as a final pipeline step (worker + future UI both notify) (D-12)
- Notify failure never aborts the pipeline; run still recorded (D-13)
- record_run captures channel + notified after the digest (D-14)
- Pipeline import-cleanliness preserved (SC5)
- SC1-SC4 proven end-to-end, including delivery-failure-reappears and success-absent-after-restart; D-11 multi-chunk partial-failure proven by the 09-01 guard in the suite
- VALIDATION map populated; nyquist_compliant: true; full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/09-notifications-digest/09-05-SUMMARY.md`
</output>
</content>
