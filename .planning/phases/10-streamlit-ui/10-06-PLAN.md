---
phase: 10-streamlit-ui
plan: 06
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - ui/pages/settings.py
  - ui/pages/status.py
  - ui/settings_logic.py
  - tests/ui/test_settings_page.py
  - tests/ui/test_status_page.py
autonomous: true
requirements: [UI-05, UI-06, UI-09]
must_haves:
  truths:
    - "Adjusting scoring weights that do not sum to 1.0 blocks the save with a clear error (PesosScoring validator) (D-08)"
    - "Valid weights persist to score_weight_* settings; deal-breakers persist as a JSON deal_breakers setting"
    - "Secrets are never displayed — only configured booleans (Telegram/Email/OpenAI ✓/✗)"
    - "The Status page shows last run, next run (last + interval), and new-offers/notified counts from get_recent_runs (D-11)"
  artifacts:
    - path: "ui/pages/settings.py"
      provides: "UI-05 weights+threshold / UI-06 deal-breakers, with validation + boolean secret display"
      contains: "PesosScoring"
    - path: "ui/settings_logic.py"
      provides: "validate_and_pack_weights + pack_deal_breakers + channel_status (pure, testable)"
      contains: "def validate_and_pack_weights"
    - path: "ui/pages/status.py"
      provides: "UI-09 scheduler dashboard from get_recent_runs + compute_next_run"
      contains: "get_recent_runs"
  key_links:
    - from: "ui/settings_logic.py::validate_and_pack_weights"
      to: "app.models.schemas.PesosScoring"
      via: "construct to trigger sum-to-1.0 validator"
      pattern: "PesosScoring"
    - from: "ui/pages/status.py"
      to: "app.status_helpers.compute_next_run"
      via: "next-run computation"
      pattern: "compute_next_run"
    - from: "ui/pages/settings.py"
      to: "storage.set_setting (deal_breakers JSON, score_weight_*, notification_min_score)"
      via: "save button"
      pattern: "set_setting"
---

<objective>
Build the Settings page (UI-05 weights+threshold / UI-06 deal-breakers) and the Status page (UI-09
scheduler dashboard). Settings validates the PesosScoring sum-to-1.0 invariant BEFORE persisting
(blocking the save with a clear Spanish error on failure), persists deal-breakers as a JSON setting,
and displays channel configuration as booleans only (never secret values). Status reads get_recent_runs
and shows last run, computed next run (via the Plan 02 compute_next_run helper), and counts. The
validation/packing logic is extracted into pure helpers (ui/settings_logic.py) so it is unit-testable.

Purpose: Close the settings→scorer loop in the UI (these feed the Plan 02 overlay) and surface the
worker's run history. Files disjoint from Plans 04/05 → parallel.
Output: ui/pages/settings.py, ui/pages/status.py, ui/settings_logic.py, two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-streamlit-ui/10-CONTEXT.md
@.planning/phases/10-streamlit-ui/10-RESEARCH.md
@.planning/phases/10-streamlit-ui/10-UI-SPEC.md

<interfaces>
<!-- Contracts the executor uses directly. -->

app/models/schemas.py:
```python
class PesosScoring(BaseModel):  # puesto/skills/ubicacion/seniority floats
    # @model_validator check_sum → raises ValueError "Los pesos deben sumar 1.0, suman X" if |sum-1|>1e-6
```
from pydantic import ValidationError   # construct PesosScoring inside try/except ValidationError

app/status_helpers.py (Plan 02): `compute_next_run(runs, schedule_interval_hours) -> datetime | None`.

ui/services.py (Plan 03): get_storage() → SQLiteStorage with get_settings/set_setting/get_recent_runs.

storage.get_recent_runs(limit) -> list[dict] rows with keys: id, started_at, finished_at, fetched,
deduped, scored, new_seen, errors (list|str), channel (str|None), notified (int). Ordered id DESC
(rows[0] = latest).

Settings keys: score_weight_puesto/skills/ubicacion/seniority, notification_min_score (threshold),
deal_breakers (JSON list — seeded "[]" in Plan 02). schedule_interval_hours (read for next-run calc).

Secret env vars (display as BOOLEAN only, NEVER the value):
  TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID → "Telegram configurado: ✓/✗"
  SMTP_HOST/SMTP_USER/SMTP_PASSWORD → "Email configurado: ✓/✗"
  OPENAI_API_KEY → "OpenAI: ✓/✗"

UI-SPEC copy (exact, Spanish):
- weights error: "Los pesos deben sumar 1.0 (suman {X})."
- settings save success: "Guardado — afecta al worker y a «Re-score»."
- status empty: "El worker aún no ha ejecutado ningún run."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ui/settings_logic.py + tests (weights validation, packing, channel status)</name>
  <read_first>
    - app/models/schemas.py (PesosScoring validator + ValidationError)
    - .planning/phases/10-streamlit-ui/10-RESEARCH.md (Pitfall 3 float sum; "Weight validation in Settings page" example)
    - .planning/phases/10-streamlit-ui/10-VALIDATION.md (UI-05/D-08 test rows)
  </read_first>
  <files>ui/settings_logic.py, tests/ui/test_settings_page.py</files>
  <behavior>
    - validate_and_pack_weights returns (ok=True, mapping) for weights summing to 1.0
    - validate_and_pack_weights returns (ok=False, error_msg) for weights not summing to 1.0 (no exception leaks)
    - pack_deal_breakers turns a list[str] into a JSON string parseable back to the same list
    - channel_status reads env presence (mocked) → booleans, never the secret value
  </behavior>
  <action>
    Create `ui/settings_logic.py` (NO streamlit — pure, testable):
    ```python
    from __future__ import annotations
    import json
    import os
    from pydantic import ValidationError
    from app.models.schemas import PesosScoring

    def validate_and_pack_weights(puesto, skills, ubicacion, seniority) -> tuple[bool, dict | str]:
        try:
            PesosScoring(puesto=puesto, skills=skills, ubicacion=ubicacion, seniority=seniority)
        except ValidationError:
            total = round(puesto + skills + ubicacion + seniority, 4)
            return False, f"Los pesos deben sumar 1.0 (suman {total})."
        return True, {
            "score_weight_puesto": str(puesto), "score_weight_skills": str(skills),
            "score_weight_ubicacion": str(ubicacion), "score_weight_seniority": str(seniority),
        }

    def pack_deal_breakers(items: list[str]) -> str:
        return json.dumps([s.strip() for s in items if s.strip()])

    def channel_status() -> dict[str, bool]:
        return {
            "telegram": bool(os.getenv("TELEGRAM_BOT_TOKEN")) and bool(os.getenv("TELEGRAM_CHAT_ID")),
            "email": bool(os.getenv("SMTP_HOST")) and bool(os.getenv("SMTP_USER")) and bool(os.getenv("SMTP_PASSWORD")),
            "openai": bool(os.getenv("OPENAI_API_KEY")),
        }
    ```
    Create `tests/ui/test_settings_page.py`:
    - `test_valid_weights_pack`: validate_and_pack_weights(0.35,0.30,0.20,0.15) → (True, mapping) and mapping["score_weight_puesto"]=="0.35".
    - `test_invalid_weights_blocked`: validate_and_pack_weights(0.5,0.3,0.2,0.2) → (False, msg) and msg startswith "Los pesos deben sumar 1.0".
    - `test_float_edge_sums_to_one`: validate_and_pack_weights(0.35,0.30,0.20,0.15) is True even with IEEE-754 drift (the validator allows 1e-6 tolerance).
    - `test_pack_deal_breakers_roundtrip`: json.loads(pack_deal_breakers(["a"," b ","",  "c"])) == ["a","b","c"].
    - `test_channel_status_never_returns_secret`: monkeypatch.setenv all secrets; assert channel_status() values are all True and each value is a bool (type(v) is bool), NOT the secret string.
    - `test_channel_status_unset`: monkeypatch.delenv (raising=False) all → all False.
  </action>
  <verify>
    <automated>python -m pytest tests/ui/test_settings_page.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ui/test_settings_page.py -q` exits 0
    - `grep -q "def validate_and_pack_weights" ui/settings_logic.py` and `grep -q "def channel_status" ui/settings_logic.py`
    - `grep -Lq "import streamlit" ui/settings_logic.py` (pure)
    - channel_status returns bools only: `grep -q "bool(os.getenv" ui/settings_logic.py` and the test asserts `type(v) is bool`
  </acceptance_criteria>
  <done>Settings logic (weight validation, deal-breaker packing, boolean channel status) implemented + unit-tested; secrets never exposed as values.</done>
</task>

<task type="auto">
  <name>Task 2: ui/pages/settings.py — weights + deal-breakers + threshold + boolean secrets</name>
  <read_first>
    - ui/settings_logic.py (Task 1 helpers)
    - ui/services.py (get_storage; get_settings/set_setting)
    - .planning/phases/10-streamlit-ui/10-UI-SPEC.md (Settings section: weights validation, deal-breakers editor, threshold, secrets-as-booleans, save copy)
  </read_first>
  <files>ui/pages/settings.py</files>
  <action>
    Create `ui/pages/settings.py` (thin; delegates validation to settings_logic). Gate the render body
    behind `from streamlit.runtime import exists as _st_running` (import-safe under pytest, mirror Plan
    04/05). Page body:
    - header "Configuración".
    - `settings = get_storage().get_settings()`.
    - **Weights (UI-05):** 4 st.number_input (puesto/skills/ubicación/seniority) pre-filled from
      `float(settings.get("score_weight_*"))`, step=0.05, format="%.2f". On "Guardar pesos" →
      `ok, payload = validate_and_pack_weights(...)`; if not ok → `st.error(payload)` (the exact
      "Los pesos deben sumar 1.0 (suman X)." message); else for k,v in payload.items():
      get_storage().set_setting(k, v) → `st.success("Guardado — afecta al worker y a «Re-score».")`.
    - **Deal-breakers (UI-06):** a st.text_area "Deal-breakers (uno por línea)" pre-filled from
      json.loads(settings.get("deal_breakers","[]")) joined by newlines. On save →
      `get_storage().set_setting("deal_breakers", pack_deal_breakers(lines))` (split text_area on \n).
    - **Threshold:** st.number_input "Umbral mínimo de score" pre-filled from
      int(settings.get("notification_min_score","70")); on save → set_setting("notification_min_score", str(val)).
    - **Secrets (boolean ONLY):** `cs = channel_status()`; render
      `st.write("Telegram configurado:", "✓" if cs["telegram"] else "✗")` and likewise for email +
      OpenAI. NEVER read or display TELEGRAM_BOT_TOKEN/SMTP_PASSWORD/OPENAI_API_KEY values — only the
      booleans from channel_status().
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('ui/pages/settings.py').read())" && grep -q "validate_and_pack_weights" ui/pages/settings.py && grep -q "channel_status" ui/pages/settings.py</automated>
  </verify>
  <acceptance_criteria>
    - Valid Python: `python -c "import ast; ast.parse(open('ui/pages/settings.py').read())"` exits 0
    - Weights error copy reachable via helper: `grep -q "validate_and_pack_weights" ui/pages/settings.py`
    - Deal-breakers persisted as JSON: `grep -q "deal_breakers" ui/pages/settings.py` and `grep -q "pack_deal_breakers" ui/pages/settings.py`
    - Save success copy: `grep -q "afecta al worker y a «Re-score»" ui/pages/settings.py`
    - Secrets boolean-only: `grep -q "channel_status" ui/pages/settings.py` and NO secret env var is read directly in the page (`grep -vq "os.getenv(\"OPENAI_API_KEY\")" ui/pages/settings.py` — the page must use channel_status(), not read the secret itself)
    - Import-safe: `grep -q "streamlit.runtime" ui/pages/settings.py`
  </acceptance_criteria>
  <done>Settings page validates weights (blocks bad save), persists deal-breakers JSON + threshold, and shows channel config as booleans only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ui/pages/status.py + test (UI-09 scheduler dashboard)</name>
  <read_first>
    - app/status_helpers.py (compute_next_run — Plan 02)
    - ui/services.py (get_storage; get_recent_runs/get_settings)
    - app/storage/sqlite.py (get_recent_runs row shape)
    - .planning/phases/10-streamlit-ui/10-UI-SPEC.md (Status section: last/next run + counts + recent runs table + empty copy)
  </read_first>
  <files>ui/pages/status.py, tests/ui/test_status_page.py</files>
  <behavior>
    - With runs present, the page computes next run = last started_at + schedule_interval_hours
    - With no runs, the page shows the empty-state message and computes None next run
  </behavior>
  <action>
    Create `ui/pages/status.py` (thin). Extract a pure helper
    `summarize_status(runs: list[dict], settings: dict) -> dict` that returns
    `{"last_run": runs[0]["started_at"] if runs else None, "next_run": compute_next_run(runs,
    float(settings.get("schedule_interval_hours","6"))), "new_seen": runs[0].get("new_seen") if runs
    else 0, "notified": runs[0].get("notified") if runs else 0}` (deferred import compute_next_run from
    app.status_helpers). Page body (gated by streamlit.runtime.exists):
    - header "Estado".
    - `runs = get_storage().get_recent_runs(limit=10)`; `settings = get_storage().get_settings()`.
    - if not runs → `st.info("El worker aún no ha ejecutado ningún run.")` and stop.
    - `s = summarize_status(runs, settings)`; st.metric columns: "Último run" (s["last_run"]),
      "Próximo run" (s["next_run"]), "Nuevas ofertas" (s["new_seen"]), "Notificadas" (s["notified"]).
    - st.dataframe of the recent runs (columns: started_at, finished_at, fetched, deduped, scored,
      new_seen, channel, notified) — read-only.
    Create `tests/ui/test_status_page.py`:
    - `test_summarize_with_runs`: runs=[{"started_at":"2026-05-25T10:00:00+00:00","new_seen":3,"notified":2}],
      settings={"schedule_interval_hours":"6"} → summarize_status returns last_run that string,
      new_seen==3, notified==2, and next_run == datetime.fromisoformat("2026-05-25T10:00:00+00:00")+timedelta(hours=6).
    - `test_summarize_empty`: runs=[], settings={} → last_run is None, next_run is None, new_seen==0.
    Import summarize_status from ui.pages.status (the helper must be importable; gate the render body
    with streamlit.runtime.exists so importing under pytest does not require a Streamlit runtime).
  </action>
  <verify>
    <automated>python -m pytest tests/ui/test_status_page.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ui/test_status_page.py -q` exits 0
    - `grep -q "def summarize_status" ui/pages/status.py` and `grep -q "compute_next_run" ui/pages/status.py`
    - `grep -q "get_recent_runs" ui/pages/status.py`
    - Empty copy: `grep -q "El worker aún no ha ejecutado ningún run" ui/pages/status.py`
    - Import-safe: `grep -q "streamlit.runtime" ui/pages/status.py`
  </acceptance_criteria>
  <done>Status page shows last/next run + new-offers/notified counts (UI-09/SC6) from get_recent_runs; summarize_status unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI form → settings table | weights/deal-breakers/threshold written to the cross-process config bus |
| env secrets → page | channel configuration surfaced to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Information Disclosure | TELEGRAM_BOT_TOKEN/SMTP_PASSWORD/OPENAI_API_KEY rendered | mitigate | channel_status() returns booleans only; the page renders ✓/✗ and never reads the secret value (acceptance criterion forbids the page reading the secret directly) |
| T-10-06-02 | Tampering | invalid weights persisted → worker crashes mid-run | mitigate | validate_and_pack_weights constructs PesosScoring (runs the validator) BEFORE set_setting; bad weights block the save with a clear error (D-08) |
| T-10-06-03 | Tampering | malformed deal_breakers persisted | mitigate | pack_deal_breakers always emits a valid JSON list (strips blanks); overlay (Plan 02) also defends with try/except |
| T-10-06-04 | Input Validation | non-numeric threshold | mitigate | threshold via st.number_input (numeric widget) + str() before persisting |
</threat_model>

<verification>
- `python -m pytest tests/ui/test_settings_page.py tests/ui/test_status_page.py -q` green.
- `python -c "import ast; ast.parse(open('ui/pages/settings.py').read()); ast.parse(open('ui/pages/status.py').read())"` exits 0.
- grep gates confirm weight validation, deal-breaker JSON persistence, boolean-only secrets, next-run computation.
</verification>

<success_criteria>
- UI-05: weights validated (sum-to-1.0) before persisting; bad weights blocked with the exact error; threshold persisted.
- UI-06: deal-breakers persisted as a JSON setting (feeds the Plan 02 overlay).
- UI-09/SC6: Status shows last/next run + new-offers/notified counts from get_recent_runs.
- Secrets shown as booleans only — never rendered as values.
</success_criteria>

<output>
After completion, create `.planning/phases/10-streamlit-ui/10-06-SUMMARY.md`
</output>
