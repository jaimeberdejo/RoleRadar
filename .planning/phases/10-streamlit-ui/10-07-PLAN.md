---
phase: 10-streamlit-ui
plan: 07
type: execute
wave: 4
depends_on: [10-04, 10-05, 10-06]
files_modified:
  - README.md
  - docs/archive/n8n/README.md
autonomous: true
requirements: [DOC-04]
must_haves:
  truths:
    - "README documents the v2.0 standalone architecture: Streamlit UI + APScheduler worker, no FastAPI/n8n in the active quick-start (DOC-04, D-13)"
    - "README explains install with uv (fallback venv+pip), the .env setup, and `docker compose up` starting ui (:8501) + worker (DOC-04, D-13)"
    - "README documents configuring search (Search Config page), uploading the CV (CV page), browsing/scoring (Results), and the scheduler status — all in-app, no HTTP endpoints (DOC-04, D-13)"
    - "README explains digest delivery (Telegram or email; silent if neither configured) and the no-auth/reverse-proxy security note"
    - "The n8n integration prose is removed from the active README and pointed to docs/archive/n8n/ as legacy (DOC-04, D-13); the broken docs/N8N-WORKFLOW.md link no longer appears"
    - "README contains only placeholder env-var NAMES — no real RAPIDAPI/OpenAI/Telegram/SMTP secret values"
  artifacts:
    - path: "README.md"
      provides: "v2.0 standalone README: install, .env, docker compose up (ui+worker), configure/use the app, digest, security note"
      contains: "docker compose up"
    - path: "docs/archive/n8n/README.md"
      provides: "Legacy archive note explaining n8n is retired in v2.0 and pointing at the archived workflow doc"
      contains: "legacy"
  key_links:
    - from: "README.md (Docker section)"
      to: "docker-compose.yml ui + worker services"
      via: "documented `docker compose up` starting both services with shared data/ + hf_cache volumes"
      pattern: "docker compose up"
    - from: "README.md (n8n legacy pointer)"
      to: "docs/archive/n8n/"
      via: "link to the archived n8n workflow doc instead of the active README"
      pattern: "docs/archive/n8n"
---

<objective>
Rewrite `README.md` for the v2.0 standalone architecture (DOC-04 / D-13). v2.0 is an app-owned
fetch tool: a Streamlit multipage UI (port 8501) plus an APScheduler worker process, both launched
by `docker compose up` from a single image sharing the `data/` and `hf_cache` volumes. The current
README still describes the retired v1.0 FastAPI service ("FastAPI headless", uvicorn, `/jobs/process`,
Swagger) and carries a large "Integración con n8n" section that links to `docs/N8N-WORKFLOW.md` — a
path that no longer exists (the file was archived under `docs/archive/n8n/` by MIG-02 in Phase 6).
This plan replaces that content with v2.0 docs and archives the n8n prose.

Purpose: Close the last Phase 10 requirement (DOC-04) so the README matches the shipped v2.0 app — a
portfolio-grade entry point that a new reader (or Jaime on a fresh machine) can follow end to end.
Output: a rewritten `README.md` and a short legacy note at `docs/archive/n8n/README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-streamlit-ui/10-CONTEXT.md
@.planning/phases/10-streamlit-ui/10-RESEARCH.md
@.planning/phases/10-streamlit-ui/10-UI-SPEC.md

<facts>
<!-- Ground truth the executor must honor. Extracted from the repo on 2026-05-25. -->

DOC-04 (REQUIREMENTS.md §DOC): "README reescrito para v2.0 standalone (instalación, `.env`, arrancar
UI Streamlit + worker vía Docker, configurar búsqueda, subir CV); la sección de integración n8n se
archiva/retira."

D-13 (10-CONTEXT.md): Rewrite README for v2.0 standalone — install (uv), `.env` (RAPIDAPI_KEY,
TELEGRAM_*, SMTP_*, OPENAI optional), `docker compose up` (ui + worker), how to configure search +
upload CV + read the digest. Archive/retire the n8n section. Remove the stale "FastAPI operativo"
messaging from the old stub era.

docker-compose.yml (current, verified): two services from one image `buscadordeempleo:latest`:
  - `ui`     → command `uv run streamlit run ui/app.py --server.port 8501`, ports `8501:8501`
  - `worker` → command `uv run python worker.py` (APScheduler), no exposed port
  Both load `env_file: .env`, set `HF_HOME=/app/.cache/huggingface` and `SQLITE_DB_PATH=/app/data/jobs.db`,
  and mount the named volumes `data:/app/data` + `hf_cache:/app/.cache/huggingface`. `.env` is in
  `.dockerignore` and `.gitignore` (never copied into the image, never committed).

.env.example (current, verified) — these are the env-var NAMES to document (NEVER print real values):
  - OPENAI_API_KEY (OPTIONAL in v2.0 — scoring runs 100% local without it; adds reasons + matched/missing skills)
  - OPENAI_MODEL_CV (opt, default gpt-4o-mini), OPENAI_MODEL_SCORING (opt, default gpt-4o)
  - SQLITE_DB_PATH (opt, default data/jobs.db)
  - RAPIDAPI_KEY (JSearch via RapidAPI — required for the worker to fetch offers)
  - TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID (opt — Telegram digest; need BOTH)
  - SMTP_HOST + SMTP_PORT + SMTP_USER + SMTP_PASSWORD + SMTP_TO (opt — email digest fallback; SMTP_TO defaults to SMTP_USER)
  - LANGFUSE_PUBLIC_KEY + LANGFUSE_SECRET_KEY (opt — LLM tracing stub)

n8n archive state (verified): `docs/N8N-WORKFLOW.md` does NOT exist; the file lives at
`docs/archive/n8n/N8N-WORKFLOW.md` (plus a `docs/archive/n8n/n8n/` folder). The current README links
to the dead path `docs/N8N-WORKFLOW.md` (line ~323) and has an active "## Integración con n8n" section
(lines ~315-502). MIG-02 (Phase 6, Complete) already archived the artifacts; this plan finishes the
DOC side by removing the active prose and pointing readers to the archive.

App pages (10-UI-SPEC.md) — the in-app workflow to document (NO HTTP endpoints anymore):
  - CV (`ui/pages/cv.py`): upload PDF → parse + cache CVProfile.
  - Search Config (`ui/pages/search.py`): country, language, position(s), date_posted, employment_types, remote-only → persisted to settings; worker reads them next run (no restart).
  - Results (`ui/pages/results.py`): browse/sort/filter scored offers + full breakdown; "▶ Run now" + "↻ Re-score stored".
  - Settings (`ui/pages/settings.py`): scoring weights + deal-breakers + threshold; secrets shown as booleans only.
  - Status (`ui/pages/status.py`): scheduler dashboard from the runs table (last/next run + counts).

Digest delivery (NOTIF, Phase 9 Complete): after each run the worker sends a digest of new
strong_fit/good_fit offers via Telegram OR email (one channel); silent skip if neither configured.

Security note required in README (10-CONTEXT.md deferred + threat model): the Streamlit UI has NO
built-in auth (personal local tool). If exposed beyond localhost, put it behind a reverse proxy / auth
(or a VPN). The old `API_KEY` / `X-API-Key` FastAPI auth no longer applies (FastAPI removed in MIG-01).

Stack reality for the README "Stack" table: Python 3.11+, uv; Streamlit (UI) + APScheduler (worker);
httpx (JSearch client); Pydantic v2 + Instructor; BGE-M3 local embeddings; OpenAI OPTIONAL enrichment;
SQLite local (WAL); pytest. There is NO FastAPI/uvicorn/Swagger and NO `/jobs/*` endpoints in v2.0.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md for the v2.0 standalone Streamlit + worker app</name>
  <read_first>
    - README.md (the full current file — the v1.0 FastAPI/n8n content to replace)
    - docker-compose.yml (ui + worker commands, ports, env, volumes — quote these exactly)
    - .env.example (the env-var NAMES + which are optional vs required for the worker)
    - .planning/phases/10-streamlit-ui/10-UI-SPEC.md (the 5 pages + their purpose, to describe the in-app workflow)
    - .planning/phases/10-streamlit-ui/10-CONTEXT.md (D-13 scope + the no-auth/reverse-proxy security note)
  </read_first>
  <files>README.md</files>
  <action>
    Rewrite `README.md` end to end for v2.0 standalone. Keep Spanish (consistent with the project) and
    a portfolio-quality tone. Structure (sections in this order; adapt headings but keep the content):

    1. **Title + intro** — "Agregador inteligente de ofertas de empleo". Describe v2.0 as a STANDALONE
       app: a Streamlit UI + an always-on APScheduler worker, scoring local-first (BGE-M3 + reglas),
       con el LLM (OpenAI) como enriquecimiento OPCIONAL. REMOVE the "Servicio Python (FastAPI)
       headless" framing and the "la orquestación … la hace n8n por fuera" sentence from the intro —
       v2.0 owns its own fetch (JSearch via RapidAPI). Keep the doble-propósito (herramienta real +
       portfolio) line.

    2. **Tabla de contenidos** — regenerate to match the new sections. It must NOT contain an
       "Integración con n8n" entry and MUST contain a "Despliegue (Docker)" / "Arrancar la app" entry.

    3. **Arquitectura (breve)** — one short paragraph or bullet list: `ui` (Streamlit :8501) and
       `worker` (APScheduler) run from one image, share `data/` (SQLite + profile.yaml) and `hf_cache`
       (BGE-M3 ~2.3 GB); settings table is the cross-process config bus the UI edits and the worker reads.

    4. **Instalación** — uv primary: `git clone … && cd … && uv sync`. Add a venv+pip fallback note
       (`python -m venv .venv && source .venv/bin/activate && pip install -e .` or `pip install -r`
       — use whatever matches the repo; uv is primary). For everyday use, point to Docker (next section).

    5. **Configuración `.env`** — `cp .env.example .env`, then a table of the env vars from .env.example.
       Mark RAPIDAPI_KEY as required for the worker to fetch; OPENAI_API_KEY as OPTIONAL (scoring works
       100% local without it). Document TELEGRAM_BOT_TOKEN/TELEGRAM_CHAT_ID and SMTP_HOST/SMTP_PORT/
       SMTP_USER/SMTP_PASSWORD/SMTP_TO as optional digest channels, and LANGFUSE_* as optional tracing.
       Use ONLY placeholder names — NEVER a real key value. Add a line: "`.env` está en `.gitignore` /
       `.dockerignore` y nunca se commitea." DELETE the old `API_KEY` / `X-API-Key` row (FastAPI auth
       is gone).

    6. **Arrancar la app (Docker)** — `docker compose up --build` (first time) and `docker compose up -d`.
       Explain it starts BOTH `ui` (Streamlit en http://localhost:8501) and `worker` (APScheduler en
       background). Explain the first run downloads BGE-M3 (~2.3 GB) into the `hf_cache` volume, and that
       `data/` + `hf_cache` persist across `docker compose restart`. Keep a short "named volumes" note
       and `docker volume inspect buscadordeempleo_data` for backups. REMOVE the uvicorn/`uv run uvicorn
       app.api.main:app` run command and the Swagger `/docs` line (FastAPI removed). Optionally mention a
       non-Docker dev run (`uv run streamlit run ui/app.py --server.port 8501` for the UI and
       `uv run python worker.py` for the worker) — but Docker is the recommended path.

    7. **Usar la app** — walk the 5 pages (from 10-UI-SPEC):
       - **CV:** subir el PDF → parsea y cachea el CVProfile; recargar el mismo PDF no re-parsea.
       - **Búsqueda (Search Config):** país, idioma, puesto(s), date_posted, employment_types, remote-only
         → se guardan en settings; el worker los usa en el siguiente run sin reiniciar Docker.
       - **Resultados:** lista ordenable/filtrable (score, recomendación) + desglose completo (4 sub-scores,
         reasons_for/against, matched/missing skills); botón "▶ Run now" (búsqueda+scoring inmediata, no
         bloquea la UI) y "↻ Re-score stored" (re-puntúa la BD con los pesos actuales, sin re-fetch).
       - **Configuración (Settings):** pesos del scoring (deben sumar 1.0), deal-breakers, umbral; los
         secretos se muestran solo como booleanos (✓/✗), nunca el valor.
       - **Estado (Status):** último run / próximo run / nuevas ofertas, desde la tabla `runs`.
       Mention `data/profile.yaml` remains the hand-edited IDENTITY (ranking_puestos, datos_personales,
       ubicación) while the UI/settings table holds operational knobs.

    8. **Entrega del digest** — tras cada run el worker envía las nuevas ofertas strong_fit/good_fit por
       **Telegram O email** (un solo canal, según config); si no hay ninguno configurado, no envía nada
       (skip silencioso). Umbral configurable; solo ofertas nuevas; se marcan vistas tras la entrega.

    9. **Nota de seguridad** — La UI de Streamlit NO tiene autenticación integrada (herramienta personal
       local). Si la expones fuera de localhost, ponla detrás de un reverse proxy / auth (o VPN). No hay
       `API_KEY` ni cabecera `X-API-Key` en v2.0 (FastAPI retirado).

    10. **Stack** — table reflecting v2.0: Python 3.11+ / uv; Streamlit (UI) + APScheduler (worker);
        httpx (cliente JSearch); Pydantic v2 + Instructor; BGE-M3 local; OpenAI OPCIONAL; SQLite (WAL);
        pytest; Langfuse stub opcional. NO FastAPI/uvicorn/Swagger row.

    11. **Correr los tests** — `uv run pytest -q` (keep). Brief note on what the suite covers (CV parse,
        normalización, dedup, la heurística de scoring local-first, JSearch client, worker pipeline,
        notificaciones, UI helpers).

    12. **Legado: n8n (retirado en v2.0)** — a SHORT subsection (a few lines, NOT the old long section):
        v1.0 usaba n8n para orquestar el fetch y la entrega; en v2.0 la app hace su propio fetch (JSearch)
        y entrega el digest, así que n8n ya no es necesario. La documentación histórica se conserva en
        [`docs/archive/n8n/`](docs/archive/n8n/). DELETE the entire active "## Integración con n8n" section
        (the JSON contract for `/jobs/process`, the flujo Schedule→HTTP→Merge→POST, the "Nodos n8n por
        endpoint" table, the curl examples against `localhost:8000`, the `X-API-Key` notes) and DELETE the
        dead `docs/N8N-WORKFLOW.md` link — replace with the archive pointer above.

    GLOBAL deletions to verify: no remaining "FastAPI", "uvicorn", "Swagger", `/docs`, `/jobs/process`,
    `/cv/parse` curl, `localhost:8000`, `X-API-Key`, or `API_KEY`-as-auth references in the ACTIVE README
    (a passing historical mention inside the short n8n-legacy paragraph is acceptable, but the operational
    quick-start must be n8n/FastAPI-free). Do NOT invent secret values anywhere.
  </action>
  <verify>
    <automated>python - <<'PY'
import re, sys
t = open("README.md", encoding="utf-8").read()
errs = []
def need(s):
    if s not in t:
        errs.append(f"missing: {s!r}")
def need_ci(s):
    if s.lower() not in t.lower():
        errs.append(f"missing (ci): {s!r}")
# v2.0 must-haves
need("docker compose up")
need_ci("streamlit")
need("8501")
if not re.search(r"APScheduler|worker", t):
    errs.append("missing: APScheduler/worker")
need("docs/archive/n8n")
need_ci("RAPIDAPI_KEY")
need_ci("TELEGRAM_BOT_TOKEN")
need_ci("SMTP_HOST")
need_ci("OPENAI_API_KEY")
# v1.0 cruft must be gone from the active doc
if "app.api.main" in t:
    errs.append("still references FastAPI app.api.main (uvicorn run cmd)")
if "/jobs/process" in t:
    errs.append("still references /jobs/process endpoint")
if "X-API-Key" in t:
    errs.append("still references X-API-Key auth")
if "docs/N8N-WORKFLOW.md" in t:
    errs.append("still links dead docs/N8N-WORKFLOW.md path")
# no real secret VALUES — only placeholder env-var names allowed
PLACEHOLDERS = {"", "...", "<tu-valor>", "<valor>", "sk-...", "sk-proj-...", "pk-lf-...", "sk-lf-..."}
for m in re.finditer(r"(RAPIDAPI_KEY|OPENAI_API_KEY|TELEGRAM_BOT_TOKEN|SMTP_PASSWORD)\s*=\s*(\S+)", t):
    val = m.group(2)
    if val not in PLACEHOLDERS:
        errs.append(f"possible real secret value for {m.group(1)}: {val!r}")
# security note present
if not re.search(r"reverse[- ]proxy|sin autenticaci|no tiene autenticaci", t, re.IGNORECASE):
    errs.append("missing security note (no auth / reverse proxy)")
if errs:
    print("FAIL:")
    for e in errs:
        print("  -", e)
    sys.exit(1)
print("OK")
PY</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "docker compose up" README.md`
    - `grep -qi "streamlit" README.md` and `grep -q "8501" README.md`
    - `grep -Eq "APScheduler|worker" README.md` (worker process documented)
    - `grep -qi "RAPIDAPI_KEY" README.md` and `grep -qi "TELEGRAM_BOT_TOKEN" README.md` and `grep -qi "SMTP_HOST" README.md` and `grep -qi "OPENAI_API_KEY" README.md`
    - `grep -q "docs/archive/n8n" README.md` (n8n pointed at legacy archive)
    - `! grep -q "docs/N8N-WORKFLOW.md" README.md` (dead link removed)
    - `! grep -q "/jobs/process" README.md` and `! grep -q "app.api.main" README.md` and `! grep -q "X-API-Key" README.md` (FastAPI/n8n operational cruft removed from the active doc)
    - No real secret VALUES: `! grep -Eq "(RAPIDAPI_KEY|OPENAI_API_KEY|TELEGRAM_BOT_TOKEN|SMTP_PASSWORD)\s*=\s*sk-[A-Za-z0-9]{6,}" README.md` (only placeholder names appear)
    - Security note present: `grep -qiE "reverse[- ]proxy|sin autenticaci|no tiene autenticaci" README.md`
  </acceptance_criteria>
  <done>README.md describes the v2.0 standalone app (Streamlit UI + APScheduler worker via `docker compose up`), documents install/.env/usage/digest/security, contains only placeholder env-var names, and the active n8n/FastAPI integration prose is removed and replaced with a short legacy pointer to docs/archive/n8n/.</done>
</task>

<task type="auto">
  <name>Task 2: Add a legacy archive note at docs/archive/n8n/README.md</name>
  <read_first>
    - docs/archive/n8n/N8N-WORKFLOW.md (confirm it exists at this path — the archived workflow guide)
    - .planning/REQUIREMENTS.md (MIG-02: n8n retired/archived; DOC-04: section archived)
  </read_first>
  <files>docs/archive/n8n/README.md</files>
  <action>
    Create a short `docs/archive/n8n/README.md` (Spanish, a few lines) that:
    - States this folder is LEGACY / archived material from v1.0 and is no longer part of the active
      v2.0 app (which does its own fetch via JSearch and delivers the digest directly — n8n is not
      required).
    - Points to `N8N-WORKFLOW.md` (in this folder) as the historical step-by-step n8n workflow guide.
    - Notes that the active project docs live in the repo-root `README.md`.
    Keep it concise — this is a signpost, not a re-export of the old content. Use the word "legacy" so
    the must-have/grep gate is satisfied.
  </action>
  <verify>
    <automated>test -f docs/archive/n8n/N8N-WORKFLOW.md && grep -qi "legacy" docs/archive/n8n/README.md && grep -qi "N8N-WORKFLOW.md" docs/archive/n8n/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/archive/n8n/README.md` (the archive note exists)
    - `grep -qi "legacy" docs/archive/n8n/README.md`
    - `grep -qi "N8N-WORKFLOW.md" docs/archive/n8n/README.md` (points at the archived guide)
    - `grep -qiE "v2\.0|JSearch|ya no.*necesario|no.*requ" docs/archive/n8n/README.md` (explains n8n is retired in v2.0)
  </acceptance_criteria>
  <done>A concise legacy note exists at docs/archive/n8n/README.md explaining the n8n material is archived v1.0 history and pointing to N8N-WORKFLOW.md, with the active docs in the root README.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo (public/portfolio) → reader | README is committed and may be published; anything in it is world-readable |
| documented config → operator | the README tells the operator which env vars to set and how to expose the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | Information Disclosure | a real secret value (RAPIDAPI_KEY / OPENAI_API_KEY / TELEGRAM_BOT_TOKEN / SMTP_PASSWORD) pasted into the README and committed | mitigate | README documents env-var NAMES only with placeholders; the verify step + acceptance criteria fail if any real key value (e.g. `sk-...`) appears; the `.env`-not-committed note is restated |
| T-10-07-02 | Information Disclosure | README implies the Streamlit UI is safe to expose publicly (no auth) | mitigate | An explicit security note states the UI has no built-in auth and must sit behind a reverse proxy / auth / VPN if exposed beyond localhost |
| T-10-07-03 | Elevation of Privilege | stale docs tell the operator to enable retired FastAPI `X-API-Key`/`API_KEY` auth that no longer exists, giving a false sense of protection | mitigate | The old FastAPI auth rows and `X-API-Key` guidance are deleted; acceptance criteria assert `! grep X-API-Key` and no `app.api.main` run command |
| T-10-07-04 | Tampering | a broken `docs/N8N-WORKFLOW.md` link or wrong run command leads an operator to a dead/misleading path | mitigate | The dead link is replaced with the real `docs/archive/n8n/` pointer (verified by grep); doc-only surface, low residual risk beyond the corrected links |
</threat_model>

<verification>
- Task 1 `python - <<'PY' ...` README check exits 0: v2.0 keywords present, FastAPI/n8n operational cruft absent, no real secret values, security note present.
- `test -f docs/archive/n8n/README.md && grep -qi legacy docs/archive/n8n/README.md` (Task 2).
- Manual skim: README reads top-to-bottom as a v2.0 standalone guide (install → .env → docker compose up → use the 5 pages → digest → security), with n8n reduced to a one-paragraph legacy pointer.
</verification>

<success_criteria>
- DOC-04 satisfied: README rewritten for v2.0 standalone (install, `.env`, `docker compose up` for ui Streamlit + worker, configure search, upload CV, read digest); n8n integration archived/retired.
- D-13 satisfied: uv install, the exact env vars documented as placeholders, Docker ui+worker + shared volumes explained, in-app workflow documented, stale FastAPI/uvicorn/Swagger messaging removed.
- Security note present (no built-in auth → reverse proxy if exposed); no `X-API-Key`/`API_KEY` FastAPI auth references remain.
- No real secret values anywhere in the README; only placeholder env-var names.
- n8n prose removed from the active README and pointed to `docs/archive/n8n/`; dead `docs/N8N-WORKFLOW.md` link gone.
</success_criteria>

<output>
After completion, create `.planning/phases/10-streamlit-ui/10-07-SUMMARY.md`
</output>
</content>
